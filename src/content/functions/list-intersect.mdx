---
title: "List.Intersect"
slug: "list-intersect"
category: "List"
description: "Returns the intersection of a list of lists, containing only items that appear in every list."
syntax: "List.Intersect(lists as list, optional equationCriteria as any) as list"
returnType: "list"
returnDescription: "A list of distinct items that appear in all of the provided lists."
parameters:
  - name: "lists"
    type: "list"
    required: true
    description: "A list of lists to intersect."
  - name: "equationCriteria"
    type: "any"
    required: false
    description: "An optional equation criteria value to control how values are compared for equality."
compatibility:
  pbiDesktop: true
  pbiService: true
  excelDesktop: true
  excelOnline: true
  dataflows: true
  fabricNotebooks: true
relatedFunctions:
  - "list-union"
  - "list-difference"
  - "list-distinct"
  - "list-containsall"
---

## Remarks

`List.Intersect` takes a **list of lists** as its first argument — not two separate list arguments — and returns a new list containing only items that appear in every one of the provided lists. The result is automatically deduplicated (distinct values only).

This function is useful for finding common values across multiple datasets: customers who appear in all sales regions, product codes active in every store, or employees on every team. The result preserves the relative order of first-appearing values from the first list.

Note the argument shape: you must wrap your lists in an outer list — `{listA, listB}` — not pass them as separate arguments. If any input list is empty, the result is immediately an empty list (nothing can be in all lists if one is empty). Comparison is case-sensitive for text by default; pass `Comparer.OrdinalIgnoreCase` as the optional second argument for case-insensitive matching.

## Examples

### Example 1: Intersect two lists

```powerquery
List.Intersect({{1, 2, 3, 4}, {2, 4, 6}})
```

<!--output
{"columns":[{"name":"Result","type":"any"}],"rows":[{"Result":"{2, 4}"}]}
-->

### Example 2: Intersect three lists

```powerquery
List.Intersect({{"A", "B", "C"}, {"B", "C", "D"}, {"C", "D", "E"}})
```

<!--output
{"columns":[{"name":"Result","type":"any"}],"rows":[{"Result":"{C}"}]}
-->

### Example 3: Find common customers across regions

```powerquery
let
    RegionA = {"Cust-01", "Cust-02", "Cust-03"},
    RegionB = {"Cust-02", "Cust-03", "Cust-04"},
    RegionC = {"Cust-02", "Cust-05", "Cust-03"},
    CommonCustomers = List.Intersect({RegionA, RegionB, RegionC})
in
    CommonCustomers
```

<!--output
{"columns":[{"name":"Result","type":"any"}],"rows":[{"Result":"{Cust-02, Cust-03}"}]}
-->

<!--steps
{
  "steps": [
    {
      "name": "RegionA",
      "description": "Defines the list of customer IDs active in Region A.",
      "output": {"columns":[{"name":"Value","type":"text"}],"rows":[{"Value":"Cust-01"},{"Value":"Cust-02"},{"Value":"Cust-03"}]}
    },
    {
      "name": "RegionB",
      "description": "Defines the list of customer IDs active in Region B.",
      "output": {"columns":[{"name":"Value","type":"text"}],"rows":[{"Value":"Cust-02"},{"Value":"Cust-03"},{"Value":"Cust-04"}]}
    },
    {
      "name": "RegionC",
      "description": "Defines the list of customer IDs active in Region C.",
      "output": {"columns":[{"name":"Value","type":"text"}],"rows":[{"Value":"Cust-02"},{"Value":"Cust-05"},{"Value":"Cust-03"}]}
    },
    {
      "name": "CommonCustomers",
      "description": "The final output — intersects all three region lists to return only the customer IDs that appear in every region.",
      "output": {"columns":[{"name":"Result","type":"any"}],"rows":[{"Result":"{Cust-02, Cust-03}"}]}
    }
  ]
}
-->

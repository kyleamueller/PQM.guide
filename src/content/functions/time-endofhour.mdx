---
title: "Time.EndOfHour"
slug: "time-endofhour"
category: "Time"
description: "Returns the last moment of the hour for the given time or datetime value."
syntax: "Time.EndOfHour(dateTime as any) as any"
returnType: "any"
returnDescription: "A time, datetime, or datetimezone value representing XX:59:59.9999999."
parameters:
  - name: "dateTime"
    type: "any"
    required: true
    description: "A time, datetime, or datetimezone value."
compatibility:
  pbiDesktop: true
  pbiService: true
  excelDesktop: true
  excelOnline: true
  dataflows: true
  fabricNotebooks: true
relatedFunctions:
  - "time-startofhour"
  - "date-endofday"
  - "time-hour"
  - "time-from"
---

## Remarks

`Time.EndOfHour` returns the very last instant of the clock hour containing the input value: XX:59:59.9999999. When passed a plain `time`, it returns a `time`. When passed a `datetime` or `datetimezone`, it returns the same type with only the time component adjusted to the end of the hour — the date and any timezone offset are preserved unchanged.

Pair `Time.EndOfHour` with `Time.StartOfHour` using the same input to build inclusive hourly range filters. For example: `[EventTime] >= Time.StartOfHour(ref) and [EventTime] <= Time.EndOfHour(ref)` matches all records within the same clock hour as `ref`. This avoids off-by-one errors from using strictly less-than comparisons.

`Time.EndOfHour` is most useful in near-real-time or streaming data scenarios where events are bucketed by hour. For day-level boundaries, use `Date.EndOfDay`/`Date.StartOfDay` instead.

## Examples

### Example 1: Add end-of-hour boundaries to log timestamps

Add a column showing the last instant of the hour for each log entry.

Input: `OrderLog`

```powerquery
Table.AddColumn(
    Table.SelectColumns(Table.FirstN(OrderLog, 4), {"LogID", "Timestamp"}),
    "HourEnd", each Time.EndOfHour([Timestamp]), type datetime
)
```

<!--output
{"columns":[{"name":"LogID","type":"text"},{"name":"Timestamp","type":"datetime"},{"name":"HourEnd","type":"datetime"}],"rows":[{"LogID":"L001","Timestamp":"2024-01-15T09:30:00","HourEnd":"2024-01-15T09:59:59.9999999"},{"LogID":"L002","Timestamp":"2024-01-16T14:15:00","HourEnd":"2024-01-16T14:59:59.9999999"},{"LogID":"L003","Timestamp":"2024-01-18T11:00:00","HourEnd":"2024-01-18T11:59:59.9999999"},{"LogID":"L004","Timestamp":"2024-01-20T08:45:00","HourEnd":"2024-01-20T08:59:59.9999999"}]}
-->

### Example 2: End of hour for a specific time

Return the last instant of the 14:xx hour.

```powerquery
#table(
    type table [Result = time],
    {{Time.EndOfHour(#time(14, 30, 0))}}
)
```

<!--output
{"columns":[{"name":"Result","type":"time"}],"rows":[{"Result":"14:59:59.9999999"}]}
-->

### Example 3: Build an inclusive hourly range filter

Filter log entries to a specific clock hour using StartOfHour and EndOfHour boundaries.

```powerquery
let
    RefTime = #datetime(2024, 1, 15, 9, 0, 0),
    HourStart = Time.StartOfHour(RefTime),
    HourEnd = Time.EndOfHour(RefTime)
in
    Table.SelectRows(
        OrderLog,
        each [Timestamp] >= HourStart and [Timestamp] <= HourEnd
    )
```

<!--output
{"columns":[{"name":"LogID","type":"text"},{"name":"OrderID","type":"number"},{"name":"Action","type":"text"},{"name":"Timestamp","type":"datetime"},{"name":"DurationMinutes","type":"number"},{"name":"Notes","type":"text"}],"rows":[{"LogID":"L001","OrderID":1,"Action":"Created","Timestamp":"2024-01-15T09:30:00","DurationMinutes":null,"Notes":"New order: Widget A"}]}
-->

<!--steps
{
  "steps": [
    {
      "name": "RefTime",
      "description": "Defines the reference datetime (January 15, 2024 at 09:00:00) used to determine the target clock hour.",
      "output": {"columns":[{"name":"Value","type":"datetime"}],"rows":[{"Value":"2024-01-15T09:00:00"}]}
    },
    {
      "name": "HourStart",
      "description": "Computes the first instant of the 9 AM hour (09:00:00.0000000) using Time.StartOfHour.",
      "output": {"columns":[{"name":"Value","type":"datetime"}],"rows":[{"Value":"2024-01-15T09:00:00.0000000"}]}
    },
    {
      "name": "HourEnd",
      "description": "Computes the last instant of the 9 AM hour (09:59:59.9999999) using Time.EndOfHour.",
      "output": {"columns":[{"name":"Value","type":"datetime"}],"rows":[{"Value":"2024-01-15T09:59:59.9999999"}]}
    },
    {
      "name": "Result",
      "description": "The final output — filters OrderLog rows to those whose Timestamp falls within the inclusive 9 AM hour range.",
      "output": {"columns":[{"name":"LogID","type":"text"},{"name":"OrderID","type":"number"},{"name":"Action","type":"text"},{"name":"Timestamp","type":"datetime"},{"name":"DurationMinutes","type":"number"},{"name":"Notes","type":"text"}],"rows":[{"LogID":"L001","OrderID":1,"Action":"Created","Timestamp":"2024-01-15T09:30:00","DurationMinutes":null,"Notes":"New order: Widget A"}]}
    }
  ]
}
-->

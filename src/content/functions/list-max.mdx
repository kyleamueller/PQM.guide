---
title: "List.Max"
slug: "list-max"
category: "List"
description: "Returns the maximum value from a list."
syntax: "List.Max(list as list, optional default as nullable any, optional comparisonCriteria as any, optional includeNulls as nullable logical) as any"
returnType: "any"
returnDescription: "The largest value in the list, or the default value if the list is empty."
parameters:
  - name: "list"
    type: "list"
    required: true
    description: "The list to search."
  - name: "default"
    type: "any"
    required: false
    description: "Value returned when the list is empty. Defaults to null."
  - name: "comparisonCriteria"
    type: "any"
    required: false
    description: "A comparer or key selector for custom ordering."
  - name: "includeNulls"
    type: "logical"
    required: false
    description: "Whether to include null values in the comparison. Defaults to false."
compatibility:
  pbiDesktop: true
  pbiService: true
  excelDesktop: true
  excelOnline: true
  dataflows: true
  fabricNotebooks: true
relatedFunctions:
  - "list-min"
  - "list-average"
  - "list-sum"
---

## Remarks

`List.Max` returns the largest value in a list. Null values are excluded from comparison by default. If the list is empty, `null` is returned unless a `default` value is provided.

The optional `comparisonCriteria` parameter accepts a comparer function or a key selector (for example, `each Text.Length(_)` to find the longest text).

Use `List.Max` on a table column by first extracting it with `Table.Column`.

## Examples

### Example 1: Find the maximum of a list of numbers

```powerquery
List.Max({5, 3, 8, 1, 9})
```

<!--output
{"columns":[{"name":"Result","type":"number"}],"rows":[{"Result":9}]}
-->

### Example 2: Find the latest date in a column

```powerquery
let
    Dates  = Table.Column(Sales, "OrderDate"),
    Latest = List.Max(Dates)
in
    #table({"LatestOrder"}, {{Latest}})
```

<!--output
{"columns":[{"name":"LatestOrder","type":"date"}],"rows":[{"LatestOrder":"2024-12-30"}]}
-->

<!--steps
{
  "steps": [
    {
      "name": "Dates",
      "description": "Extracts the OrderDate column from the Sales table as a list of date values.",
      "output": {"columns":[{"name":"Value","type":"date"}],"rows":[{"Value":"2024-01-15"},{"Value":"2024-01-18"},{"Value":"2024-02-01"},{"Value":"2024-02-10"},{"Value":"2024-03-05"},{"Value":"2024-03-12"},{"Value":"2024-04-01"},{"Value":"2024-04-15"}]}
    },
    {
      "name": "Latest",
      "description": "Finds the maximum (most recent) date from the Dates list.",
      "output": {"columns":[{"name":"Value","type":"date"}],"rows":[{"Value":"2024-12-30"}]}
    },
    {
      "name": "Result",
      "description": "The final output — wraps the latest date into a single-row table with a \"LatestOrder\" column header.",
      "output": {"columns":[{"name":"LatestOrder","type":"date"}],"rows":[{"LatestOrder":"2024-12-30"}]}
    }
  ]
}
-->

---
title: "List.Random"
slug: "list-random"
category: "List"
description: "Returns a list of random numbers between 0 and 1, with an optional seed for reproducibility."
syntax: "List.Random(count as number, optional seed as nullable number) as list"
returnType: "list"
returnDescription: "A list of count random numbers, each between 0 (inclusive) and 1 (exclusive)."
parameters:
  - name: "count"
    type: "number"
    required: true
    description: "The number of random values to generate."
  - name: "seed"
    type: "nullable number"
    required: false
    description: "An optional integer seed for the random number generator. Using the same seed produces the same sequence."
compatibility:
  pbiDesktop: true
  pbiService: true
  excelDesktop: true
  excelOnline: true
  dataflows: true
  fabricNotebooks: true
relatedFunctions:
  - "list-numbers"
  - "list-transform"
  - "list-count"
---

## Remarks

`List.Random` generates a list of pseudorandom numbers uniformly distributed between 0 (inclusive) and 1 (exclusive). This is useful for sampling, simulations, randomized testing, shuffling data, and adding noise.

Without a `seed`, `List.Random` is **non-deterministic**: each query refresh produces a different sequence of values. This means the results will change every time the query is evaluated, including between previews. If you need stable values within a session (for example, to use the same random values in multiple calculations), wrap the result in `List.Buffer` immediately after generating it.

With a fixed `seed`, the same sequence is produced on every evaluation. Use a seed when you need reproducible results for testing or documentation purposes.

To generate random integers in a range `[min, max)`, use `List.Transform(List.Random(n), each Number.RoundDown(_ * (max - min)) + min)`. To simulate random boolean values (coin flips), use `List.Transform(List.Random(n), each _ >= 0.5)`.

## Examples

### Example 1: Generate 5 random numbers

```powerquery
List.Random(5)
```

<!--output
{"columns":[{"name":"Result","type":"any"}],"rows":[{"Result":"{0.347, 0.812, 0.091, 0.563, 0.724}"}]}
-->

### Example 2: Generate reproducible random numbers with a seed

```powerquery
List.Random(5, 42)
```

<!--output
{"columns":[{"name":"Result","type":"any"}],"rows":[{"Result":"{0.634, 0.218, 0.879, 0.456, 0.123}"}]}
-->

### Example 3: Generate random integers between 1 and 100

```powerquery
let
    Randoms = List.Random(5, 1),
    Integers = List.Transform(Randoms, each Number.RoundDown(_ * 100) + 1)
in
    Integers
```

<!--output
{"columns":[{"name":"Result","type":"any"}],"rows":[{"Result":"{64, 22, 88, 46, 13}"}]}
-->

<!--steps
{
  "steps": [
    {
      "name": "Randoms",
      "description": "Generates a list of 5 random decimal numbers between 0 and 1 using seed 1 for reproducibility.",
      "output": {"columns":[{"name":"Value","type":"number"}],"rows":[{"Value":0.64},{"Value":0.22},{"Value":0.88},{"Value":0.46},{"Value":0.13}]}
    },
    {
      "name": "Integers",
      "description": "Transforms each decimal in Randoms by multiplying by 100 and rounding down, then adding 1 to produce integers in the range 1–100.",
      "output": {"columns":[{"name":"Result","type":"any"}],"rows":[{"Result":"{64, 22, 88, 46, 13}"}]}
    },
    {
      "name": "Result",
      "description": "The final output — a list of 5 random integers between 1 and 100 generated from seed 1.",
      "output": {"columns":[{"name":"Result","type":"any"}],"rows":[{"Result":"{64, 22, 88, 46, 13}"}]}
    }
  ]
}
-->

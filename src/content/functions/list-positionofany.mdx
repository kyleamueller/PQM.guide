---
title: "List.PositionOfAny"
slug: "list-positionofany"
category: "List"
description: "Returns the position of the first item in a list that matches any value from a second list."
syntax: "List.PositionOfAny(list as list, values as list, optional occurrence as nullable number, optional equationCriteria as any) as any"
returnType: "any"
returnDescription: "The zero-based index of the first matching item, or -1 if no match is found."
parameters:
  - name: "list"
    type: "list"
    required: true
    description: "The list to search."
  - name: "values"
    type: "list"
    required: true
    description: "The list of values to search for."
  - name: "occurrence"
    type: "nullable number"
    required: false
    description: "Controls which occurrence to find: Occurrence.First (default), Occurrence.Last, or Occurrence.All."
  - name: "equationCriteria"
    type: "any"
    required: false
    description: "An optional equation criteria to control how equality is determined."
compatibility:
  pbiDesktop: true
  pbiService: true
  excelDesktop: true
  excelOnline: true
  dataflows: true
  fabricNotebooks: true
relatedFunctions:
  - "list-positionof"
  - "list-containsany"
  - "list-positions"
  - "list-findtext"
---

## Remarks

`List.PositionOfAny` searches a list for the first item that matches **any** value from a second list of targets. It is the multi-target version of `List.PositionOf` — useful when you need to find where any of several values first appears in a sequence.

Returns `-1` if none of the target values are found. The `occurrence` parameter controls which match to return: `Occurrence.First` (default) for the earliest match, `Occurrence.Last` for the latest, and `Occurrence.All` to return a list of all positions where any target appears. When using `Occurrence.All`, the return type changes from a number to a list of numbers.

Comparison is case-sensitive for text by default. This function is particularly useful for finding the position of the first delimiter or separator in a list, or for locating the first occurrence of any of a set of sentinel values. For checking presence without needing position, `List.ContainsAny` is simpler.

## Examples

### Example 1: Find position of first match from a set of values

```powerquery
List.PositionOfAny({"A", "B", "C", "D", "E"}, {"C", "E"})
```

<!--output
{"columns":[{"name":"Result","type":"number"}],"rows":[{"Result":2}]}
-->

### Example 2: No match found

```powerquery
List.PositionOfAny({10, 20, 30}, {99, 100})
```

<!--output
{"columns":[{"name":"Result","type":"number"}],"rows":[{"Result":-1}]}
-->

### Example 3: Find all positions of any matching value

```powerquery
List.PositionOfAny({"A", "B", "C", "A", "D", "B"}, {"A", "B"}, Occurrence.All)
```

<!--output
{"columns":[{"name":"Result","type":"any"}],"rows":[{"Result":"{0, 1, 3, 5}"}]}
-->

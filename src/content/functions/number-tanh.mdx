---
title: "Number.Tanh"
slug: "number-tanh"
category: "Number"
description: "Returns the hyperbolic tangent of a number."
syntax: "Number.Tanh(number as nullable number) as nullable number"
returnType: "number"
returnDescription: "The hyperbolic tangent of the number, in the range (-1, 1)."
parameters:
  - name: "number"
    type: "number"
    required: true
    description: "The number whose hyperbolic tangent is computed."
compatibility:
  pbiDesktop: true
  pbiService: true
  excelDesktop: true
  excelOnline: true
  dataflows: true
  fabricNotebooks: true
relatedFunctions:
  - "number-sinh"
  - "number-cosh"
  - "number-exp"
---

## Remarks

`Number.Tanh` returns the hyperbolic tangent, defined mathematically as:

tanh(x) = sinh(x) / cosh(x) = (e^x − e^(−x)) / (e^x + e^(−x))

The result is always in the open interval (-1, 1). As x approaches +∞, tanh(x) approaches 1 asymptotically; as x approaches -∞, it approaches -1. At x = 0 the result is exactly 0. The function is **odd**: `tanh(-x) = -tanh(x)`.

A practical feature of `Number.Tanh` is that it provides a **smooth, bounded normalization** of any real number to the range (-1, 1) without a hard clamp. This makes it useful in Power Query when you need to squash outliers while preserving the ordering and sign of values. Values close to zero change nearly linearly (slope ≈ 1 at x=0), while extreme values get compressed toward ±1. For normalizing to (0, 1) instead, the logistic function `1 / (1 + Number.Exp(-x))` is the standard alternative.

Unlike circular trigonometric functions, hyperbolic functions do not take angles in radians or degrees — they accept any real number.

## Examples

### Example 1: Hyperbolic tangent of 0 is 0

```powerquery
Number.Tanh(0)
```

<!--output
{"columns":[{"name":"Result","type":"number"}],"rows":[{"Result":0}]}
-->

### Example 2: Large inputs approach ±1 asymptotically

```powerquery
Number.Tanh(10)
```

<!--output
{"columns":[{"name":"Result","type":"number"}],"rows":[{"Result":0.9999999958776927}]}
-->

### Example 3: Normalize revenue z-scores to (-1, 1) using tanh

```powerquery
let
    Scores = {-3.0, -1.0, 0.0, 1.0, 3.0},
    Normalized = List.Transform(Scores, Number.Tanh)
in
    #table({"ZScore", "Tanh"}, List.Zip({Scores, Normalized}))
```

<!--output
{"columns":[{"name":"ZScore","type":"number"},{"name":"Tanh","type":"number"}],"rows":[{"ZScore":-3,"Tanh":-0.9950547536867305},{"ZScore":-1,"Tanh":-0.7615941559557649},{"ZScore":0,"Tanh":0},{"ZScore":1,"Tanh":0.7615941559557649},{"ZScore":3,"Tanh":0.9950547536867305}]}
-->
<!--steps
{
  "steps": [
    {
      "name": "Scores",
      "description": "Defines a list of five z-score values spanning from -3 to 3 to demonstrate the tanh normalization range.",
      "output": {"columns":[{"name":"Value","type":"number"}],"rows":[{"Value":-3},{"Value":-1},{"Value":0},{"Value":1},{"Value":3}]}
    },
    {
      "name": "Normalized",
      "description": "Applies Number.Tanh to every element of the Scores list, transforming each z-score into a value in the open interval (-1, 1).",
      "output": {"columns":[{"name":"Value","type":"number"}],"rows":[{"Value":-0.9950547536867305},{"Value":-0.7615941559557649},{"Value":0},{"Value":0.7615941559557649},{"Value":0.9950547536867305}]}
    },
    {
      "name": "Result",
      "description": "The final output — a two-column table pairing each original z-score with its tanh-normalized equivalent.",
      "output": {"columns":[{"name":"ZScore","type":"number"},{"name":"Tanh","type":"number"}],"rows":[{"ZScore":-3,"Tanh":-0.9950547536867305},{"ZScore":-1,"Tanh":-0.7615941559557649},{"ZScore":0,"Tanh":0},{"ZScore":1,"Tanh":0.7615941559557649},{"ZScore":3,"Tanh":0.9950547536867305}]}
    }
  ]
}
-->

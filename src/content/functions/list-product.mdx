---
title: "List.Product"
slug: "list-product"
category: "List"
description: "Returns the product of all numbers in a list."
syntax: "List.Product(numberList as list, optional precision as nullable number) as nullable number"
returnType: "nullable number"
returnDescription: "The product of all numeric values in the list, or null if the list is empty."
parameters:
  - name: "numberList"
    type: "list"
    required: true
    description: "The list of numbers to multiply together."
  - name: "precision"
    type: "nullable number"
    required: false
    description: "An optional precision value. Use Precision.Double for IEEE double-precision arithmetic."
compatibility:
  pbiDesktop: true
  pbiService: true
  excelDesktop: true
  excelOnline: true
  dataflows: true
  fabricNotebooks: true
relatedFunctions:
  - "list-sum"
  - "list-average"
  - "list-accumulate"
  - "list-numbers"
---

## Remarks

`List.Product` multiplies all values in a numeric list together and returns the result. This is the multiplicative equivalent of `List.Sum`. Common use cases include compound growth calculations (multiplying a series of rate factors), factorial-style computations, and scenarios where values are expressed as multipliers.

Returns `null` if the list is empty. Null values in the list are skipped — they do not contribute to the product and do not cause an error. Including `0` in the list produces a result of `0` regardless of other values.

The optional `precision` parameter accepts `Precision.Double` to use IEEE 754 double-precision floating-point arithmetic. This can differ from the default decimal arithmetic for very large or very small numbers, and may be needed for interoperability with systems that use double-precision math. For most business data, the default is sufficient.

## Examples

### Example 1: Product of a simple list

```powerquery
List.Product({1, 2, 3, 4, 5})
```

<!--output
{"columns":[{"name":"Result","type":"number"}],"rows":[{"Result":120}]}
-->

### Example 2: Compound growth calculation

Calculate the cumulative growth factor over 4 years with annual growth rates.

```powerquery
let
    GrowthRates = {1.05, 1.03, 1.08, 1.02},
    CumulativeGrowth = List.Product(GrowthRates)
in
    CumulativeGrowth
```

<!--output
{"columns":[{"name":"Result","type":"number"}],"rows":[{"Result":1.1940588}]}
-->

<!--steps
{
  "steps": [
    {
      "name": "GrowthRates",
      "description": "Defines a list of four annual growth factors representing 5%, 3%, 8%, and 2% growth respectively.",
      "output": {"columns":[{"name":"Value","type":"number"}],"rows":[{"Value":1.05},{"Value":1.03},{"Value":1.08},{"Value":1.02}]}
    },
    {
      "name": "CumulativeGrowth",
      "description": "Multiplies all growth factors together to compute the total compounded growth over 4 years (1.05 × 1.03 × 1.08 × 1.02 ≈ 1.1940588).",
      "output": {"columns":[{"name":"Value","type":"number"}],"rows":[{"Value":1.1940588}]}
    },
    {
      "name": "Result",
      "description": "The final output — the cumulative growth factor after applying all four annual rates.",
      "output": {"columns":[{"name":"Result","type":"number"}],"rows":[{"Result":1.1940588}]}
    }
  ]
}
-->

### Example 3: Product with a zero

```powerquery
List.Product({5, 10, 0, 3})
```

<!--output
{"columns":[{"name":"Result","type":"number"}],"rows":[{"Result":0}]}
-->

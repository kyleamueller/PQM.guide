---
title: "Record.TransformFields"
slug: "record-transformfields"
category: "Record"
description: "Applies transformation functions to specified fields in a record, returning a new record with the transformed values."
syntax: "Record.TransformFields(record as record, transformOperations as list, optional missingField as nullable number) as record"
returnType: "record"
returnDescription: "A new record with the specified fields transformed."
parameters:
  - name: "record"
    type: "record"
    required: true
    description: "The source record."
  - name: "transformOperations"
    type: "list"
    required: true
    description: "A list of transformation operations. Each element is a two-item list {fieldName, transformFunction} or a three-item list {fieldName, transformFunction, newFieldName}."
  - name: "missingField"
    type: "number"
    required: false
    description: "Controls behavior when a specified field does not exist: MissingField.Error (default), MissingField.Ignore, or MissingField.UseNull."
compatibility:
  pbiDesktop: true
  pbiService: true
  excelDesktop: true
  excelOnline: true
  dataflows: true
  fabricNotebooks: true
relatedFunctions:
  - "record-renamefields"
  - "record-addfield"
  - "record-removefields"
  - "record-selectfields"
---

## Remarks

`Record.TransformFields` applies transformation functions to specified fields in a record and returns a new record with the results. Each item in the `transformOperations` list describes one transformation:

- `{fieldName, function}` — a two-element list that applies `function` to the named field in place, keeping the same field name
- `{fieldName, function, newFieldName}` — a three-element list that applies `function` to the named field and renames it to `newFieldName`

The transform function receives the current field value and must return the new value. Fields not listed in `transformOperations` are passed through unchanged. All transformations are applied to the original record — they are not chained sequentially — so the order of operations within the list does not matter.

`Record.TransformFields` is the record-level equivalent of `Table.TransformColumns`. Use it when you need to modify one or more field values within a record without constructing a new record literal manually. The optional `missingField` parameter controls behavior when a listed field does not exist: `MissingField.Error` (default, raises an error), `MissingField.Ignore` (skips the operation silently), or `MissingField.UseNull` (applies the function to null).

A common pattern is to use this function in `Table.TransformRows` or inside `each` expressions to normalize record fields while processing rows from a table.

## Examples

### Example 1: Uppercase a text field in a record

```powerquery
Record.TransformFields(
    [Name = "alice", Age = 30],
    {{"Name", Text.Upper}}
)
```

<!--output
{"columns":[{"name":"Name","type":"text"},{"name":"Age","type":"number"}],"rows":[{"Name":"ALICE","Age":30}]}
-->

### Example 2: Transform multiple fields at once

```powerquery
Record.TransformFields(
    [Name = "bob", Age = 25.7, City = "seattle"],
    {
        {"Name", Text.Proper},
        {"Age", Number.RoundDown},
        {"City", Text.Proper}
    }
)
```

<!--output
{"columns":[{"name":"Name","type":"text"},{"name":"Age","type":"number"},{"name":"City","type":"text"}],"rows":[{"Name":"Bob","Age":25,"City":"Seattle"}]}
-->

### Example 3: Transform and rename a field using Sales data

Input: `Sales`

Compute line total from a Sales row and rename `UnitPrice` to `LineTotal` in a single operation:

```powerquery
let
    Row = Sales{0},
    Transformed = Record.TransformFields(
        Row,
        {{"UnitPrice", each _ * Row[Quantity], "LineTotal"}}
    )
in
    Record.SelectFields(Transformed, {"OrderID", "CustomerName", "LineTotal"})
```

<!--output
{"columns":[{"name":"OrderID","type":"number"},{"name":"CustomerName","type":"text"},{"name":"LineTotal","type":"number"}],"rows":[{"OrderID":1,"CustomerName":"Alice","LineTotal":100}]}
-->

<!--steps
{
  "steps": [
    {
      "name": "Row",
      "description": "Retrieves the first row of the Sales table as a record containing all eight fields.",
      "output": {"columns":[{"name":"Field","type":"text"},{"name":"Value","type":"any"}],"rows":[{"Field":"OrderID","Value":1},{"Field":"CustomerName","Value":"Alice"},{"Field":"Product","Value":"Widget A"},{"Field":"Category","Value":"Widgets"},{"Field":"UnitPrice","Value":25},{"Field":"Quantity","Value":4},{"Field":"OrderDate","Value":"2024-01-15"},{"Field":"Region","Value":"East"}]}
    },
    {
      "name": "Transformed",
      "description": "Transforms the UnitPrice field by multiplying it by Quantity to compute the line total, and renames the field to LineTotal in one operation.",
      "output": {"columns":[{"name":"Field","type":"text"},{"name":"Value","type":"any"}],"rows":[{"Field":"OrderID","Value":1},{"Field":"CustomerName","Value":"Alice"},{"Field":"Product","Value":"Widget A"},{"Field":"Category","Value":"Widgets"},{"Field":"LineTotal","Value":100},{"Field":"Quantity","Value":4},{"Field":"OrderDate","Value":"2024-01-15"},{"Field":"Region","Value":"East"}]}
    },
    {
      "name": "Result",
      "description": "The final output — selects only OrderID, CustomerName, and LineTotal from the transformed record.",
      "output": {"columns":[{"name":"OrderID","type":"number"},{"name":"CustomerName","type":"text"},{"name":"LineTotal","type":"number"}],"rows":[{"OrderID":1,"CustomerName":"Alice","LineTotal":100}]}
    }
  ]
}
-->

---
title: "Number.Atan2"
slug: "number-atan2"
category: "Number"
description: "Returns the angle in radians between the positive x-axis and the point (x, y), correctly handling all four quadrants."
syntax: "Number.Atan2(y as nullable number, x as nullable number) as nullable number"
returnType: "number"
returnDescription: "The angle in radians in the range (-π, π]."
parameters:
  - name: "y"
    type: "number"
    required: true
    description: "The y-coordinate (vertical component)."
  - name: "x"
    type: "number"
    required: true
    description: "The x-coordinate (horizontal component)."
compatibility:
  pbiDesktop: true
  pbiService: true
  excelDesktop: true
  excelOnline: true
  dataflows: true
  fabricNotebooks: true
relatedFunctions:
  - "number-atan"
  - "number-sin"
  - "number-cos"
---

## Remarks

`Number.Atan2(y, x)` computes the angle θ (in **radians**) between the positive x-axis and the point (x, y). The result is in the range (-π, π]. This is the two-argument form of the arctangent that correctly handles all four quadrants by considering the signs of both x and y independently.

**Critical: the parameter order is y first, then x.** This is the standard mathematical convention and matches how `ATAN2` works in Excel. A common mistake is reversing the arguments — `Number.Atan2(x, y)` will give the wrong angle for any non-symmetric point.

The advantage over `Number.Atan(y/x)` is quadrant correctness: for the point (-3, -4), `y/x = 4/3` and `Number.Atan(4/3)` gives ~53°, but the correct angle is ~−127° (third quadrant). `Number.Atan2(-4, -3)` correctly returns ~−127°. Additionally, `Number.Atan2` handles the case `x = 0` without division-by-zero — it returns ±π/2 depending on the sign of y.

To convert the radian result to degrees, multiply by `180 / Number.PI`. All Power Query M trigonometric functions work in radians, not degrees.

## Examples

### Example 1: Angle to the point (1, 1) — 45 degrees

```powerquery
Number.Atan2(1, 1) * 180 / Number.PI
```

<!--output
{"columns":[{"name":"Result","type":"number"}],"rows":[{"Result":45}]}
-->

### Example 2: Angle to a negative-x point (180 degrees)

```powerquery
Number.Atan2(0, -1) * 180 / Number.PI
```

<!--output
{"columns":[{"name":"Result","type":"number"}],"rows":[{"Result":180}]}
-->

### Example 3: Compute bearing angle in degrees to a 2D coordinate

Compute the angle from the origin to the point (x=3, y=4) — approximately 53 degrees from the positive x-axis.

```powerquery
let
    X = 3,
    Y = 4,
    AngleDegrees = Number.Atan2(Y, X) * 180 / Number.PI
in
    AngleDegrees
```

<!--output
{"columns":[{"name":"Result","type":"number"}],"rows":[{"Result":53.13010235415598}]}
-->
<!--steps
{
  "steps": [
    {
      "name": "AngleDegrees",
      "description": "Calls Number.Atan2 with Y=4 and X=3 to get the angle in radians, then multiplies by 180/Number.PI to convert to degrees, giving approximately 53.13°.",
      "output": {"columns":[{"name":"Value","type":"number"}],"rows":[{"Value":53.13010235415598}]}
    },
    {
      "name": "Result",
      "description": "The final output — the bearing angle in degrees from the positive x-axis to the point (3, 4).",
      "output": {"columns":[{"name":"Result","type":"number"}],"rows":[{"Result":53.13010235415598}]}
    }
  ]
}
-->

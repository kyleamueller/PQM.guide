---
title: "List.SingleOrDefault"
slug: "list-singleordefault"
category: "List"
description: "Returns the single element of a list, or a default value if the list is empty. Throws an error if the list has more than one element."
syntax: "List.SingleOrDefault(list as list, optional default as nullable any) as any"
returnType: "any"
returnDescription: "The single item in the list, the default value if the list is empty, or an error if more than one item exists."
parameters:
  - name: "list"
    type: "list"
    required: true
    description: "The list expected to contain zero or one elements."
  - name: "default"
    type: "nullable any"
    required: false
    description: "The value to return if the list is empty. Defaults to null if not specified."
compatibility:
  pbiDesktop: true
  pbiService: true
  excelDesktop: true
  excelOnline: true
  dataflows: true
  fabricNotebooks: true
relatedFunctions:
  - "list-single"
  - "list-first"
  - "list-isempty"
  - "list-count"
---

## Remarks

`List.SingleOrDefault` is a null-safe version of `List.Single`. It returns the single element from the list if exactly one exists, or returns the `default` value (defaulting to `null` if omitted) if the list is empty. If the list has more than one element, it still throws an error — the "or default" only applies to the empty-list case.

This is the right choice for lookups where no match is valid and should return a fallback value, but multiple matches indicate a data quality issue that should surface as an error. It documents intent clearly: "I expect zero or one results; more than one is a bug."

- If `default` is omitted, an empty list returns `null`.
- A list with more than one element throws: `Expression.Error: There were too many elements in the enumeration.`
- To silently take the first of multiple results, use `List.First` with a `default` parameter.
- To require exactly one result (no empty allowed), use `List.Single`.

## Examples

### Example 1: Single element returns that element

```powerquery
List.SingleOrDefault({99})
```

<!--output
{"columns":[{"name":"Result","type":"number"}],"rows":[{"Result":99}]}
-->

### Example 2: Empty list returns null (default)

```powerquery
List.SingleOrDefault({})
```

<!--output
{"columns":[{"name":"Result","type":"any"}],"rows":[{"Result":"null"}]}
-->

### Example 3: Empty list returns a custom default

```powerquery
List.SingleOrDefault({}, -1)
```

<!--output
{"columns":[{"name":"Result","type":"number"}],"rows":[{"Result":-1}]}
-->

### Example 4: Safe lookup with fallback

```powerquery
let
    Prices = #table({"Product", "Price"}, {{"Widget", 9.99}, {"Gadget", 24.99}}),
    LookupProduct = "Thingamajig",
    MatchedPrice = List.SingleOrDefault(
        Table.SelectRows(Prices, each [Product] = LookupProduct)[Price],
        0
    )
in
    MatchedPrice
```

<!--output
{"columns":[{"name":"Result","type":"number"}],"rows":[{"Result":0}]}
-->
<!--steps
{
  "steps": [
    {
      "name": "Prices",
      "description": "Creates an inline two-column table with Product and Price entries for Widget and Gadget.",
      "output": {"columns":[{"name":"Product","type":"text"},{"name":"Price","type":"number"}],"rows":[{"Product":"Widget","Price":9.99},{"Product":"Gadget","Price":24.99}]}
    },
    {
      "name": "LookupProduct",
      "description": "Stores the text value \"Thingamajig\" as the product name to look up — a product that does not exist in the table.",
      "output": {"columns":[{"name":"Value","type":"text"}],"rows":[{"Value":"Thingamajig"}]}
    },
    {
      "name": "MatchedPrice",
      "description": "Filters Prices for rows matching LookupProduct, extracts the Price column, and calls List.SingleOrDefault with a fallback of 0 — returning 0 because no rows matched.",
      "output": {"columns":[{"name":"Value","type":"number"}],"rows":[{"Value":0}]}
    },
    {
      "name": "Result",
      "description": "The final output — the fallback value 0, returned because \"Thingamajig\" was not found in the Prices table.",
      "output": {"columns":[{"name":"Result","type":"number"}],"rows":[{"Result":0}]}
    }
  ]
}
-->

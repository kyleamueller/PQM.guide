---
title: "Date.IsInNextNWeeks"
slug: "date-isinnextnweeks"
category: "Date"
description: "Returns true if the date falls within the next N calendar weeks."
syntax: "Date.IsInNextNWeeks(dateTime as any, weeks as number) as logical"
returnType: "logical"
returnDescription: "true if the date falls within the next N weeks after the current week, false otherwise."
parameters:
  - name: "dateTime"
    type: "any"
    required: true
    description: "A date, datetime, or datetimezone value to test."
  - name: "weeks"
    type: "number"
    required: true
    description: "The number of weeks ahead to include. Must be a positive integer."
compatibility:
  pbiDesktop: true
  pbiService: true
  excelDesktop: true
  excelOnline: true
  dataflows: true
  fabricNotebooks: true
relatedFunctions:
  - "date-isinnextweek"
  - "date-isinpreviousnweeks"
  - "date-isinnextndays"
  - "date-isinnextnmonths"
---

## Remarks

`Date.IsInNextNWeeks` returns `true` if the input date falls within the next N complete Sunday–Saturday calendar weeks after the current week. The current week itself is not included; use `Date.IsInCurrentWeek` to also match the current week. The function is re-evaluated on each query refresh.

Weeks are always Sunday-to-Saturday boundaries — there is no parameter to use Monday-based weeks. If your organization uses Monday-based weeks, build the filter manually using `Date.StartOfWeek(Date.From(DateTime.LocalNow()), Day.Monday)`.

This function counts complete weeks, not a rolling 7-day window. For a rolling N-day window from tomorrow, use `Date.IsInNextNDays(n * 7)` instead.

## Examples

### Example 1: Find orders due in the next 2 weeks

Keep only rows where OrderDate falls within the next 2 complete Sunday–Saturday weeks.

Input: `Sales`

```powerquery
Table.SelectRows(
    Sales,
    each Date.IsInNextNWeeks([OrderDate], 2)
)
```

<!--output
{"columns":[{"name":"OrderID","type":"number"},{"name":"CustomerName","type":"text"},{"name":"OrderDate","type":"date"}],"rows":[{"OrderID":43,"CustomerName":"Alice","OrderDate":"2026-03-15"},{"OrderID":44,"CustomerName":"Bob","OrderDate":"2026-03-20"}]}
-->

### Example 2: Flag orders due in the next 4 weeks

Add a boolean column for orders due within the next 4 complete weeks.

Input: `Sales`

```powerquery
Table.AddColumn(
    Sales,
    "DueInNext4Weeks", each Date.IsInNextNWeeks([OrderDate], 4), type logical
)
```

<!--output
{"columns":[{"name":"OrderID","type":"number"},{"name":"OrderDate","type":"date"},{"name":"DueInNext4Weeks","type":"logical"}],"rows":[{"OrderID":42,"OrderDate":"2026-03-08","DueInNext4Weeks":false},{"OrderID":43,"OrderDate":"2026-03-15","DueInNext4Weeks":true},{"OrderID":46,"OrderDate":"2026-04-15","DueInNext4Weeks":false}]}
-->

### Example 3: Combine current and next 2 weeks for a 3-week window

Flag rows falling in either the current week or the next 2 weeks.

Input: `Sales`

```powerquery
Table.SelectRows(
    Sales,
    each Date.IsInCurrentWeek([OrderDate]) or Date.IsInNextNWeeks([OrderDate], 2)
)
```

<!--output
{"columns":[{"name":"OrderID","type":"number"},{"name":"CustomerName","type":"text"},{"name":"OrderDate","type":"date"}],"rows":[{"OrderID":42,"CustomerName":"Charlie","OrderDate":"2026-03-08"},{"OrderID":43,"CustomerName":"Alice","OrderDate":"2026-03-15"},{"OrderID":44,"CustomerName":"Bob","OrderDate":"2026-03-20"}]}
-->

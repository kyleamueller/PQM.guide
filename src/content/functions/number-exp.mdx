---
title: "Number.Exp"
slug: "number-exp"
category: "Number"
description: "Returns e raised to the power of a number (the inverse of Number.Ln)."
syntax: "Number.Exp(number as nullable number) as nullable number"
returnType: "number"
returnDescription: "e raised to the power of the given number."
parameters:
  - name: "number"
    type: "number"
    required: true
    description: "The exponent to which e is raised."
compatibility:
  pbiDesktop: true
  pbiService: true
  excelDesktop: true
  excelOnline: true
  dataflows: true
  fabricNotebooks: true
relatedFunctions:
  - "number-ln"
  - "number-log"
  - "number-power"
---

## Remarks

`Number.Exp` computes e^x, where e is Euler's number (approximately 2.71828182845904523). It is the **inverse of `Number.Ln`**: `Number.Exp(Number.Ln(x)) = x` for any positive x, and `Number.Ln(Number.Exp(x)) = x` for any real x.

`Number.Exp(0) = 1` and `Number.Exp(1) = e`. For large positive x, the result grows extremely rapidly and overflows to infinity around x ≈ 709. For large negative x, the result approaches 0. These limits are inherent to double-precision floating-point.

Common uses in data transformation include continuous compounding in finance (`FV = PV * e^(r*t)`), exponential growth and decay models, and softmax-style normalization. The exponential function is also the building block for `Number.Cosh` and `Number.Sinh`, defined as `(e^x ± e^(-x)) / 2`. Note that `Number.Power(Number.E, x)` is equivalent but less precise and slower than `Number.Exp(x)` for most implementations.

## Examples

### Example 1: e raised to the power of 1 — returns Euler's number

```powerquery
Number.Exp(1)
```

<!--output
{"columns":[{"name":"Result","type":"number"}],"rows":[{"Result":2.718281828459045}]}
-->

### Example 2: e raised to the power of 0 always returns 1

```powerquery
Number.Exp(0)
```

<!--output
{"columns":[{"name":"Result","type":"number"}],"rows":[{"Result":1}]}
-->

### Example 3: Continuous compounding — compute final investment value

Principal of $1,000 at 5% annual rate continuously compounded for 3 years: FV = P × e^(r×t).

```powerquery
let
    Principal = 1000,
    Rate = 0.05,
    Years = 3,
    FinalValue = Principal * Number.Exp(Rate * Years)
in
    FinalValue
```

<!--output
{"columns":[{"name":"Result","type":"number"}],"rows":[{"Result":1161.8341765024749}]}
-->
<!--steps
{
  "steps": [
    {
      "name": "Principal",
      "description": "Assigns the initial investment amount of $1,000.",
      "output": {"columns":[{"name":"Value","type":"number"}],"rows":[{"Value":1000}]}
    },
    {
      "name": "Rate",
      "description": "Assigns the annual interest rate of 5% expressed as a decimal (0.05).",
      "output": {"columns":[{"name":"Value","type":"number"}],"rows":[{"Value":0.05}]}
    },
    {
      "name": "Years",
      "description": "Assigns the investment duration of 3 years.",
      "output": {"columns":[{"name":"Value","type":"number"}],"rows":[{"Value":3}]}
    },
    {
      "name": "FinalValue",
      "description": "Applies the continuous compounding formula P × e^(r×t): multiplies $1,000 by e^(0.05 × 3) = e^0.15 ≈ 1.16183, yielding approximately $1,161.83.",
      "output": {"columns":[{"name":"Value","type":"number"}],"rows":[{"Value":1161.8341765024749}]}
    },
    {
      "name": "Result",
      "description": "The final output — the continuously compounded future value of the investment after 3 years.",
      "output": {"columns":[{"name":"Result","type":"number"}],"rows":[{"Result":1161.8341765024749}]}
    }
  ]
}
-->

---
title: "Type.TableColumn"
slug: "type-tablecolumn"
category: "Type"
description: "Returns the type of a specific column in a table type."
syntax: "Type.TableColumn(type as type, column as text) as type"
returnType: "type"
returnDescription: "The type of the specified column in the table type."
parameters:
  - name: "type"
    type: "type"
    required: true
    description: "A table type whose column type is queried."
  - name: "column"
    type: "text"
    required: true
    description: "The name of the column whose type is returned."
compatibility:
  pbiDesktop: true
  pbiService: true
  excelDesktop: true
  excelOnline: true
  dataflows: true
  fabricNotebooks: true
relatedFunctions:
  - "type-tableschema"
  - "type-tablekeys"
  - "value-type"
  - "table-transformcolumntypes"
---

## Remarks

`Type.TableColumn` returns the declared type of a named column within a table type. This function operates on a **table type value** — not the table itself. To obtain a table's type, call `Value.Type(table)` and pass the result to `Type.TableColumn`.

If the specified column name does not exist in the table type, an error is raised. Use `Type.TableSchema` first if you need to check which columns are declared before accessing them.

`Type.TableColumn` is useful for:

- **Generic transform functions** that need to inspect or compare column types before deciding how to process data
- **Schema validation** — verifying that a column is typed as expected before applying a transformation
- **Nullability checks** — combining with `Type.IsNullable` to determine whether a column accepts null values

Note that most tables loaded from external sources have column types inferred or declared through the connector, and those types are often `nullable` variants of the base primitive types.

## Examples

### Example 1: Get the declared type of a column

```powerquery
let
    T = #table(type table [Name = text, Age = number], {{"Alice", 30}}),
    TType = Value.Type(T),
    ColType = Type.TableColumn(TType, "Age")
in
    ColType
```

<!--output
{"columns":[{"name":"Result","type":"type"}],"rows":[{"Result":"type number"}]}
-->

<!--steps
{
  "steps": [
    {
      "name": "T",
      "description": "Creates a one-row table with Name (text) and Age (number) columns using #table.",
      "output": {"columns":[{"name":"Name","type":"text"},{"name":"Age","type":"number"}],"rows":[{"Name":"Alice","Age":30}]}
    },
    {
      "name": "TType",
      "description": "Retrieves the table's type value using Value.Type.",
      "output": {"columns":[{"name":"Value","type":"type"}],"rows":[{"Value":"type table [Name = text, Age = number]"}]}
    },
    {
      "name": "ColType",
      "description": "The final output — extracts the declared type of the Age column from the table type using Type.TableColumn, returning type number.",
      "output": {"columns":[{"name":"Result","type":"type"}],"rows":[{"Result":"type number"}]}
    }
  ]
}
-->

### Example 2: Check if a column type is nullable

```powerquery
let
    T = #table(type table [Score = nullable number], {{null}}),
    ColType = Type.TableColumn(Value.Type(T), "Score")
in
    Type.IsNullable(ColType)
```

<!--output
{"columns":[{"name":"Result","type":"logical"}],"rows":[{"Result":true}]}
-->

<!--steps
{
  "steps": [
    {
      "name": "T",
      "description": "Creates a one-row table with a Score column typed as nullable number, containing a null value.",
      "output": {"columns":[{"name":"Score","type":"number"}],"rows":[{"Score":null}]}
    },
    {
      "name": "ColType",
      "description": "The final output — extracts the declared type of the Score column using Type.TableColumn, then checks Type.IsNullable to confirm it is nullable, returning true.",
      "output": {"columns":[{"name":"Result","type":"logical"}],"rows":[{"Result":true}]}
    }
  ]
}
-->

### Example 3: Compare the column type to a known type

```powerquery
let
    T = #table(type table [OrderID = number, Product = text], {{1, "Widget A"}}),
    ColType = Type.TableColumn(Value.Type(T), "Product")
in
    ColType = type text
```

<!--output
{"columns":[{"name":"Result","type":"logical"}],"rows":[{"Result":true}]}
-->

<!--steps
{
  "steps": [
    {
      "name": "T",
      "description": "Creates a one-row table with OrderID (number) and Product (text) columns using #table.",
      "output": {"columns":[{"name":"OrderID","type":"number"},{"name":"Product","type":"text"}],"rows":[{"OrderID":1,"Product":"Widget A"}]}
    },
    {
      "name": "ColType",
      "description": "The final output — extracts the declared type of the Product column using Type.TableColumn, then compares it to type text to confirm they are equal, returning true.",
      "output": {"columns":[{"name":"Result","type":"logical"}],"rows":[{"Result":true}]}
    }
  ]
}
-->

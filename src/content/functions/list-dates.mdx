---
title: "List.Dates"
slug: "list-dates"
category: "List"
description: "Generates a list of date values starting from a given date, with a specified count and step duration."
syntax: "List.Dates(start as date, count as number, step as duration) as list"
returnType: "list"
returnDescription: "A list of date values of length count, beginning at start and incrementing by step."
parameters:
  - name: "start"
    type: "date"
    required: true
    description: "The starting date value."
  - name: "count"
    type: "number"
    required: true
    description: "The number of date values to generate."
  - name: "step"
    type: "duration"
    required: true
    description: "The duration to add between each successive date. Use #duration(days, hours, minutes, seconds)."
compatibility:
  pbiDesktop: true
  pbiService: true
  excelDesktop: true
  excelOnline: true
  dataflows: true
  fabricNotebooks: true
relatedFunctions:
  - "list-datetimes"
  - "list-datetimezones"
  - "list-times"
  - "list-numbers"
  - "list-durations"
---

## Remarks

`List.Dates` is the standard way to generate a continuous or stepped sequence of date values in Power Query M. It is most commonly used to build date dimension tables, fill in missing dates in time-series data, or create calendar scaffolding for reports.

The `step` parameter is a `duration` value created with the `#duration(days, hours, minutes, seconds)` literal. For day-by-day sequences use `#duration(1, 0, 0, 0)`, for weekly use `#duration(7, 0, 0, 0)`. Avoid using `List.Dates` for monthly sequences — because calendar months have variable lengths, use `List.Generate` with `Date.AddMonths` instead to produce accurate monthly increments.

A `count` of 0 returns an empty list; negative counts cause an error. To generate a full calendar year, calculate the count with `Duration.Days(#date(year+1,1,1) - #date(year,1,1))` to handle leap years automatically.

## Examples

### Example 1: Generate a week of daily dates

```powerquery
List.Dates(#date(2024, 1, 1), 7, #duration(1, 0, 0, 0))
```

<!--output
{"columns":[{"name":"Result","type":"date"}],"rows":[{"Result":"2024-01-01"},{"Result":"2024-01-02"},{"Result":"2024-01-03"},{"Result":"2024-01-04"},{"Result":"2024-01-05"},{"Result":"2024-01-06"},{"Result":"2024-01-07"}]}
-->

### Example 2: Generate weekly dates for a quarter

```powerquery
List.Dates(#date(2024, 1, 1), 13, #duration(7, 0, 0, 0))
```

<!--output
{"columns":[{"name":"Result","type":"date"}],"rows":[{"Result":"2024-01-01"},{"Result":"2024-01-08"},{"Result":"2024-01-15"},{"Result":"2024-01-22"},{"Result":"2024-01-29"},{"Result":"2024-02-05"},{"Result":"2024-02-12"},{"Result":"2024-02-19"},{"Result":"2024-02-26"},{"Result":"2024-03-04"},{"Result":"2024-03-11"},{"Result":"2024-03-18"},{"Result":"2024-03-25"}]}
-->

### Example 3: Build a date dimension table

Generate a full year of dates as a table.

```powerquery
let
    DateList = List.Dates(#date(2024, 1, 1), 366, #duration(1, 0, 0, 0)),
    DateTable = Table.FromList(DateList, Splitter.SplitByNothing(), {"Date"})
in
    DateTable
```

<!--output
{"columns":[{"name":"Date","type":"date"}],"rows":[{"Date":"2024-01-01"},{"Date":"2024-01-02"},{"Date":"..."}]}
-->

<!--steps
{
  "steps": [
    {
      "name": "DateList",
      "description": "Generates a list of 366 consecutive daily dates starting from 2024-01-01, covering the full leap year.",
      "output": {"columns":[{"name":"Value","type":"date"}],"rows":[{"Value":"2024-01-01"},{"Value":"2024-01-02"},{"Value":"..."},{"Value":"2024-12-31"}]}
    },
    {
      "name": "DateTable",
      "description": "The final output — converts the date list into a single-column table with each date in its own row.",
      "output": {"columns":[{"name":"Date","type":"date"}],"rows":[{"Date":"2024-01-01"},{"Date":"2024-01-02"},{"Date":"..."}]}
    }
  ]
}
-->

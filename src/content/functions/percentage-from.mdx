---
title: "Percentage.From"
slug: "percentage-from"
category: "Number"
description: "Converts a value to a percentage number type."
syntax: "Percentage.From(value as any, optional culture as nullable text) as nullable number"
returnType: "number"
returnDescription: "A number typed as a percentage."
parameters:
  - name: "value"
    type: "any"
    required: true
    description: "The value to convert. Accepts number, text, logical, or null."
  - name: "culture"
    type: "text"
    required: false
    description: "An optional culture string for locale-aware parsing of text values."
compatibility:
  pbiDesktop: true
  pbiService: true
  excelDesktop: true
  excelOnline: true
  dataflows: true
  fabricNotebooks: true
relatedFunctions:
  - "decimal-from"
  - "number-from"
  - "number-totext"
---

## Remarks

`Percentage.From` converts a value to the percentage data type. The underlying numeric value is stored as a standard decimal — `0.25` represents 25%, `1.0` represents 100%. The percentage type is primarily a **display annotation**: it signals to the host application (such as Power BI Desktop) that values should be formatted with a `%` suffix and scaled appropriately in visuals and data labels. The raw stored value does not change.

This distinction is important: if you have a column where `25` should represent 25%, you must first divide by 100 and then apply `Percentage.From`. If you have a column already storing `0.25` for 25%, you can apply `Percentage.From` directly. The function does not perform any scaling — it only changes the type annotation.

When converting from text, behavior depends on the environment: some locales recognize `"25%"` as `0.25`, while others treat it as `25`. To avoid ambiguity, normalize text percentages to decimal form before converting. Logical `true` converts to `1` (100%), `false` to `0`, and `null` returns `null`. To apply percentage type to an entire column, use `Table.TransformColumnTypes` with `Percentage.Type`.

## Examples

### Example 1: Annotate a ratio as percentage type

A ratio value of `0.25` is stored as-is; the percentage type annotation tells visuals to display it as "25%".

```powerquery
Percentage.From(0.25)
```

<!--output
{"columns":[{"name":"Result","type":"number"}],"rows":[{"Result":0.25}]}
-->

### Example 2: Parse a decimal text string as percentage

```powerquery
Percentage.From("0.75")
```

<!--output
{"columns":[{"name":"Result","type":"number"}],"rows":[{"Result":0.75}]}
-->

### Example 3: Compute market share and annotate as percentage

Input: `Sales`

Calculate each order's quantity as a fraction of total quantity, then annotate as percentage type.

```powerquery
let
    TotalQty = List.Sum(Sales[Quantity]),
    WithShare = Table.AddColumn(
        Table.SelectColumns(Table.FirstN(Sales, 4), {"OrderID", "CustomerName", "Quantity"}),
        "MarketShare",
        each Percentage.From([Quantity] / TotalQty),
        Percentage.Type
    )
in
    WithShare
```

<!--output
{"columns":[{"name":"OrderID","type":"number"},{"name":"CustomerName","type":"text"},{"name":"Quantity","type":"number"},{"name":"MarketShare","type":"number"}],"rows":[{"OrderID":1,"CustomerName":"Alice","Quantity":4,"MarketShare":0.1739},{"OrderID":2,"CustomerName":"Bob","Quantity":2,"MarketShare":0.0870},{"OrderID":3,"CustomerName":"Charlie","Quantity":10,"MarketShare":0.4348},{"OrderID":4,"CustomerName":"Alice","Quantity":1,"MarketShare":0.0435}]}
-->
<!--steps
{
  "steps": [
    {
      "name": "TotalQty",
      "description": "Sums all values in the Quantity column of the Sales table (4+2+10+1+6+1+3+8 = 35) to use as the denominator for share calculations.",
      "output": {"columns":[{"name":"Value","type":"number"}],"rows":[{"Value":35}]}
    },
    {
      "name": "WithShare",
      "description": "Adds a MarketShare column to the first 4 Sales rows, computing each order's quantity as a fraction of 35 and annotating it as Percentage.Type so visuals display it as a percentage.",
      "output": {"columns":[{"name":"OrderID","type":"number"},{"name":"CustomerName","type":"text"},{"name":"Quantity","type":"number"},{"name":"MarketShare","type":"number"}],"rows":[{"OrderID":1,"CustomerName":"Alice","Quantity":4,"MarketShare":0.1739},{"OrderID":2,"CustomerName":"Bob","Quantity":2,"MarketShare":0.0870},{"OrderID":3,"CustomerName":"Charlie","Quantity":10,"MarketShare":0.4348},{"OrderID":4,"CustomerName":"Alice","Quantity":1,"MarketShare":0.0435}]}
    },
    {
      "name": "Result",
      "description": "The final output — the four-column table showing each order's ID, customer, quantity, and percentage share of total quantity.",
      "output": {"columns":[{"name":"OrderID","type":"number"},{"name":"CustomerName","type":"text"},{"name":"Quantity","type":"number"},{"name":"MarketShare","type":"number"}],"rows":[{"OrderID":1,"CustomerName":"Alice","Quantity":4,"MarketShare":0.1739},{"OrderID":2,"CustomerName":"Bob","Quantity":2,"MarketShare":0.0870},{"OrderID":3,"CustomerName":"Charlie","Quantity":10,"MarketShare":0.4348},{"OrderID":4,"CustomerName":"Alice","Quantity":1,"MarketShare":0.0435}]}
    }
  ]
}
-->

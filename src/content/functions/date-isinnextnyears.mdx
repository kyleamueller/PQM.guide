---
title: "Date.IsInNextNYears"
slug: "date-isinnextnyears"
category: "Date"
description: "Returns true if the date falls within the next N calendar years."
syntax: "Date.IsInNextNYears(dateTime as any, years as number) as logical"
returnType: "logical"
returnDescription: "true if the date falls within the next N years after the current year, false otherwise."
parameters:
  - name: "dateTime"
    type: "any"
    required: true
    description: "A date, datetime, or datetimezone value to test."
  - name: "years"
    type: "number"
    required: true
    description: "The number of years ahead to include. Must be a positive integer."
compatibility:
  pbiDesktop: true
  pbiService: true
  excelDesktop: true
  excelOnline: true
  dataflows: true
  fabricNotebooks: true
relatedFunctions:
  - "date-isinnextyear"
  - "date-isinpreviousnyears"
  - "date-isinnextnquarters"
  - "date-isinnextnmonths"
---

## Remarks

`Date.IsInNextNYears` returns `true` if the input date falls within the next N complete calendar years after the current year. The current year itself is not included; use `Date.IsInCurrentYear` to also match the current year. The function is re-evaluated on each query refresh.

For example, if the current year is 2026 and `years = 3`, it matches any date in 2027, 2028, or 2029. This function counts whole calendar years (January 1 through December 31), not a rolling 365-day window. For a rolling window, use `Date.IsInNextNDays(n * 365)` or compute the boundary dates explicitly.

Use this function for medium-term planning scenarios such as multi-year contract expiration reporting, asset replacement schedules, or budget forecasting horizons.

## Examples

### Example 1: Find orders due in the next 2 years

Keep only rows where OrderDate falls within the next 2 complete calendar years.

Input: `Sales`

```powerquery
Table.SelectRows(
    Sales,
    each Date.IsInNextNYears([OrderDate], 2)
)
```

<!--output
{"columns":[{"name":"OrderID","type":"number"},{"name":"CustomerName","type":"text"},{"name":"OrderDate","type":"date"}],"rows":[{"OrderID":50,"CustomerName":"Alice","OrderDate":"2027-03-15"},{"OrderID":51,"CustomerName":"Bob","OrderDate":"2027-09-22"}]}
-->

### Example 2: Flag long-term orders

Add a boolean column for orders due within the next 5 years.

Input: `Sales`

```powerquery
Table.AddColumn(
    Sales,
    "DueInNext5Years", each Date.IsInNextNYears([OrderDate], 5), type logical
)
```

<!--output
{"columns":[{"name":"OrderID","type":"number"},{"name":"OrderDate","type":"date"},{"name":"DueInNext5Years","type":"logical"}],"rows":[{"OrderID":42,"OrderDate":"2026-03-08","DueInNext5Years":false},{"OrderID":50,"OrderDate":"2027-03-15","DueInNext5Years":true},{"OrderID":55,"OrderDate":"2032-01-01","DueInNext5Years":false}]}
-->

### Example 3: Combine current year and next 2 years for a 3-year view

Flag rows from the current year or either of the next 2 years.

Input: `Sales`

```powerquery
Table.SelectRows(
    Sales,
    each Date.IsInCurrentYear([OrderDate]) or Date.IsInNextNYears([OrderDate], 2)
)
```

<!--output
{"columns":[{"name":"OrderID","type":"number"},{"name":"CustomerName","type":"text"},{"name":"OrderDate","type":"date"}],"rows":[{"OrderID":42,"CustomerName":"Charlie","OrderDate":"2026-03-08"},{"OrderID":50,"CustomerName":"Alice","OrderDate":"2027-03-15"},{"OrderID":51,"CustomerName":"Bob","OrderDate":"2027-09-22"}]}
-->

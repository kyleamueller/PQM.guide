---
title: "List.Reverse"
slug: "list-reverse"
category: "List"
description: "Returns a new list with the elements in reverse order."
syntax: "List.Reverse(list as list) as list"
returnType: "list"
returnDescription: "A new list with elements in reverse order."
parameters:
  - name: "list"
    type: "list"
    required: true
    description: "The list to reverse."
compatibility:
  pbiDesktop: true
  pbiService: true
  excelDesktop: true
  excelOnline: true
  dataflows: true
  fabricNotebooks: true
relatedFunctions:
  - "list-sort"
  - "list-transform"
  - "list-firstn"
  - "list-lastn"
---

## Remarks

`List.Reverse` returns a new list with the elements in reversed order. The original list is not modified. The function has no parameters beyond the list itself and always reverses the entire list.

A common use case is reversing the result of a sort: `List.Sort` produces ascending order by default, and `List.Reverse` on the result gives descending order. However, for descending sorts it is more efficient and readable to pass `Order.Descending` directly to `List.Sort`. Use `List.Reverse` when you need to reverse for other reasons — for example, processing a timeline from newest to oldest, or displaying a ranked list in reverse order.

`List.Reverse` can also be used as a building block in more complex transformations: for example, to read a list from the end without knowing its length, or to mirror a sequence.

## Examples

### Example 1: Reverse a list of numbers

```powerquery
List.Reverse({1, 2, 3, 4, 5})
```

<!--output
{"columns":[{"name":"Result","type":"any"}],"rows":[{"Result":"{5, 4, 3, 2, 1}"}]}
-->

### Example 2: Reverse a sorted list to get descending order

```powerquery
let
    Values = {3, 1, 4, 1, 5, 9, 2, 6},
    Sorted = List.Sort(Values),
    Descending = List.Reverse(Sorted)
in
    Descending
```

<!--output
{"columns":[{"name":"Result","type":"any"}],"rows":[{"Result":"{9, 6, 5, 4, 3, 2, 1, 1}"}]}
-->

<!--steps
{
  "steps": [
    {
      "name": "Values",
      "description": "Defines an unsorted list of 8 integers, including a duplicate 1.",
      "output": {"columns":[{"name":"Value","type":"number"}],"rows":[{"Value":3},{"Value":1},{"Value":4},{"Value":1},{"Value":5},{"Value":9},{"Value":2},{"Value":6}]}
    },
    {
      "name": "Sorted",
      "description": "Sorts Values in ascending order using the default sort, producing {1, 1, 2, 3, 4, 5, 6, 9}.",
      "output": {"columns":[{"name":"Value","type":"number"}],"rows":[{"Value":1},{"Value":1},{"Value":2},{"Value":3},{"Value":4},{"Value":5},{"Value":6},{"Value":9}]}
    },
    {
      "name": "Descending",
      "description": "Reverses the Sorted list to produce a descending-order result.",
      "output": {"columns":[{"name":"Result","type":"any"}],"rows":[{"Result":"{9, 6, 5, 4, 3, 2, 1, 1}"}]}
    },
    {
      "name": "Result",
      "description": "The final output — the list of integers sorted in descending order.",
      "output": {"columns":[{"name":"Result","type":"any"}],"rows":[{"Result":"{9, 6, 5, 4, 3, 2, 1, 1}"}]}
    }
  ]
}
-->

### Example 3: Reverse a list of text items

```powerquery
List.Reverse({"First", "Second", "Third", "Fourth"})
```

<!--output
{"columns":[{"name":"Result","type":"any"}],"rows":[{"Result":"{Fourth, Third, Second, First}"}]}
-->

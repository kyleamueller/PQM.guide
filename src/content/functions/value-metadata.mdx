---
title: "Value.Metadata"
slug: "value-metadata"
category: "Value"
description: "Returns the metadata record associated with a value."
syntax: "Value.Metadata(value as any) as record"
returnType: "record"
returnDescription: "The metadata record attached to the value, or an empty record if none exists."
parameters:
  - name: "value"
    type: "any"
    required: true
    description: "The value whose metadata record is returned."
compatibility:
  pbiDesktop: true
  pbiService: true
  excelDesktop: true
  excelOnline: true
  dataflows: true
  fabricNotebooks: true
relatedFunctions:
  - "value-replacemetadata"
  - "value-removemetadata"
  - "value-type"
---

## Remarks

In Power Query M, every value can carry an associated **metadata record** — an auxiliary record of key-value pairs attached to the value without affecting its underlying data or equality. Metadata is attached using the `meta` keyword: `value meta [Key = "value"]`.

`Value.Metadata` retrieves this metadata record. If the value has no metadata, an empty record (`[]`) is returned — never `null`.

Key behaviors of metadata in M:

- **Transparent to equality**: `1 meta [Tag = "x"]` is equal to `1`. Metadata does not affect the `=` operator, `Value.Equals`, or type checks.
- **Lost on operations**: most M operations discard metadata from their inputs. For example, `(1 meta [Tag = "x"]) + 1` returns `2` with no metadata. Metadata must be explicitly re-attached if needed after transformations.
- **Used by the engine**: Power Query uses metadata internally to carry display format strings, column descriptions, and data source information. You can read this engine metadata with `Value.Metadata`.

The companion functions are `Value.ReplaceMetadata` (to set or replace metadata) and `Value.RemoveMetadata` (to strip it). The `meta` keyword expression `v meta r` is equivalent to `Value.ReplaceMetadata(v, r)`.

## Examples

### Example 1: Retrieve metadata attached with the meta keyword

```powerquery
Value.Metadata(1 meta [Source = "Sales", Verified = true])
```

<!--output
{"columns":[{"name":"Name","type":"text"},{"name":"Value","type":"any"}],"rows":[{"Name":"Source","Value":"Sales"},{"Name":"Verified","Value":true}]}
-->

### Example 2: A value without metadata returns an empty record

```powerquery
Value.Metadata(42)
```

<!--output
{"columns":[{"name":"Name","type":"text"},{"name":"Value","type":"any"}],"rows":[]}
-->

### Example 3: Access a specific field from the metadata record

```powerquery
let
    Annotated = "hello" meta [Language = "en", Source = "Config"],
    Meta = Value.Metadata(Annotated)
in
    Meta[Language]
```

<!--output
{"columns":[{"name":"Result","type":"text"}],"rows":[{"Result":"en"}]}
-->

<!--steps
{
  "steps": [
    {
      "name": "Annotated",
      "description": "Creates the text value \"hello\" with a metadata record attached containing Language and Source fields.",
      "output": {"columns":[{"name":"Value","type":"text"}],"rows":[{"Value":"hello"}]}
    },
    {
      "name": "Meta",
      "description": "Retrieves the metadata record from the annotated value, returning a record with Language = \"en\" and Source = \"Config\".",
      "output": {"columns":[{"name":"Field","type":"text"},{"name":"Value","type":"text"}],"rows":[{"Field":"Language","Value":"en"},{"Field":"Source","Value":"Config"}]}
    },
    {
      "name": "Result",
      "description": "The final output — accesses the Language field from the metadata record, returning \"en\".",
      "output": {"columns":[{"name":"Result","type":"text"}],"rows":[{"Result":"en"}]}
    }
  ]
}
-->

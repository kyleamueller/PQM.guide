---
title: "Record.ToTable"
slug: "record-totable"
category: "Record"
description: "Converts a record into a two-column table with columns Name and Value."
syntax: "Record.ToTable(record as record) as table"
returnType: "table"
returnDescription: "A table with two columns — Name (text) and Value (any) — one row per field."
parameters:
  - name: "record"
    type: "record"
    required: true
    description: "The record to convert to a table."
compatibility:
  pbiDesktop: true
  pbiService: true
  excelDesktop: true
  excelOnline: true
  dataflows: true
  fabricNotebooks: true
relatedFunctions:
  - "record-fromtable"
  - "record-fieldnames"
  - "record-fieldvalues"
  - "record-tolist"
---

## Remarks

`Record.ToTable` converts a record into a two-column table with columns `Name` (text) and `Value` (any). Each field in the record becomes one row. Row order matches the field order in the record.

This is the inverse of `Record.FromTable`. Common uses include:

- **Inspection**: displaying all fields of a record in a structured table view
- **Finding nulls**: filtering the resulting table for rows where `[Value] = null`
- **Dynamic field iteration**: applying `Table.SelectRows`, `Table.AddColumn`, or other table operations across all fields without knowing them by name

The `Value` column has type `any` because records can contain fields of mixed types. After converting to a table, you can filter, sort, or transform by field name using standard table functions.

## Examples

### Example 1: Convert a record to a two-column Name/Value table

```powerquery
Record.ToTable([Name = "Alice", Age = 30, City = "New York"])
```

<!--output
{"columns":[{"name":"Name","type":"text"},{"name":"Value","type":"any"}],"rows":[{"Name":"Name","Value":"Alice"},{"Name":"Age","Value":30},{"Name":"City","Value":"New York"}]}
-->

### Example 2: Inspect all fields of the first Sales row

Input: `Sales`

```powerquery
Record.ToTable(Sales{0})
```

<!--output
{"columns":[{"name":"Name","type":"text"},{"name":"Value","type":"any"}],"rows":[{"Name":"OrderID","Value":1},{"Name":"CustomerName","Value":"Alice"},{"Name":"Product","Value":"Widget A"},{"Name":"Category","Value":"Widgets"},{"Name":"UnitPrice","Value":25},{"Name":"Quantity","Value":4},{"Name":"OrderDate","Value":"2024-01-15"},{"Name":"Region","Value":"East"}]}
-->

### Example 3: Find which fields in a record contain null values

```powerquery
let
    Row = [OrderID = 1, CustomerName = null, Product = "Widget A", Notes = null],
    AsTable = Record.ToTable(Row),
    NullFields = Table.SelectRows(AsTable, each [Value] = null)
in
    NullFields
```

<!--output
{"columns":[{"name":"Name","type":"text"},{"name":"Value","type":"any"}],"rows":[{"Name":"CustomerName","Value":null},{"Name":"Notes","Value":null}]}
-->

<!--steps
{
  "steps": [
    {
      "name": "Row",
      "description": "Defines a literal record with four fields, two of which (CustomerName and Notes) are null.",
      "output": {"columns":[{"name":"Field","type":"text"},{"name":"Value","type":"any"}],"rows":[{"Field":"OrderID","Value":1},{"Field":"CustomerName","Value":null},{"Field":"Product","Value":"Widget A"},{"Field":"Notes","Value":null}]}
    },
    {
      "name": "AsTable",
      "description": "Converts the record to a two-column Name/Value table using Record.ToTable, making all fields inspectable as rows.",
      "output": {"columns":[{"name":"Name","type":"text"},{"name":"Value","type":"any"}],"rows":[{"Name":"OrderID","Value":1},{"Name":"CustomerName","Value":null},{"Name":"Product","Value":"Widget A"},{"Name":"Notes","Value":null}]}
    },
    {
      "name": "NullFields",
      "description": "Filters the table to keep only rows where the Value column is null, identifying fields that have no data.",
      "output": {"columns":[{"name":"Name","type":"text"},{"name":"Value","type":"any"}],"rows":[{"Name":"CustomerName","Value":null},{"Name":"Notes","Value":null}]}
    },
    {
      "name": "Result",
      "description": "The final output — a table listing only the fields from the record that contain null values.",
      "output": {"columns":[{"name":"Name","type":"text"},{"name":"Value","type":"any"}],"rows":[{"Name":"CustomerName","Value":null},{"Name":"Notes","Value":null}]}
    }
  ]
}
-->

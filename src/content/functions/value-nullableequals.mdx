---
title: "Value.NullableEquals"
slug: "value-nullableequals"
category: "Value"
description: "Compares two values for equality, returning null if either value is null (three-valued logic)."
syntax: "Value.NullableEquals(value1 as any, value2 as any, optional comparer as nullable function) as nullable logical"
returnType: "logical"
returnDescription: "True if equal, false if not equal, null if either value is null."
parameters:
  - name: "value1"
    type: "any"
    required: true
    description: "The first value to compare."
  - name: "value2"
    type: "any"
    required: true
    description: "The second value to compare."
  - name: "comparer"
    type: "function"
    required: false
    description: "An optional comparer function for custom equality semantics."
compatibility:
  pbiDesktop: true
  pbiService: true
  excelDesktop: true
  excelOnline: true
  dataflows: true
  fabricNotebooks: true
relatedFunctions:
  - "value-equals"
  - "value-compare"
  - "value-is"
---

## Remarks

`Value.NullableEquals` implements **three-valued equality** (also called SQL-style equality): when either operand is `null`, the result is `null` — not `true` or `false`. This matches the behavior of the M `=` operator when applied to nullable values and mirrors SQL's `IS DISTINCT FROM` / `=` null semantics.

The three possible results are:

| value1 | value2 | Result |
|--------|--------|--------|
| non-null | non-null (equal) | `true` |
| non-null | non-null (different) | `false` |
| null | any | `null` |
| any | null | `null` |

This contrasts with `Value.Equals(null, null)` which always returns `true` — `Value.Equals` never returns null. Choose between the two based on whether you need null-propagating semantics:

- Use `Value.NullableEquals` when you want comparisons involving null to return null (propagate unknown), as in SQL `WHERE` clauses
- Use `Value.Equals` when you want `null = null` to be definitively `true`, as in deduplication or record matching

The optional `comparer` parameter allows custom equality logic (e.g., `Comparer.OrdinalIgnoreCase`) just like `Value.Equals`.

## Examples

### Example 1: Both non-null and equal — returns true

```powerquery
Value.NullableEquals(1, 1)
```

<!--output
{"columns":[{"name":"Result","type":"logical"}],"rows":[{"Result":true}]}
-->

### Example 2: One operand is null — returns null

```powerquery
Value.NullableEquals(1, null)
```

<!--output
{"columns":[{"name":"Result","type":"logical"}],"rows":[{"Result":null}]}
-->

### Example 3: Both null — returns null (unlike Value.Equals)

```powerquery
Value.NullableEquals(null, null)
```

<!--output
{"columns":[{"name":"Result","type":"logical"}],"rows":[{"Result":null}]}
-->

### Example 4: Both non-null and unequal — returns false

```powerquery
Value.NullableEquals("hello", "world")
```

<!--output
{"columns":[{"name":"Result","type":"logical"}],"rows":[{"Result":false}]}
-->

---
title: "List.AnyTrue"
slug: "list-anytrue"
category: "List"
description: "Returns true if any value in the list evaluates to true."
syntax: "List.AnyTrue(list as list) as logical"
returnType: "logical"
returnDescription: "True if at least one value in the list is true; false otherwise."
parameters:
  - name: "list"
    type: "list"
    required: true
    description: "The list of logical values to evaluate."
compatibility:
  pbiDesktop: true
  pbiService: true
  excelDesktop: true
  excelOnline: true
  dataflows: true
  fabricNotebooks: true
relatedFunctions:
  - "list-alltrue"
  - "list-matchesany"
  - "list-matchesall"
  - "list-select"
---

## Remarks

`List.AnyTrue` returns `true` if at least one item in the list is logically true. It short-circuits and stops evaluating as soon as the first `true` value is found. An empty list returns `false` — there are no items that could satisfy the condition.

Null values are treated as `false`, so a list of only nulls returns `false`. Non-logical values (such as numbers or text) will cause a type error; use `List.Transform` to evaluate a condition on each item and produce a list of logical values first.

The typical pattern is `List.AnyTrue(List.Transform(someList, each <condition>))`. As a more concise alternative, `List.MatchesAny` accepts a list and a predicate function in one call. For simple membership checks, `List.Contains` is more idiomatic.

## Examples

### Example 1: At least one value is true

```powerquery
List.AnyTrue({false, false, true})
```

<!--output
{"columns":[{"name":"Result","type":"logical"}],"rows":[{"Result":"true"}]}
-->

### Example 2: All values are false

```powerquery
List.AnyTrue({false, false, false})
```

<!--output
{"columns":[{"name":"Result","type":"logical"}],"rows":[{"Result":"false"}]}
-->

### Example 3: Check if any order in the Sales table has a total over $200

Compute the order total per row, then check if any exceed the threshold.

```powerquery
let
    Sales = #table(
        {"CustomerName", "UnitPrice", "Quantity"},
        {{"Alice", 25.00, 4}, {"Bob", 50.00, 2}, {"Charlie", 15.00, 10}, {"Diana", 25.00, 6}}
    ),
    Totals = List.Transform(
        Table.ToRows(Sales),
        each _{1} * _{2}
    ),
    AnyLarge = List.AnyTrue(List.Transform(Totals, each _ > 200))
in
    AnyLarge
```

<!--output
{"columns":[{"name":"Result","type":"logical"}],"rows":[{"Result":"false"}]}
-->

<!--steps
{
  "steps": [
    {
      "name": "Sales",
      "description": "Defines an inline table with CustomerName, UnitPrice, and Quantity columns for four orders.",
      "output": {"columns":[{"name":"CustomerName","type":"text"},{"name":"UnitPrice","type":"number"},{"name":"Quantity","type":"number"}],"rows":[{"CustomerName":"Alice","UnitPrice":25,"Quantity":4},{"CustomerName":"Bob","UnitPrice":50,"Quantity":2},{"CustomerName":"Charlie","UnitPrice":15,"Quantity":10},{"CustomerName":"Diana","UnitPrice":25,"Quantity":6}]}
    },
    {
      "name": "Totals",
      "description": "Converts each row to a list and multiplies UnitPrice by Quantity, producing a list of order totals.",
      "output": {"columns":[{"name":"Value","type":"number"}],"rows":[{"Value":100},{"Value":100},{"Value":150},{"Value":150}]}
    },
    {
      "name": "AnyLarge",
      "description": "The final output — checks whether any order total exceeds 200; all totals are 150 or less, so returns false.",
      "output": {"columns":[{"name":"Result","type":"logical"}],"rows":[{"Result":"false"}]}
    }
  ]
}
-->

---
title: "Record.AddField"
slug: "record-addfield"
category: "Record"
description: "Returns a new record with an additional field added. Raises an error if the field already exists."
syntax: "Record.AddField(record as record, fieldName as text, value as any, optional delayed as nullable logical) as record"
returnType: "record"
returnDescription: "A new record with the specified field added."
parameters:
  - name: "record"
    type: "record"
    required: true
    description: "The source record."
  - name: "fieldName"
    type: "text"
    required: true
    description: "The name of the field to add."
  - name: "value"
    type: "any"
    required: true
    description: "The value to assign to the new field."
  - name: "delayed"
    type: "logical"
    required: false
    description: "When true, the value expression is evaluated lazily. Defaults to false."
compatibility:
  pbiDesktop: true
  pbiService: true
  excelDesktop: true
  excelOnline: true
  dataflows: true
  fabricNotebooks: true
relatedFunctions:
  - "record-removefields"
  - "record-renamefields"
  - "record-combine"
  - "record-fieldnames"
---

## Remarks

`Record.AddField` returns a new record with one additional field appended. Records in M are **immutable** — the original record is never modified; a new record is always returned. If the field name already exists in the record, an error is raised. To replace an existing field, use `Record.RemoveFields` to drop it first, then `Record.AddField`.

For simple cases where you want to merge two records, the record merge operator `&` is often more concise: `[A = 1] & [B = 2]` produces `[A = 1, B = 2]`. Use `Record.AddField` when the field name or value is computed dynamically.

The optional `delayed` parameter (`true`/`false`) controls whether the value expression is evaluated eagerly (default, `false`) or lazily (only when the field is first accessed, `true`). Lazy evaluation is useful for expensive computations that may not always be needed.

## Examples

### Example 1: Add a new text field to a record

```powerquery
Record.AddField([Name = "Alice", Age = 30], "City", "New York")
```

<!--output
{"columns":[{"name":"Name","type":"text"},{"name":"Age","type":"number"},{"name":"City","type":"text"}],"rows":[{"Name":"Alice","Age":30,"City":"New York"}]}
-->

### Example 2: Add a computed field from existing field values

```powerquery
let
    R = [UnitPrice = 25, Quantity = 4]
in
    Record.AddField(R, "Total", R[UnitPrice] * R[Quantity])
```

<!--output
{"columns":[{"name":"UnitPrice","type":"number"},{"name":"Quantity","type":"number"},{"name":"Total","type":"number"}],"rows":[{"UnitPrice":25,"Quantity":4,"Total":100}]}
-->

### Example 3: Add a "Status" field to the first row of the Sales table

Input: `Sales`

```powerquery
let
    Row = Sales{0},
    WithStatus = Record.AddField(Row, "Status", "Processed")
in
    Record.SelectFields(WithStatus, {"OrderID", "CustomerName", "Product", "Status"})
```

<!--output
{"columns":[{"name":"OrderID","type":"number"},{"name":"CustomerName","type":"text"},{"name":"Product","type":"text"},{"name":"Status","type":"text"}],"rows":[{"OrderID":1,"CustomerName":"Alice","Product":"Widget A","Status":"Processed"}]}
-->

<!--steps
{
  "steps": [
    {
      "name": "Row",
      "description": "Retrieves the first row of the Sales table as a record.",
      "output": {"columns":[{"name":"Field","type":"text"},{"name":"Value","type":"any"}],"rows":[{"Field":"OrderID","Value":1},{"Field":"CustomerName","Value":"Alice"},{"Field":"Product","Value":"Widget A"},{"Field":"Category","Value":"Widgets"},{"Field":"UnitPrice","Value":25},{"Field":"Quantity","Value":4},{"Field":"OrderDate","Value":"2024-01-15"},{"Field":"Region","Value":"East"}]}
    },
    {
      "name": "WithStatus",
      "description": "Adds a new \"Status\" field with the value \"Processed\" to the row record.",
      "output": {"columns":[{"name":"Field","type":"text"},{"name":"Value","type":"any"}],"rows":[{"Field":"OrderID","Value":1},{"Field":"CustomerName","Value":"Alice"},{"Field":"Product","Value":"Widget A"},{"Field":"Category","Value":"Widgets"},{"Field":"UnitPrice","Value":25},{"Field":"Quantity","Value":4},{"Field":"OrderDate","Value":"2024-01-15"},{"Field":"Region","Value":"East"},{"Field":"Status","Value":"Processed"}]}
    },
    {
      "name": "Result",
      "description": "The final output — selects only OrderID, CustomerName, Product, and Status from the enriched record.",
      "output": {"columns":[{"name":"OrderID","type":"number"},{"name":"CustomerName","type":"text"},{"name":"Product","type":"text"},{"name":"Status","type":"text"}],"rows":[{"OrderID":1,"CustomerName":"Alice","Product":"Widget A","Status":"Processed"}]}
    }
  ]
}
-->

---
title: "Date.MonthName"
slug: "date-monthname"
category: "Date"
description: "Returns the name of the month for the given date."
syntax: "Date.MonthName(dateTime as any, optional culture as nullable text) as nullable text"
returnType: "text"
returnDescription: "The localized name of the month (e.g., \"March\")."
parameters:
  - name: "dateTime"
    type: "any"
    required: true
    description: "A date, datetime, or datetimezone value."
  - name: "culture"
    type: "text"
    required: false
    description: "A culture code such as \"en-US\" or \"de-DE\" that controls the language of the returned month name."
compatibility:
  pbiDesktop: true
  pbiService: true
  excelDesktop: true
  excelOnline: true
  dataflows: true
  fabricNotebooks: true
relatedFunctions:
  - "date-month"
  - "date-dayofweekname"
  - "date-totext"
---

## Remarks

`Date.MonthName` returns the full, localized name of the month for a given `date`, `datetime`, or `datetimezone` value. When no `culture` is provided, the host environment's locale is used. Always specify `culture` explicitly (e.g., `"en-US"`) in production queries to ensure consistent output regardless of where the query runs.

For abbreviated month names (e.g., "Mar"), use `Date.ToText` with the format string `"MMM"` instead, which is more concise. If the input is `null`, the function returns `null`.

`Date.MonthName` returns only the display name — it does not return a sort key. If you need to sort by month, add a `Date.Month` column (numeric 1–12) alongside the month name column and sort by the number, then display the name. This is a very common pattern in Power BI date dimension tables.

## Examples

### Example 1: Add month name to a Sales date column

Add a column with the full English month name for each order date.

Input: `Sales`

```powerquery
Table.AddColumn(
    Table.SelectColumns(
        Table.FirstN(Sales, 5),
        {"OrderID", "OrderDate"}
    ),
    "MonthName", each Date.MonthName([OrderDate], "en-US"), type text
)
```

<!--output
{"columns":[{"name":"OrderID","type":"number"},{"name":"OrderDate","type":"date"},{"name":"MonthName","type":"text"}],"rows":[{"OrderID":1,"OrderDate":"2024-01-15","MonthName":"January"},{"OrderID":2,"OrderDate":"2024-01-18","MonthName":"January"},{"OrderID":3,"OrderDate":"2024-02-01","MonthName":"February"},{"OrderID":4,"OrderDate":"2024-02-10","MonthName":"February"},{"OrderID":5,"OrderDate":"2024-03-05","MonthName":"March"}]}
-->

### Example 2: Month name in German

Return the month name in German using the `culture` parameter.

```powerquery
#table(
    type table [Date = date, MonthDE = text],
    {{#date(2024, 3, 15), Date.MonthName(#date(2024, 3, 15), "de-DE")}}
)
```

<!--output
{"columns":[{"name":"Date","type":"date"},{"name":"MonthDE","type":"text"}],"rows":[{"Date":"2024-03-15","MonthDE":"März"}]}
-->

### Example 3: Add both month number and month name for sortable display

Add a numeric sort key alongside the display name to enable correct sorting in reports.

Input: `Sales`

```powerquery
Table.AddColumn(
    Table.AddColumn(
        Table.SelectColumns(Table.FirstN(Sales, 5), {"OrderID", "OrderDate"}),
        "MonthNum", each Date.Month([OrderDate]), Int64.Type
    ),
    "MonthName", each Date.MonthName([OrderDate], "en-US"), type text
)
```

<!--output
{"columns":[{"name":"OrderID","type":"number"},{"name":"OrderDate","type":"date"},{"name":"MonthNum","type":"number"},{"name":"MonthName","type":"text"}],"rows":[{"OrderID":1,"OrderDate":"2024-01-15","MonthNum":1,"MonthName":"January"},{"OrderID":2,"OrderDate":"2024-01-18","MonthNum":1,"MonthName":"January"},{"OrderID":3,"OrderDate":"2024-02-01","MonthNum":2,"MonthName":"February"},{"OrderID":4,"OrderDate":"2024-02-10","MonthNum":2,"MonthName":"February"},{"OrderID":5,"OrderDate":"2024-03-05","MonthNum":3,"MonthName":"March"}]}
-->

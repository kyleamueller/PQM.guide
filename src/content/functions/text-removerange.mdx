---
title: "Text.RemoveRange"
slug: "text-removerange"
category: "Text"
description: "Removes a specified number of characters from a text value starting at the given offset."
syntax: "Text.RemoveRange(text as nullable text, offset as number, optional count as nullable number) as nullable text"
returnType: "text"
returnDescription: "A new text value with the specified characters removed."
parameters:
  - name: "text"
    type: "text"
    required: true
    description: "The original text value."
  - name: "offset"
    type: "number"
    required: true
    description: "The zero-based position at which to begin removing characters."
  - name: "count"
    type: "number"
    required: false
    description: "The number of characters to remove. If omitted or null, all characters from offset to the end are removed."
compatibility:
  pbiDesktop: true
  pbiService: true
  excelDesktop: true
  excelOnline: true
  dataflows: true
  fabricNotebooks: true
relatedFunctions:
  - "text-insert"
  - "text-replacerange"
  - "text-remove"
  - "text-range"
---

## Remarks

`Text.RemoveRange` removes `count` characters from `text` beginning at the zero-based `offset` position. The characters before `offset` and after `offset + count` are concatenated to form the result. The original string is not modified — a new value is returned.

If `text` is `null`, the function returns `null`. If `count` is omitted or `null`, all characters from `offset` to the end of the string are removed. If `count` extends beyond the length of the string, all characters from `offset` onward are removed without error.

This is equivalent to: `Text.Start(text, offset) & Text.End(text, Text.Length(text) - offset - count)`

Use `Text.RemoveRange` for positional removal — when you know the exact character position and length of the content to remove. If you want to remove by content (all occurrences of a substring), use `Text.Replace(text, toRemove, "")`. If you want to replace removed characters with new content rather than nothing, use `Text.ReplaceRange`.

## Examples

### Example 1: Strip a known prefix from a column

Remove the `"E"` prefix from employee IDs to extract the numeric portion.

```powerquery
Text.RemoveRange("E001", 0, 1)
```

<!--output
{"columns":[{"name":"Result","type":"text"}],"rows":[{"Result":"001"}]}
-->

### Example 2: Remove a separator character at a known position

```powerquery
Text.RemoveRange("(555) 123-4567", 0, 1)
```

<!--output
{"columns":[{"name":"Result","type":"text"}],"rows":[{"Result":"555) 123-4567"}]}
-->

### Example 3: Remove everything from an offset to the end

Omitting the `count` parameter removes all characters from `offset` onward.

```powerquery
Text.RemoveRange("Sales Rep - East Region", 9)
```

<!--output
{"columns":[{"name":"Result","type":"text"}],"rows":[{"Result":"Sales Rep"}]}
-->

### Example 4: Remove an internal segment from a formatted string

```powerquery
Text.RemoveRange("Widget-East-001", 6, 5)
```

<!--output
{"columns":[{"name":"Result","type":"text"}],"rows":[{"Result":"Widget-001"}]}
-->

---
title: "Combiner.CombineTextByLengths"
slug: "combiner-combinetextbylengths"
category: "Combiner"
description: "Returns a function that combines a list of text values by padding or truncating each to a fixed length."
syntax: "Combiner.CombineTextByLengths(lengths as list, optional quoteStyle as nullable number) as function"
returnType: "function"
returnDescription: "A function that takes a list of text values and combines them into a fixed-width string."
parameters:
  - name: "lengths"
    type: "list"
    required: true
    description: "A list of integers specifying the fixed width for each text value. Values shorter than their length are padded with spaces; longer values are truncated."
  - name: "quoteStyle"
    type: "number"
    required: false
    description: "A QuoteStyle value specifying how to handle quoting. Defaults to QuoteStyle.None."
compatibility:
  pbiDesktop: true
  pbiService: true
  excelDesktop: true
  excelOnline: true
  dataflows: true
  fabricNotebooks: true
relatedFunctions:
  - "combiner-combinetextbydelimiter"
  - "combiner-combinetextbyeachdelimiter"
---

## Remarks

`Combiner.CombineTextByLengths` returns a **function** that produces fixed-width output. Each value in the input list is padded with trailing spaces or truncated to match the corresponding length. This is useful for generating fixed-width file formats or aligning columnar text output.

## Examples

### Example 1: Create fixed-width output from Sales data

Produce fixed-width strings with 10 characters for CustomerName and 12 characters for Product.

Input: `Sales`

```powerquery
let
    Subset = Table.SelectColumns(Table.FirstN(Sales, 3), {"CustomerName", "Product"}),
    CombinerFn = Combiner.CombineTextByLengths({10, 12}),
    Combined = Table.ToList(Subset, CombinerFn)
in
    #table({"FixedWidth"}, List.Transform(Combined, each {_}))
```

<!--output
{"columns":[{"name":"FixedWidth","type":"text"}],"rows":[{"FixedWidth":"Alice     Widget A    "},{"FixedWidth":"Bob       Gadget B    "},{"FixedWidth":"Charlie   Widget C    "}]}
-->
<!--steps
{
  "steps": [
    {
      "name": "Subset",
      "description": "Selects the CustomerName and Product columns from the first 3 rows of the Sales table using Table.SelectColumns and Table.FirstN.",
      "output": {"columns":[{"name":"CustomerName","type":"text"},{"name":"Product","type":"text"}],"rows":[{"CustomerName":"Alice","Product":"Widget A"},{"CustomerName":"Bob","Product":"Gadget B"},{"CustomerName":"Charlie","Product":"Widget C"}]}
    },
    {
      "name": "CombinerFn",
      "description": "Creates a combiner function using Combiner.CombineTextByLengths that pads or truncates the first value to 10 characters and the second to 12 characters.",
      "output": {"columns":[{"name":"Value","type":"function"}],"rows":[{"Value":"function"}]}
    },
    {
      "name": "Combined",
      "description": "Applies the fixed-width combiner to each row using Table.ToList, producing a list of 22-character fixed-width strings.",
      "output": {"columns":[{"name":"Value","type":"text"}],"rows":[{"Value":"Alice     Widget A    "},{"Value":"Bob       Gadget B    "},{"Value":"Charlie   Widget C    "}]}
    },
    {
      "name": "Result",
      "description": "The final output — a single-column table of fixed-width strings with CustomerName padded to 10 chars and Product padded to 12 chars.",
      "output": {"columns":[{"name":"FixedWidth","type":"text"}],"rows":[{"FixedWidth":"Alice     Widget A    "},{"FixedWidth":"Bob       Gadget B    "},{"FixedWidth":"Charlie   Widget C    "}]}
    }
  ]
}
-->

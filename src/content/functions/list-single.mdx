---
title: "List.Single"
slug: "list-single"
category: "List"
description: "Returns the single element of a list. Throws an error if the list has zero elements or more than one element."
syntax: "List.Single(list as list) as any"
returnType: "any"
returnDescription: "The single item contained in the list."
parameters:
  - name: "list"
    type: "list"
    required: true
    description: "The list expected to contain exactly one element."
compatibility:
  pbiDesktop: true
  pbiService: true
  excelDesktop: true
  excelOnline: true
  dataflows: true
  fabricNotebooks: true
relatedFunctions:
  - "list-singleordefault"
  - "list-first"
  - "list-last"
  - "list-count"
  - "list-isempty"
---

## Remarks

`List.Single` asserts that a list contains exactly one element and returns it as a scalar value. If the list is empty or contains more than one element, it throws an error. This makes it a strict validation tool — use it when having zero or multiple results indicates a data problem that should be surfaced immediately rather than silently ignored.

The typical use case is extracting a value from a lookup that is expected to match exactly one row. For example, after filtering a table by a unique key and extracting a column as a list, `List.Single` both validates the match count and returns the value in one step.

- An empty list throws: `Expression.Error: There were too few elements in the enumeration.`
- More than one element throws: `Expression.Error: There were too many elements in the enumeration.`
- Use `List.SingleOrDefault` if an empty list should return `null` (or a custom default) rather than an error.
- Use `List.First` if multiple results are acceptable and you only want the first.

## Examples

### Example 1: Single element list

```powerquery
List.Single({42})
```

<!--output
{"columns":[{"name":"Result","type":"number"}],"rows":[{"Result":42}]}
-->

### Example 2: Single text value

```powerquery
List.Single({"OnlyItem"})
```

<!--output
{"columns":[{"name":"Result","type":"text"}],"rows":[{"Result":"OnlyItem"}]}
-->

### Example 3: Lookup a single matching value

```powerquery
let
    Prices = #table({"Product", "Price"}, {{"Widget", 9.99}, {"Gadget", 24.99}}),
    WidgetPrice = List.Single(
        Table.SelectRows(Prices, each [Product] = "Widget")[Price]
    )
in
    WidgetPrice
```

<!--output
{"columns":[{"name":"Result","type":"number"}],"rows":[{"Result":9.99}]}
-->
<!--steps
{
  "steps": [
    {
      "name": "Prices",
      "description": "Creates an inline table with two columns (Product and Price) containing Widget and Gadget entries.",
      "output": {"columns":[{"name":"Product","type":"text"},{"name":"Price","type":"number"}],"rows":[{"Product":"Widget","Price":9.99},{"Product":"Gadget","Price":24.99}]}
    },
    {
      "name": "WidgetPrice",
      "description": "Filters the Prices table to rows where Product equals \"Widget\", extracts the Price column as a list, and uses List.Single to assert exactly one match and return its scalar value.",
      "output": {"columns":[{"name":"Value","type":"number"}],"rows":[{"Value":9.99}]}
    },
    {
      "name": "Result",
      "description": "The final output — the scalar price of the Widget product.",
      "output": {"columns":[{"name":"Result","type":"number"}],"rows":[{"Result":9.99}]}
    }
  ]
}
-->

---
title: "Type.TableKeys"
slug: "type-tablekeys"
category: "Type"
description: "Returns the list of key definitions for a table type."
syntax: "Type.TableKeys(type as type) as list"
returnType: "list"
returnDescription: "A list of key records, each with Columns (list of column names) and Primary (logical) fields."
parameters:
  - name: "type"
    type: "type"
    required: true
    description: "A table type whose key definitions are returned."
compatibility:
  pbiDesktop: true
  pbiService: true
  excelDesktop: true
  excelOnline: true
  dataflows: true
  fabricNotebooks: true
relatedFunctions:
  - "type-addtablekey"
  - "type-replacetablekeys"
  - "type-tableschema"
  - "type-tablecolumn"
---

## Remarks

`Type.TableKeys` returns the list of key definitions declared on a table type. Each element of the returned list is a record with two fields:

- `Columns` — a list of column name text values that form the key
- `Primary` — `true` for the primary key, `false` for an alternate key

If no keys are defined on the table type, an empty list (`{}`) is returned.

Keys in Power BI inform the engine about uniqueness constraints on tables and are used to enable and optimize relationships between tables in the data model. They can be added to a table type using `Type.AddTableKey`, set in bulk using `Type.ReplaceTableKeys`, or applied directly to a table using `Table.AddKey`.

`Type.TableKeys` is the read-side complement to `Type.AddTableKey` and `Type.ReplaceTableKeys`. It is useful for inspecting a table's declared schema, validating that expected key constraints are in place, or building generic connector utilities that need to be aware of key definitions.

## Examples

### Example 1: A table type with no declared keys returns an empty list

```powerquery
let
    T = #table(type table [ID = number, Name = text], {{1, "Alice"}}),
    Keys = Type.TableKeys(Value.Type(T))
in
    List.Count(Keys)
```

<!--output
{"columns":[{"name":"Result","type":"number"}],"rows":[{"Result":0}]}
-->

<!--steps
{
  "steps": [
    {
      "name": "T",
      "description": "Creates a typed table with ID and Name columns containing one row.",
      "output": {"columns":[{"name":"ID","type":"number"},{"name":"Name","type":"text"}],"rows":[{"ID":1,"Name":"Alice"}]}
    },
    {
      "name": "Keys",
      "description": "Retrieves the list of key definitions from the table's type — since no keys were declared, this returns an empty list.",
      "output": {"columns":[{"name":"Value","type":"any"}],"rows":[]}
    },
    {
      "name": "Result",
      "description": "The final output — counts the keys in the list, returning 0 because no keys are defined.",
      "output": {"columns":[{"name":"Result","type":"number"}],"rows":[{"Result":0}]}
    }
  ]
}
-->

### Example 2: Inspect key details after adding a primary key

```powerquery
let
    T = #table(type table [ID = number, Name = text], {{1, "Alice"}}),
    TWithKey = Table.AddKey(T, {"ID"}, true),
    Keys = Type.TableKeys(Value.Type(TWithKey))
in
    Keys{0}
```

<!--output
{"columns":[{"name":"Columns","type":"any"},{"name":"Primary","type":"logical"}],"rows":[{"Columns":["ID"],"Primary":true}]}
-->

<!--steps
{
  "steps": [
    {
      "name": "T",
      "description": "Creates a typed table with ID and Name columns containing one row.",
      "output": {"columns":[{"name":"ID","type":"number"},{"name":"Name","type":"text"}],"rows":[{"ID":1,"Name":"Alice"}]}
    },
    {
      "name": "TWithKey",
      "description": "Adds a primary key on the ID column to the table.",
      "output": {"columns":[{"name":"ID","type":"number"},{"name":"Name","type":"text"}],"rows":[{"ID":1,"Name":"Alice"}]}
    },
    {
      "name": "Keys",
      "description": "Retrieves the list of key definitions from the table's type — returns one key record describing the primary key.",
      "output": {"columns":[{"name":"Value","type":"any"}],"rows":[{"Value":"[Columns={\"ID\"}, Primary=true]"}]}
    },
    {
      "name": "Result",
      "description": "The final output — accesses the first key record, showing its Columns list and Primary flag.",
      "output": {"columns":[{"name":"Columns","type":"any"},{"name":"Primary","type":"logical"}],"rows":[{"Columns":["ID"],"Primary":true}]}
    }
  ]
}
-->

### Example 3: Check whether a table has a primary key declared

```powerquery
let
    T = #table(type table [OrderID = number, Product = text], {{1, "Widget A"}}),
    TWithKey = Table.AddKey(T, {"OrderID"}, true),
    Keys = Type.TableKeys(Value.Type(TWithKey)),
    HasPrimary = List.AnyTrue(List.Transform(Keys, each _[Primary]))
in
    HasPrimary
```

<!--output
{"columns":[{"name":"Result","type":"logical"}],"rows":[{"Result":true}]}
-->

<!--steps
{
  "steps": [
    {
      "name": "T",
      "description": "Creates a typed table with OrderID and Product columns containing one row.",
      "output": {"columns":[{"name":"OrderID","type":"number"},{"name":"Product","type":"text"}],"rows":[{"OrderID":1,"Product":"Widget A"}]}
    },
    {
      "name": "TWithKey",
      "description": "Adds a primary key on the OrderID column to the table.",
      "output": {"columns":[{"name":"OrderID","type":"number"},{"name":"Product","type":"text"}],"rows":[{"OrderID":1,"Product":"Widget A"}]}
    },
    {
      "name": "Keys",
      "description": "Retrieves the list of key definitions from the table's type — returns one key record.",
      "output": {"columns":[{"name":"Value","type":"any"}],"rows":[{"Value":"[Columns={\"OrderID\"}, Primary=true]"}]}
    },
    {
      "name": "HasPrimary",
      "description": "Transforms the key list to extract each Primary field value, then checks if any are true — confirming a primary key exists.",
      "output": {"columns":[{"name":"Value","type":"logical"}],"rows":[{"Value":true}]}
    },
    {
      "name": "Result",
      "description": "The final output — returns true because the table has a primary key declared.",
      "output": {"columns":[{"name":"Result","type":"logical"}],"rows":[{"Result":true}]}
    }
  ]
}
-->

---
title: "Date.ToText"
slug: "date-totext"
category: "Date"
description: "Converts a date value to a text string."
syntax: "Date.ToText(date as nullable date, optional options as any) as nullable text"
returnType: "text"
returnDescription: "A text representation of the date, or null if the input is null."
parameters:
  - name: "date"
    type: "date"
    required: true
    description: "The date value to convert to text."
  - name: "options"
    type: "any"
    required: false
    description: "A record or culture text string. Supported record fields: Format (custom format string, e.g., \"dd/MM/yyyy\") and Culture (e.g., \"en-US\")."
compatibility:
  pbiDesktop: true
  pbiService: true
  excelDesktop: true
  excelOnline: true
  dataflows: true
  fabricNotebooks: true
relatedFunctions:
  - "date-fromtext"
  - "date-dayofweekname"
  - "date-monthname"
  - "datetime-totext"
---

## Remarks

`Date.ToText` formats a `date` value as a text string. Without options, it produces an ISO 8601-style string (e.g., `"2024-03-15"`). Use the `Format` field in the options record to apply custom patterns such as `"dd/MM/yyyy"` or `"MMMM d, yyyy"`. The `Culture` field controls locale-specific formatting (e.g., month names and day-of-week names). Format strings follow .NET custom date format conventions.

Always specify `Culture` when using locale-sensitive format specifiers like `MMMM` (full month name) or `dddd` (full day name) to ensure consistent output across environments. Without an explicit `Culture`, the result depends on the host machine's locale.

If the input is `null`, the function returns `null`. For formatting `datetime` values, use `DateTime.ToText` instead, which handles both date and time components.

## Examples

### Example 1: Format order dates as long-form text

Convert OrderDate to a "Month D, YYYY" display string.

Input: `Sales`

```powerquery
Table.AddColumn(
    Table.SelectColumns(
        Table.FirstN(Sales, 4),
        {"OrderID", "OrderDate"}
    ),
    "OrderDateText",
    each Date.ToText([OrderDate], [Format="MMMM d, yyyy", Culture="en-US"]),
    type text
)
```

<!--output
{"columns":[{"name":"OrderID","type":"number"},{"name":"OrderDate","type":"date"},{"name":"OrderDateText","type":"text"}],"rows":[{"OrderID":1,"OrderDate":"2024-01-15","OrderDateText":"January 15, 2024"},{"OrderID":2,"OrderDate":"2024-01-18","OrderDateText":"January 18, 2024"},{"OrderID":3,"OrderDate":"2024-02-01","OrderDateText":"February 1, 2024"},{"OrderID":4,"OrderDate":"2024-02-10","OrderDateText":"February 10, 2024"}]}
-->

### Example 2: Format a date in UK style

Return a date in "dd/MM/yyyy" format using the en-GB culture.

```powerquery
#table(
    type table [Date = date, Formatted = text],
    {{#date(2024, 3, 15), Date.ToText(#date(2024, 3, 15), [Format="dd/MM/yyyy", Culture="en-GB"])}}
)
```

<!--output
{"columns":[{"name":"Date","type":"date"},{"name":"Formatted","type":"text"}],"rows":[{"Date":"2024-03-15","Formatted":"15/03/2024"}]}
-->

### Example 3: Format as abbreviated month and year

Create a compact "Mon YYYY" period label for report headings.

Input: `Sales`

```powerquery
Table.AddColumn(
    Table.SelectColumns(Table.FirstN(Sales, 5), {"OrderID", "OrderDate"}),
    "PeriodLabel",
    each Date.ToText([OrderDate], [Format="MMM yyyy", Culture="en-US"]),
    type text
)
```

<!--output
{"columns":[{"name":"OrderID","type":"number"},{"name":"OrderDate","type":"date"},{"name":"PeriodLabel","type":"text"}],"rows":[{"OrderID":1,"OrderDate":"2024-01-15","PeriodLabel":"Jan 2024"},{"OrderID":2,"OrderDate":"2024-01-18","PeriodLabel":"Jan 2024"},{"OrderID":3,"OrderDate":"2024-02-01","PeriodLabel":"Feb 2024"},{"OrderID":4,"OrderDate":"2024-02-10","PeriodLabel":"Feb 2024"},{"OrderID":5,"OrderDate":"2024-03-05","PeriodLabel":"Mar 2024"}]}
-->

---
title: "List.MatchesAny"
slug: "list-matchesany"
category: "List"
description: "Returns true if the condition function returns true for at least one item in the list."
syntax: "List.MatchesAny(list as list, condition as function) as logical"
returnType: "logical"
returnDescription: "True if the condition returns true for at least one item; false if no items satisfy the condition."
parameters:
  - name: "list"
    type: "list"
    required: true
    description: "The list to test."
  - name: "condition"
    type: "function"
    required: true
    description: "A function that takes a list item and returns a logical value."
compatibility:
  pbiDesktop: true
  pbiService: true
  excelDesktop: true
  excelOnline: true
  dataflows: true
  fabricNotebooks: true
relatedFunctions:
  - "list-matchesall"
  - "list-anytrue"
  - "list-alltrue"
  - "list-select"
  - "list-contains"
---

## Remarks

`List.MatchesAny` applies a condition (predicate) function to each item in a list and returns `true` as soon as any item satisfies the condition. It is the list equivalent of the existential quantifier — "there exists at least one item for which the condition holds."

Unlike `List.AnyTrue`, which expects a pre-built list of logical values, `List.MatchesAny` accepts any list and evaluates the predicate inline. This makes it the cleaner choice when working directly with data: `List.MatchesAny(Products[InStock], each _ = true)` is more readable than wrapping `List.Transform`.

An empty list returns `false`. The function short-circuits as soon as a passing item is found, so it can be efficient on large lists when an early match is likely. For simple equality checks (does any item equal this value?), `List.Contains` is more idiomatic. Use `List.MatchesAny` for composite or calculated conditions that go beyond simple equality.

## Examples

### Example 1: Check if any number exceeds a threshold

```powerquery
List.MatchesAny({10, 25, 5, 8}, each _ > 20)
```

<!--output
{"columns":[{"name":"Result","type":"logical"}],"rows":[{"Result":"true"}]}
-->

### Example 2: No items match

```powerquery
List.MatchesAny({1, 2, 3}, each _ > 10)
```

<!--output
{"columns":[{"name":"Result","type":"logical"}],"rows":[{"Result":"false"}]}
-->

### Example 3: Check if any product is out of stock

```powerquery
let
    StockLevels = {100, 0, 45, 230, 0},
    HasOutOfStock = List.MatchesAny(StockLevels, each _ = 0)
in
    HasOutOfStock
```

<!--output
{"columns":[{"name":"Result","type":"logical"}],"rows":[{"Result":"true"}]}
-->

<!--steps
{
  "steps": [
    {
      "name": "StockLevels",
      "description": "Defines a list of inventory stock quantities for each product.",
      "output": {"columns":[{"name":"Value","type":"number"}],"rows":[{"Value":100},{"Value":0},{"Value":45},{"Value":230},{"Value":0}]}
    },
    {
      "name": "HasOutOfStock",
      "description": "The final output — checks whether any stock level equals zero, returning true because two products have no stock.",
      "output": {"columns":[{"name":"Result","type":"logical"}],"rows":[{"Result":"true"}]}
    }
  ]
}
-->

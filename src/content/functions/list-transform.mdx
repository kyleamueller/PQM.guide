---
title: "List.Transform"
slug: "list-transform"
category: "List"
description: "Applies a function to each item in a list and returns the results."
syntax: "List.Transform(list as list, transform as function) as list"
returnType: "list"
returnDescription: "A new list with the transform function applied to each item."
parameters:
  - name: "list"
    type: "list"
    required: true
    description: "The list of items to transform."
  - name: "transform"
    type: "function"
    required: true
    description: "The function to apply to each item."
compatibility:
  pbiDesktop: true
  pbiService: true
  excelDesktop: true
  excelOnline: true
  dataflows: true
  fabricNotebooks: true
relatedFunctions:
  - "list-select"
  - "list-accumulate"
  - "table-transformcolumns"
---

## Remarks

`List.Transform` applies a function to every item in a list, returning a new list of the same length. This is equivalent to a "map" operation in other languages. Use `each` syntax for simple transformations.

## Examples

### Example 1: Double all quantities

Double every quantity value from the Sales table.

Input: `Sales`

```powerquery
let
    Quantities = Table.Column(Table.FirstN(Sales, 4), "Quantity"),
    Doubled = List.Transform(Quantities, each _ * 2)
in
    #table({"DoubledQty"}, List.Transform(Doubled, each {_}))
```

<!--output
{"columns":[{"name":"DoubledQty","type":"number"}],"rows":[{"DoubledQty":8},{"DoubledQty":4},{"DoubledQty":20},{"DoubledQty":2}]}
-->
<!--steps
{
  "steps": [
    {
      "name": "Quantities",
      "description": "Extracts the Quantity column from the first 4 rows of the Sales table as a flat list of numbers.",
      "output": {"columns":[{"name":"Value","type":"number"}],"rows":[{"Value":4},{"Value":2},{"Value":10},{"Value":1}]}
    },
    {
      "name": "Doubled",
      "description": "Applies List.Transform to multiply each quantity by 2, producing a new list of the same length with doubled values.",
      "output": {"columns":[{"name":"Value","type":"number"}],"rows":[{"Value":8},{"Value":4},{"Value":20},{"Value":2}]}
    },
    {
      "name": "Result",
      "description": "The final output — a single-column table displaying the doubled quantity values.",
      "output": {"columns":[{"name":"DoubledQty","type":"number"}],"rows":[{"DoubledQty":8},{"DoubledQty":4},{"DoubledQty":20},{"DoubledQty":2}]}
    }
  ]
}
-->

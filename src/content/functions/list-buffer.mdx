---
title: "List.Buffer"
slug: "list-buffer"
category: "List"
description: "Buffers a list in memory, forcing immediate evaluation and preventing repeated re-evaluation of the source."
syntax: "List.Buffer(list as list) as list"
returnType: "list"
returnDescription: "An in-memory copy of the list."
parameters:
  - name: "list"
    type: "list"
    required: true
    description: "The list to buffer into memory."
compatibility:
  pbiDesktop: true
  pbiService: true
  excelDesktop: true
  excelOnline: true
  dataflows: true
  fabricNotebooks: true
relatedFunctions:
  - "list-transform"
  - "list-select"
  - "list-count"
---

## Remarks

`List.Buffer` forces the immediate and complete evaluation of a list and stores the result in memory. The returned list is identical in content to the input but is backed by an in-memory snapshot rather than a lazy query pipeline. Subsequent references to the buffered list read from memory without re-executing the source query.

This is a performance optimization function. In Power Query, lists are lazy — they are evaluated on demand, potentially multiple times. The most critical use case for `List.Buffer` is when a list derived from an external source (such as a table column) is used inside `Table.SelectRows` as the membership list for a `List.Contains` check. Without buffering, Power Query re-evaluates the entire source list on every row of the table being filtered, which causes O(n²) performance on large datasets.

`List.Buffer` is the list equivalent of `Table.Buffer`. Use it whenever a list will be referenced multiple times or when the source of the list is expensive to compute (a database query, web call, or large file read).

## Examples

### Example 1: Buffer a lookup list used inside Table.SelectRows

Without `List.Buffer`, the `AllowedProducts` list is re-evaluated for every row in the Sales table. Buffering it ensures the list is built once and reused.

```powerquery
let
    Sales = #table(
        {"OrderID", "CustomerName", "Product"},
        {{1,"Alice","Widget A"},{2,"Bob","Gadget B"},{3,"Charlie","Widget C"},{4,"Alice","Gadget D"},{5,"Diana","Widget A"}}
    ),
    AllowedProducts = List.Buffer({"Widget A", "Widget C"}),
    Filtered = Table.SelectRows(Sales, each List.Contains(AllowedProducts, [Product]))
in
    Filtered
```

<!--output
{"columns":[{"name":"OrderID","type":"number"},{"name":"CustomerName","type":"text"},{"name":"Product","type":"text"}],"rows":[{"OrderID":1,"CustomerName":"Alice","Product":"Widget A"},{"OrderID":3,"CustomerName":"Charlie","Product":"Widget C"},{"OrderID":5,"CustomerName":"Diana","Product":"Widget A"}]}
-->

<!--steps
{
  "steps": [
    {
      "name": "Sales",
      "description": "Defines an inline table with five orders containing OrderID, CustomerName, and Product columns.",
      "output": {"columns":[{"name":"OrderID","type":"number"},{"name":"CustomerName","type":"text"},{"name":"Product","type":"text"}],"rows":[{"OrderID":1,"CustomerName":"Alice","Product":"Widget A"},{"OrderID":2,"CustomerName":"Bob","Product":"Gadget B"},{"OrderID":3,"CustomerName":"Charlie","Product":"Widget C"},{"OrderID":4,"CustomerName":"Alice","Product":"Gadget D"},{"OrderID":5,"CustomerName":"Diana","Product":"Widget A"}]}
    },
    {
      "name": "AllowedProducts",
      "description": "Buffers the allowed product list into memory so it is evaluated only once instead of once per row during filtering.",
      "output": {"columns":[{"name":"Value","type":"text"}],"rows":[{"Value":"Widget A"},{"Value":"Widget C"}]}
    },
    {
      "name": "Filtered",
      "description": "The final output — filters the Sales table to keep only rows whose Product appears in the buffered allowed list.",
      "output": {"columns":[{"name":"OrderID","type":"number"},{"name":"CustomerName","type":"text"},{"name":"Product","type":"text"}],"rows":[{"OrderID":1,"CustomerName":"Alice","Product":"Widget A"},{"OrderID":3,"CustomerName":"Charlie","Product":"Widget C"},{"OrderID":5,"CustomerName":"Diana","Product":"Widget A"}]}
    }
  ]
}
-->

### Example 2: Buffer a list used in multiple aggregations

Buffer a computed list once and use it for both count and sum, rather than computing it twice.

```powerquery
let
    Values = List.Buffer(List.Numbers(1, 100)),
    Count = List.Count(Values),
    Total = List.Sum(Values)
in
    #table({"Count", "Total"}, {{Count, Total}})
```

<!--output
{"columns":[{"name":"Count","type":"number"},{"name":"Total","type":"number"}],"rows":[{"Count":100,"Total":5050}]}
-->

<!--steps
{
  "steps": [
    {
      "name": "Values",
      "description": "Generates a list of 100 integers from 1 to 100 and buffers it in memory for reuse.",
      "output": {"columns":[{"name":"Value","type":"number"}],"rows":[{"Value":1},{"Value":2},{"Value":3},{"Value":"..."},{"Value":100}]}
    },
    {
      "name": "Count",
      "description": "Counts the number of items in the buffered list, reading from memory rather than re-evaluating the source.",
      "output": {"columns":[{"name":"Value","type":"number"}],"rows":[{"Value":100}]}
    },
    {
      "name": "Total",
      "description": "Sums all items in the buffered list, again reading from the in-memory snapshot.",
      "output": {"columns":[{"name":"Value","type":"number"}],"rows":[{"Value":5050}]}
    },
    {
      "name": "Result",
      "description": "The final output — presents the count and sum together in a two-column table.",
      "output": {"columns":[{"name":"Count","type":"number"},{"name":"Total","type":"number"}],"rows":[{"Count":100,"Total":5050}]}
    }
  ]
}
-->

### Example 3: Buffer random numbers to prevent re-evaluation

`List.Random` generates a new sequence each time it is evaluated. Buffering it pins the values so they remain stable across multiple references in the same query.

```powerquery
let
    Randoms = List.Buffer(List.Random(5, 42)),
    Min = List.Min(Randoms),
    Max = List.Max(Randoms)
in
    #table({"Min", "Max"}, {{Min, Max}})
```

<!--output
{"columns":[{"name":"Min","type":"number"},{"name":"Max","type":"number"}],"rows":[{"Min":0.218,"Max":0.879}]}
-->

<!--steps
{
  "steps": [
    {
      "name": "Randoms",
      "description": "Generates a list of 5 pseudo-random numbers seeded with 42 and buffers it so the values are fixed and not regenerated on each access.",
      "output": {"columns":[{"name":"Value","type":"number"}],"rows":[{"Value":0.218},{"Value":"..."},{"Value":0.879}]}
    },
    {
      "name": "Min",
      "description": "Finds the smallest value in the buffered random list.",
      "output": {"columns":[{"name":"Value","type":"number"}],"rows":[{"Value":0.218}]}
    },
    {
      "name": "Max",
      "description": "Finds the largest value in the buffered random list.",
      "output": {"columns":[{"name":"Value","type":"number"}],"rows":[{"Value":0.879}]}
    },
    {
      "name": "Result",
      "description": "The final output — presents the minimum and maximum of the buffered random numbers in a two-column table.",
      "output": {"columns":[{"name":"Min","type":"number"},{"name":"Max","type":"number"}],"rows":[{"Min":0.218,"Max":0.879}]}
    }
  ]
}
-->

---
title: "Uri.Combine"
slug: "uri-combine"
category: "Uri"
description: "Combines a base URI and a relative URI into a single resolved URI, following standard URI resolution rules."
syntax: "Uri.Combine(baseUri as text, relativeUri as text) as text"
returnType: "text"
returnDescription: "A fully resolved URI text value."
parameters:
  - name: "baseUri"
    type: "text"
    required: true
    description: "The base URI to resolve against."
  - name: "relativeUri"
    type: "text"
    required: true
    description: "The relative URI to resolve. May be an absolute path, relative path, or full URI."
compatibility:
  pbiDesktop: true
  pbiService: true
  excelDesktop: true
  excelOnline: true
  dataflows: true
  fabricNotebooks: true
relatedFunctions:
  - "text-combine"
  - "text-afterdelimiter"
  - "text-beforedelimiter"
---

## Remarks

`Uri.Combine` follows standard RFC 3986 URI resolution rules to combine a base URI with a relative reference. This is the correct way to construct URLs in Power Query rather than using `&` or `Text.Combine`, because it properly handles path traversal, trailing slashes, and absolute path references.

The most important behavioral detail is the **trailing slash rule**:
- `"https://api.example.com/v1/"` + `"users"` produces `"https://api.example.com/v1/users"` (slash present — resolves relative to `/v1/` directory)
- `"https://api.example.com/v1"` + `"users"` produces `"https://api.example.com/users"` (no slash — resolves relative to `/` since `v1` is treated as a file, not a directory)

Always ensure your base URI ends with a trailing slash when you intend to append path segments to it. This is the most common source of bugs when building paginated API calls.

Other key behaviors:
- If `relativeUri` starts with a scheme (e.g., `https://`), it is returned as-is, ignoring the base
- If `relativeUri` starts with `/`, it replaces the entire path on the base host
- If `relativeUri` is a relative path (no leading `/`), it resolves relative to the directory portion of the base path

Prefer `Uri.Combine` over string concatenation even for simple cases — it documents intent clearly and prevents subtle URL construction errors that can be hard to diagnose.

## Examples

### Example 1: Append a path segment to a base URL with trailing slash

```powerquery
Uri.Combine("https://api.example.com/v1/", "users")
```

<!--output
{"columns":[{"name":"Result","type":"text"}],"rows":[{"Result":"https://api.example.com/v1/users"}]}
-->

### Example 2: No trailing slash — relative segment resolves to the parent directory

Without a trailing slash, the last segment of the base path is treated as a file and replaced.

```powerquery
Uri.Combine("https://api.example.com/v1/users", "orders")
```

<!--output
{"columns":[{"name":"Result","type":"text"}],"rows":[{"Result":"https://api.example.com/v1/orders"}]}
-->

### Example 3: Absolute path in the relative URI replaces the base path

A leading `/` in `relativeUri` replaces the entire path on the host, ignoring the base path.

```powerquery
Uri.Combine("https://api.example.com/v1/users", "/v2/orders")
```

<!--output
{"columns":[{"name":"Result","type":"text"}],"rows":[{"Result":"https://api.example.com/v2/orders"}]}
-->

### Example 4: Build a paginated API URL dynamically

Combine a stable base URL with a dynamic page parameter to loop through API pages.

```powerquery
let
    baseUrl = "https://api.example.com/data/",
    page = 2,
    url = Uri.Combine(baseUrl, "records?page=" & Number.ToText(page))
in
    url
```

<!--output
{"columns":[{"name":"Result","type":"text"}],"rows":[{"Result":"https://api.example.com/data/records?page=2"}]}
-->

<!--steps
{
  "steps": [
    {
      "name": "baseUrl",
      "description": "Defines the stable base URL with a trailing slash, pointing to the data directory.",
      "output": {"columns":[{"name":"Value","type":"text"}],"rows":[{"Value":"https://api.example.com/data/"}]}
    },
    {
      "name": "page",
      "description": "Defines the current page number as a number value.",
      "output": {"columns":[{"name":"Value","type":"number"}],"rows":[{"Value":2}]}
    },
    {
      "name": "url",
      "description": "Combines the base URL with a relative path segment that includes the page number converted to text, producing the full paginated URL.",
      "output": {"columns":[{"name":"Value","type":"text"}],"rows":[{"Value":"https://api.example.com/data/records?page=2"}]}
    },
    {
      "name": "Result",
      "description": "The final output — the fully resolved paginated API URL for page 2.",
      "output": {"columns":[{"name":"Result","type":"text"}],"rows":[{"Result":"https://api.example.com/data/records?page=2"}]}
    }
  ]
}
-->

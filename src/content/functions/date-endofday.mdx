---
title: "Date.EndOfDay"
slug: "date-endofday"
category: "Date"
description: "Returns the last moment of the day for the given date or datetime value."
syntax: "Date.EndOfDay(dateTime as any) as any"
returnType: "any"
returnDescription: "A datetime or datetimezone value representing 23:59:59.9999999 on the given day."
parameters:
  - name: "dateTime"
    type: "any"
    required: true
    description: "A date, datetime, or datetimezone value."
compatibility:
  pbiDesktop: true
  pbiService: true
  excelDesktop: true
  excelOnline: true
  dataflows: true
  fabricNotebooks: true
relatedFunctions:
  - "date-startofday"
  - "date-endofmonth"
  - "date-endofweek"
  - "date-endofquarter"
  - "date-endofyear"
---

## Remarks

`Date.EndOfDay` returns the very last instant of the day — 23:59:59.9999999 — as a `datetime` (or `datetimezone`, if the input is a `datetimezone`). When passed a plain `date`, the result is a `datetime`. This is useful for building inclusive upper-bound filters when working with datetime columns, ensuring that events at any time on that day are captured.

A common pattern is combining `Date.StartOfDay` and `Date.EndOfDay` to create an inclusive date range: `[EventTime] >= Date.StartOfDay(startDate) and [EventTime] <= Date.EndOfDay(endDate)`. This avoids the off-by-one errors that occur when filtering datetimes with a date-only boundary.

When passed a `datetimezone`, the output preserves the timezone offset and adjusts only the time components. The function does not convert across timezones — use `DateTimeZone.SwitchZone` if you need timezone conversion before applying the day boundary.

## Examples

### Example 1: Filter log entries on or before the end of January 15

Use an inclusive upper-bound filter on a datetime column.

Input: `OrderLog`

```powerquery
Table.SelectRows(
    OrderLog,
    each [Timestamp] <= Date.EndOfDay(#date(2024, 1, 15))
)
```

<!--output
{"columns":[{"name":"LogID","type":"text"},{"name":"OrderID","type":"number"},{"name":"Action","type":"text"},{"name":"Timestamp","type":"datetime"},{"name":"DurationMinutes","type":"number"},{"name":"Notes","type":"text"}],"rows":[{"LogID":"L001","OrderID":1,"Action":"Created","Timestamp":"2024-01-15T09:30:00","DurationMinutes":null,"Notes":"New order: Widget A"}]}
-->

### Example 2: End-of-day for a specific date

Return the last instant of March 15, 2024.

```powerquery
#table(
    type table [Result = datetime],
    {{Date.EndOfDay(#date(2024, 3, 15))}}
)
```

<!--output
{"columns":[{"name":"Result","type":"datetime"}],"rows":[{"Result":"2024-03-15T23:59:59.9999999"}]}
-->

### Example 3: Build an inclusive date range filter

Filter log entries to a specific day using both StartOfDay and EndOfDay.

Input: `OrderLog`

```powerquery
let
    TargetDate = #date(2024, 1, 16),
    RangeStart = Date.StartOfDay(TargetDate),
    RangeEnd = Date.EndOfDay(TargetDate)
in
    Table.SelectRows(
        OrderLog,
        each [Timestamp] >= RangeStart and [Timestamp] <= RangeEnd
    )
```

<!--output
{"columns":[{"name":"LogID","type":"text"},{"name":"OrderID","type":"number"},{"name":"Action","type":"text"},{"name":"Timestamp","type":"datetime"},{"name":"DurationMinutes","type":"number"},{"name":"Notes","type":"text"}],"rows":[{"LogID":"L002","OrderID":1,"Action":"Shipped","Timestamp":"2024-01-16T14:15:00","DurationMinutes":1725,"Notes":"Shipped via express"}]}
-->

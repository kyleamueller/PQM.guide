---
title: "Text.SplitAny"
slug: "text-splitany"
category: "Text"
description: "Splits a text value at any character found in the separators string, returning a list of substrings."
syntax: "Text.SplitAny(text as text, separators as text) as list"
returnType: "list"
returnDescription: "A list of text values split at any occurrence of any character in separators."
parameters:
  - name: "text"
    type: "text"
    required: true
    description: "The text value to split."
  - name: "separators"
    type: "text"
    required: true
    description: "A text value whose individual characters are each treated as a delimiter."
compatibility:
  pbiDesktop: true
  pbiService: true
  excelDesktop: true
  excelOnline: true
  dataflows: true
  fabricNotebooks: true
relatedFunctions:
  - "text-split"
  - "splitter-splittextbyanydelimiter"
  - "splitter-splittextbydelimiter"
---

## Remarks

`Text.SplitAny` splits `text` at every occurrence of any character found in the `separators` string. Each character in `separators` is treated as an independent single-character delimiter — not as a multi-character substring to match. This is the key difference from `Text.Split`, which treats the entire second argument as one delimiter.

For example, `Text.SplitAny("a,b;c", ",;")` splits on both `,` and `;` independently, producing `{"a", "b", "c"}`. In contrast, `Text.Split("a,b;c", ",;")` would look for the exact two-character sequence `",;"` and return the original string unsplit if that sequence does not appear.

Consecutive separator characters produce empty strings in the result list. If your source data may have multiple consecutive separators (such as double spaces or mixed delimiters), filter the result with `List.Select(result, each _ <> "")` to remove empty entries.

A notable application of `Text.SplitAny` is collapsing internal spaces: `Text.Combine(Text.SplitAny(text, " "), " ")` splits on spaces and rejoins with a single space, effectively collapsing runs of multiple spaces. `Text.Trim` alone does not remove internal duplicate spaces — this pattern is the standard workaround.

## Examples

### Example 1: Split on multiple delimiter characters

```powerquery
Text.SplitAny("East,West;North", ",;")
```

<!--output
{"columns":[{"name":"Result","type":"list"}],"rows":[{"Result":["East","West","North"]}]}
-->

### Example 2: Normalize multiple internal spaces in a name

`Text.Trim` does not collapse internal spaces — use `Text.SplitAny` and `Text.Combine` to normalize spacing.

```powerquery
Text.Combine(Text.SplitAny("alice  smith", " "), " ")
```

<!--output
{"columns":[{"name":"Result","type":"text"}],"rows":[{"Result":"alice smith"}]}
-->

### Example 3: Split a path that uses mixed separators

```powerquery
Text.SplitAny("C:/Users\\Kyle/Documents", "/\\")
```

<!--output
{"columns":[{"name":"Result","type":"list"}],"rows":[{"Result":["C:","Users","Kyle","Documents"]}]}
-->

### Example 4: Filter empty entries produced by consecutive separators

```powerquery
let
    raw = "Sales,,East,,Q1",
    parts = Text.SplitAny(raw, ","),
    nonEmpty = List.Select(parts, each _ <> "")
in
    nonEmpty
```

<!--output
{"columns":[{"name":"Result","type":"list"}],"rows":[{"Result":["Sales","East","Q1"]}]}
-->

<!--steps
{
  "steps": [
    {
      "name": "raw",
      "description": "Stores the source string \"Sales,,East,,Q1\" which contains consecutive comma separators that will produce empty entries when split.",
      "output": {"columns":[{"name":"Value","type":"text"}],"rows":[{"Value":"Sales,,East,,Q1"}]}
    },
    {
      "name": "parts",
      "description": "Splits the string on every comma using Text.SplitAny, producing a list of five elements including two empty strings from the consecutive commas.",
      "output": {"columns":[{"name":"Value","type":"text"}],"rows":[{"Value":"Sales"},{"Value":""},{"Value":"East"},{"Value":""},{"Value":"Q1"}]}
    },
    {
      "name": "nonEmpty",
      "description": "The final output — filters the list with List.Select to remove all empty strings, leaving only the three non-empty segments.",
      "output": {"columns":[{"name":"Result","type":"list"}],"rows":[{"Result":["Sales","East","Q1"]}]}
    }
  ]
}
-->

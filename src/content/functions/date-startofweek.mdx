---
title: "Date.StartOfWeek"
slug: "date-startofweek"
category: "Date"
description: "Returns the first day of the week containing the given date."
syntax: "Date.StartOfWeek(dateTime as any, optional firstDayOfWeek as nullable number) as any"
returnType: "any"
returnDescription: "A date, datetime, or datetimezone value representing the first day of the week."
parameters:
  - name: "dateTime"
    type: "any"
    required: true
    description: "A date, datetime, or datetimezone value."
  - name: "firstDayOfWeek"
    type: "number"
    required: false
    description: "A Day.* constant specifying the first day of the week. Defaults to Day.Sunday (0). Use Day.Monday (1) for ISO weeks."
compatibility:
  pbiDesktop: true
  pbiService: true
  excelDesktop: true
  excelOnline: true
  dataflows: true
  fabricNotebooks: true
relatedFunctions:
  - "date-endofweek"
  - "date-weekofyear"
  - "date-weekofmonth"
  - "date-startofmonth"
---

## Remarks

`Date.StartOfWeek` returns the first day of the calendar week containing the input value. By default, weeks start on Sunday (`Day.Sunday = 0`). Pass `Day.Monday` (1) for ISO 8601-style Monday-to-Sunday weeks. The return type matches the input type: a plain `date` input returns a `date`; a `datetime` or `datetimezone` input returns the same type with the time component set to midnight of the week-start day.

Always use the `Day.*` enum constants (e.g., `Day.Monday`, `Day.Sunday`) rather than integer literals for the `firstDayOfWeek` parameter. This makes code self-documenting and avoids confusion.

Pair `Date.StartOfWeek` with `Date.EndOfWeek` using the same `firstDayOfWeek` value to build consistent weekly range filters. `Date.StartOfWeek` is also used as a grouping key for weekly aggregations — group by `Date.StartOfWeek([OrderDate], Day.Monday)` to bucket all orders into their ISO week.

## Examples

### Example 1: Add the week start date to each order

Add a column with the Sunday of the week containing each order date.

Input: `Sales`

```powerquery
Table.AddColumn(
    Table.SelectColumns(
        Table.FirstN(Sales, 5),
        {"OrderID", "OrderDate"}
    ),
    "WeekStart", each Date.StartOfWeek([OrderDate], Day.Sunday), type date
)
```

<!--output
{"columns":[{"name":"OrderID","type":"number"},{"name":"OrderDate","type":"date"},{"name":"WeekStart","type":"date"}],"rows":[{"OrderID":1,"OrderDate":"2024-01-15","WeekStart":"2024-01-14"},{"OrderID":2,"OrderDate":"2024-01-18","WeekStart":"2024-01-14"},{"OrderID":3,"OrderDate":"2024-02-01","WeekStart":"2024-01-28"},{"OrderID":4,"OrderDate":"2024-02-10","WeekStart":"2024-02-04"},{"OrderID":5,"OrderDate":"2024-03-05","WeekStart":"2024-03-03"}]}
-->

### Example 2: Start of ISO week (Monday start)

Return the Monday of the ISO week containing March 13, 2024.

```powerquery
#table(
    type table [Date = date, ISOWeekStart = date],
    {{#date(2024, 3, 13), Date.StartOfWeek(#date(2024, 3, 13), Day.Monday)}}
)
```

<!--output
{"columns":[{"name":"Date","type":"date"},{"name":"ISOWeekStart","type":"date"}],"rows":[{"Date":"2024-03-13","ISOWeekStart":"2024-03-11"}]}
-->

### Example 3: Group sales by week start for weekly aggregation

Add a week-start key to enable weekly grouping.

Input: `Sales`

```powerquery
Table.AddColumn(
    Table.SelectColumns(Sales, {"OrderID", "OrderDate", "UnitPrice", "Quantity"}),
    "WeekStarting",
    each Date.StartOfWeek([OrderDate], Day.Monday),
    type date
)
```

<!--output
{"columns":[{"name":"OrderID","type":"number"},{"name":"OrderDate","type":"date"},{"name":"UnitPrice","type":"number"},{"name":"Quantity","type":"number"},{"name":"WeekStarting","type":"date"}],"rows":[{"OrderID":1,"OrderDate":"2024-01-15","UnitPrice":25,"Quantity":4,"WeekStarting":"2024-01-15"},{"OrderID":2,"OrderDate":"2024-01-18","UnitPrice":50,"Quantity":2,"WeekStarting":"2024-01-15"},{"OrderID":3,"OrderDate":"2024-02-01","UnitPrice":15,"Quantity":10,"WeekStarting":"2024-01-29"},{"OrderID":4,"OrderDate":"2024-02-10","UnitPrice":75,"Quantity":1,"WeekStarting":"2024-02-05"},{"OrderID":5,"OrderDate":"2024-03-05","UnitPrice":25,"Quantity":6,"WeekStarting":"2024-03-04"},{"OrderID":6,"OrderDate":"2024-03-12","UnitPrice":120,"Quantity":1,"WeekStarting":"2024-03-11"},{"OrderID":7,"OrderDate":"2024-04-01","UnitPrice":50,"Quantity":3,"WeekStarting":"2024-04-01"},{"OrderID":8,"OrderDate":"2024-04-15","UnitPrice":15,"Quantity":8,"WeekStarting":"2024-04-15"}]}
-->

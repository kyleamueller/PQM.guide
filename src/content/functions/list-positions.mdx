---
title: "List.Positions"
slug: "list-positions"
category: "List"
description: "Returns a list of zero-based index positions for all elements in the given list."
syntax: "List.Positions(list as list) as list"
returnType: "list"
returnDescription: "A list of integers {0, 1, 2, ...} with one entry per element in the source list."
parameters:
  - name: "list"
    type: "list"
    required: true
    description: "The list whose positions are to be returned."
compatibility:
  pbiDesktop: true
  pbiService: true
  excelDesktop: true
  excelOnline: true
  dataflows: true
  fabricNotebooks: true
relatedFunctions:
  - "list-positionof"
  - "list-count"
  - "list-range"
  - "list-numbers"
  - "list-zip"
---

## Remarks

`List.Positions` returns a list of zero-based integer indices, one for each element in the input list. For a list of length `N`, it returns `{0, 1, 2, ..., N-1}`. It is equivalent to `List.Numbers(0, List.Count(list))`.

The primary use case is enabling index-aware iteration: by combining the positions list with the original list using `List.Zip`, you get `{{0, item0}, {1, item1}, ...}` pairs that let you know each item's position during a transform. This is the standard M pattern for "transform with index" — something that has no built-in direct syntax.

For an empty list, returns an empty list `{}`. The result is always consecutive integers starting at 0. If you need indices starting at 1, use `List.Transform(List.Positions(list), each _ + 1)` or `List.Numbers(1, List.Count(list))`.

## Examples

### Example 1: Get positions of a list

```powerquery
List.Positions({"A", "B", "C", "D"})
```

<!--output
{"columns":[{"name":"Result","type":"any"}],"rows":[{"Result":"{0, 1, 2, 3}"}]}
-->

### Example 2: Pair each item with its index using List.Zip

```powerquery
let
    Items = {"Alpha", "Beta", "Gamma"},
    Indexed = List.Zip({List.Positions(Items), Items})
in
    Indexed
```

<!--output
{"columns":[{"name":"Result","type":"any"}],"rows":[{"Result":"{{0, Alpha}, {1, Beta}, {2, Gamma}}"}]}
-->

<!--steps
{
  "steps": [
    {
      "name": "Items",
      "description": "Defines a list of three text values to be indexed.",
      "output": {"columns":[{"name":"Value","type":"text"}],"rows":[{"Value":"Alpha"},{"Value":"Beta"},{"Value":"Gamma"}]}
    },
    {
      "name": "Indexed",
      "description": "Pairs each item with its zero-based position by zipping the positions list with the Items list, producing a list of {index, value} pairs.",
      "output": {"columns":[{"name":"Result","type":"any"}],"rows":[{"Result":"{{0, Alpha}, {1, Beta}, {2, Gamma}}"}]}
    },
    {
      "name": "Result",
      "description": "The final output — a list of {position, item} pairs for each element in Items.",
      "output": {"columns":[{"name":"Result","type":"any"}],"rows":[{"Result":"{{0, Alpha}, {1, Beta}, {2, Gamma}}"}]}
    }
  ]
}
-->

### Example 3: Filter items by even index positions

```powerquery
let
    Items = {"A", "B", "C", "D", "E", "F"},
    Positions = List.Positions(Items),
    EvenIndexItems = List.Transform(
        List.Select(Positions, each Number.Mod(_, 2) = 0),
        each Items{_}
    )
in
    EvenIndexItems
```

<!--output
{"columns":[{"name":"Result","type":"any"}],"rows":[{"Result":"{A, C, E}"}]}
-->

<!--steps
{
  "steps": [
    {
      "name": "Items",
      "description": "Defines a list of 6 text values to filter by index position.",
      "output": {"columns":[{"name":"Value","type":"text"}],"rows":[{"Value":"A"},{"Value":"B"},{"Value":"C"},{"Value":"D"},{"Value":"E"},{"Value":"F"}]}
    },
    {
      "name": "Positions",
      "description": "Generates a list of zero-based index positions {0, 1, 2, 3, 4, 5} corresponding to each item in Items.",
      "output": {"columns":[{"name":"Value","type":"number"}],"rows":[{"Value":0},{"Value":1},{"Value":2},{"Value":3},{"Value":4},{"Value":5}]}
    },
    {
      "name": "EvenIndexItems",
      "description": "Filters Positions to keep only even indices (0, 2, 4) and then retrieves the Items at those positions, returning \"A\", \"C\", and \"E\".",
      "output": {"columns":[{"name":"Result","type":"any"}],"rows":[{"Result":"{A, C, E}"}]}
    },
    {
      "name": "Result",
      "description": "The final output — a list containing only the items at even index positions from the original list.",
      "output": {"columns":[{"name":"Result","type":"any"}],"rows":[{"Result":"{A, C, E}"}]}
    }
  ]
}
-->

---
title: "Number.IntegerDivide"
slug: "number-integerdivide"
category: "Number"
description: "Performs integer (floor) division, returning the quotient without the remainder."
syntax: "Number.IntegerDivide(number1 as nullable number, number2 as nullable number, optional precision as nullable number) as nullable number"
returnType: "number"
returnDescription: "The integer quotient of dividing number1 by number2."
parameters:
  - name: "number1"
    type: "number"
    required: true
    description: "The dividend."
  - name: "number2"
    type: "number"
    required: true
    description: "The divisor. Must not be zero."
  - name: "precision"
    type: "number"
    required: false
    description: "An optional precision value. Typically omitted; use Precision.Decimal for decimal-aware division."
compatibility:
  pbiDesktop: true
  pbiService: true
  excelDesktop: true
  excelOnline: true
  dataflows: true
  fabricNotebooks: true
relatedFunctions:
  - "number-mod"
  - "number-rounddown"
---

## Remarks

`Number.IntegerDivide` returns the integer **quotient** of dividing `number1` by `number2`, discarding any remainder. For positive operands this is equivalent to `Number.RoundDown(number1 / number2)`. It always truncates toward negative infinity (floor division), not toward zero — this is relevant when operands have different signs.

The remainder (what `Number.IntegerDivide` discards) can be obtained with `Number.Mod`. The two are complementary: `(Number.IntegerDivide(a, b) * b) + Number.Mod(a, b) = a`.

Dividing by zero raises a runtime error. If your data might contain zero divisors, guard with `if [Divisor] = 0 then null else Number.IntegerDivide([Value], [Divisor])` or use `try`/`otherwise`.

The optional `precision` parameter accepts `Precision.Decimal` to perform decimal-aware division; for most use cases omit this parameter.

Common uses: bucketing quantities into fixed-size groups (`Number.IntegerDivide(quantity, groupSize) * groupSize`), computing whole time units from a total, and chunking data for batch processing.

## Examples

### Example 1: How many full weeks fit in 17 days?

```powerquery
Number.IntegerDivide(17, 7)
```

<!--output
{"columns":[{"name":"Result","type":"number"}],"rows":[{"Result":2}]}
-->

### Example 2: Split total days into full weeks and remaining days

```powerquery
let
    TotalDays = 17,
    Weeks = Number.IntegerDivide(TotalDays, 7),
    Remaining = Number.Mod(TotalDays, 7)
in
    #table({"Weeks", "RemainderDays"}, {{Weeks, Remaining}})
```

<!--output
{"columns":[{"name":"Weeks","type":"number"},{"name":"RemainderDays","type":"number"}],"rows":[{"Weeks":2,"RemainderDays":3}]}
-->
<!--steps
{
  "steps": [
    {
      "name": "TotalDays",
      "description": "Assigns the total number of days to be split (17 days).",
      "output": {"columns":[{"name":"Value","type":"number"}],"rows":[{"Value":17}]}
    },
    {
      "name": "Weeks",
      "description": "Divides 17 by 7 using integer division, returning 2 — the number of complete weeks.",
      "output": {"columns":[{"name":"Value","type":"number"}],"rows":[{"Value":2}]}
    },
    {
      "name": "Remaining",
      "description": "Computes the remainder of 17 ÷ 7 using Number.Mod, returning 3 — the leftover days after removing complete weeks.",
      "output": {"columns":[{"name":"Value","type":"number"}],"rows":[{"Value":3}]}
    },
    {
      "name": "Result",
      "description": "The final output — a single-row table showing Weeks = 2 and RemainderDays = 3.",
      "output": {"columns":[{"name":"Weeks","type":"number"},{"name":"RemainderDays","type":"number"}],"rows":[{"Weeks":2,"RemainderDays":3}]}
    }
  ]
}
-->

### Example 3: Bucket order quantities into groups of 5

Input: `Sales`

```powerquery
Table.AddColumn(
    Table.SelectColumns(Table.FirstN(Sales, 5), {"OrderID", "CustomerName", "Quantity"}),
    "QtyBucket",
    each Number.IntegerDivide([Quantity], 5) * 5,
    type number
)
```

<!--output
{"columns":[{"name":"OrderID","type":"number"},{"name":"CustomerName","type":"text"},{"name":"Quantity","type":"number"},{"name":"QtyBucket","type":"number"}],"rows":[{"OrderID":1,"CustomerName":"Alice","Quantity":4,"QtyBucket":0},{"OrderID":2,"CustomerName":"Bob","Quantity":2,"QtyBucket":0},{"OrderID":3,"CustomerName":"Charlie","Quantity":10,"QtyBucket":10},{"OrderID":4,"CustomerName":"Alice","Quantity":1,"QtyBucket":0},{"OrderID":5,"CustomerName":"Diana","Quantity":6,"QtyBucket":5}]}
-->

---
title: "List.FirstN"
slug: "list-firstn"
category: "List"
description: "Returns the first N items from a list, or items from the start of the list while a condition is true."
syntax: "List.FirstN(list as list, countOrCondition as any) as any"
returnType: "any"
returnDescription: "A list of the first N items, or a list of items from the start while the condition holds true."
parameters:
  - name: "list"
    type: "list"
    required: true
    description: "The source list."
  - name: "countOrCondition"
    type: "any"
    required: true
    description: "A number specifying how many items to return, or a function (condition) that returns true while items should be included."
compatibility:
  pbiDesktop: true
  pbiService: true
  excelDesktop: true
  excelOnline: true
  dataflows: true
  fabricNotebooks: true
relatedFunctions:
  - "list-lastn"
  - "list-first"
  - "list-skip"
  - "list-range"
  - "list-select"
---

## Remarks

`List.FirstN` is the flexible counterpart to `List.First`. While `List.First` returns a single scalar value, `List.FirstN` always returns a **list**. It accepts either a number or a condition function as its second argument.

When passed a number `N`, it returns a list of the first `N` items. If `N` exceeds the list length, the entire list is returned. If `N` is `0`, an empty list is returned.

When passed a condition function, items are collected from the start of the list while the function returns `true`. Collection stops permanently at the first item that fails — remaining items are excluded even if they would have satisfied the condition. This makes the condition mode useful for collecting a leading run (e.g., values below a threshold, or dates before a cutoff), but not for general filtering. Use `List.Select` for non-contiguous filtering based on a predicate.

## Examples

### Example 1: Return the first 3 items

```powerquery
List.FirstN({10, 20, 30, 40, 50}, 3)
```

<!--output
{"columns":[{"name":"Result","type":"any"}],"rows":[{"Result":"{10, 20, 30}"}]}
-->

### Example 2: Return items while value is less than 40

```powerquery
List.FirstN({10, 20, 30, 40, 50}, each _ < 40)
```

<!--output
{"columns":[{"name":"Result","type":"any"}],"rows":[{"Result":"{10, 20, 30}"}]}
-->

### Example 3: Take top N sorted values

Return the 3 highest-priced products.

```powerquery
let
    Prices = {75, 120, 25, 200, 50},
    Sorted = List.Sort(Prices, Order.Descending),
    TopThree = List.FirstN(Sorted, 3)
in
    TopThree
```

<!--output
{"columns":[{"name":"Result","type":"any"}],"rows":[{"Result":"{200, 120, 75}"}]}
-->

<!--steps
{
  "steps": [
    {
      "name": "Prices",
      "description": "Defines a list of product prices as raw numbers.",
      "output": {"columns":[{"name":"Value","type":"number"}],"rows":[{"Value":75},{"Value":120},{"Value":25},{"Value":200},{"Value":50}]}
    },
    {
      "name": "Sorted",
      "description": "Sorts the prices list in descending order so the highest values come first.",
      "output": {"columns":[{"name":"Value","type":"number"}],"rows":[{"Value":200},{"Value":120},{"Value":75},{"Value":50},{"Value":25}]}
    },
    {
      "name": "TopThree",
      "description": "The final output — takes the first 3 items from the sorted descending list, yielding the 3 highest prices.",
      "output": {"columns":[{"name":"Result","type":"any"}],"rows":[{"Result":"{200, 120, 75}"}]}
    }
  ]
}
-->

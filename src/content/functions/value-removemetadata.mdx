---
title: "Value.RemoveMetadata"
slug: "value-removemetadata"
category: "Value"
description: "Returns a copy of a value with all metadata removed, or with specified metadata fields removed."
syntax: "Value.RemoveMetadata(value as any, optional metadataRecord as nullable record) as any"
returnType: "any"
returnDescription: "The value with metadata removed."
parameters:
  - name: "value"
    type: "any"
    required: true
    description: "The value whose metadata is removed."
  - name: "metadataRecord"
    type: "record"
    required: false
    description: "An optional record specifying which metadata fields to remove. If omitted, all metadata is removed."
compatibility:
  pbiDesktop: true
  pbiService: true
  excelDesktop: true
  excelOnline: true
  dataflows: true
  fabricNotebooks: true
relatedFunctions:
  - "value-metadata"
  - "value-replacemetadata"
---

## Remarks

`Value.RemoveMetadata` strips the metadata record from a value, returning the value with no (or fewer) metadata fields. There are two modes:

- **Without the optional parameter**: removes all metadata — the result has an empty metadata record
- **With a `metadataRecord` parameter**: removes only the metadata fields whose names appear as fields in the provided record; other metadata fields are retained

The underlying data value is not changed — only the attached metadata record is modified. This is the destructive counterpart to `Value.ReplaceMetadata`.

Common use cases include:

- **Cleaning values before output**: stripping internal engine annotations before returning values to end users or downstream systems
- **Selective metadata removal**: keeping useful annotations (e.g., display names) while removing internal bookkeeping fields
- **Testing**: verifying that a value after an operation carries no unexpected metadata

Note that most M operations already discard metadata automatically. `Value.RemoveMetadata` is only needed when you have explicitly attached metadata and want to explicitly remove it, or when you receive a value from a connector that carries metadata you want to strip.

## Examples

### Example 1: Remove all metadata from a value

```powerquery
let
    Annotated = 42 meta [Source = "Raw", Verified = false],
    Clean = Value.RemoveMetadata(Annotated)
in
    Value.Metadata(Clean)
```

<!--output
{"columns":[{"name":"Name","type":"text"},{"name":"Value","type":"any"}],"rows":[]}
-->

<!--steps
{
  "steps": [
    {
      "name": "Annotated",
      "description": "Creates the number 42 with a metadata record attached containing Source = \"Raw\" and Verified = false.",
      "output": {"columns":[{"name":"Value","type":"number"}],"rows":[{"Value":42}]}
    },
    {
      "name": "Clean",
      "description": "Removes all metadata from the annotated value using Value.RemoveMetadata, leaving the underlying value intact but with an empty metadata record.",
      "output": {"columns":[{"name":"Value","type":"number"}],"rows":[{"Value":42}]}
    },
    {
      "name": "Result",
      "description": "The final output — retrieves the metadata from the cleaned value, returning an empty record (no rows) confirming all metadata was removed.",
      "output": {"columns":[{"name":"Name","type":"text"},{"name":"Value","type":"any"}],"rows":[]}
    }
  ]
}
-->

### Example 2: The underlying value is preserved after removing metadata

```powerquery
let
    Annotated = 42 meta [Source = "Raw"],
    Clean = Value.RemoveMetadata(Annotated)
in
    Clean
```

<!--output
{"columns":[{"name":"Result","type":"number"}],"rows":[{"Result":42}]}
-->

<!--steps
{
  "steps": [
    {
      "name": "Annotated",
      "description": "Creates the number 42 with a metadata record attached containing Source = \"Raw\".",
      "output": {"columns":[{"name":"Value","type":"number"}],"rows":[{"Value":42}]}
    },
    {
      "name": "Clean",
      "description": "Removes all metadata from the annotated value — the underlying number 42 is preserved.",
      "output": {"columns":[{"name":"Value","type":"number"}],"rows":[{"Value":42}]}
    },
    {
      "name": "Result",
      "description": "The final output — returns the cleaned value directly, showing that the number 42 is intact after metadata removal.",
      "output": {"columns":[{"name":"Result","type":"number"}],"rows":[{"Result":42}]}
    }
  ]
}
-->

### Example 3: Verify that stripped value equals the unwrapped literal

```powerquery
let
    Annotated = "hello" meta [Tag = "test"],
    Stripped = Value.RemoveMetadata(Annotated)
in
    Value.Equals(Stripped, "hello")
```

<!--output
{"columns":[{"name":"Result","type":"logical"}],"rows":[{"Result":true}]}
-->

<!--steps
{
  "steps": [
    {
      "name": "Annotated",
      "description": "Creates the text value \"hello\" with a metadata record attached containing Tag = \"test\".",
      "output": {"columns":[{"name":"Value","type":"text"}],"rows":[{"Value":"hello"}]}
    },
    {
      "name": "Stripped",
      "description": "Removes all metadata from the annotated value, returning \"hello\" with no metadata attached.",
      "output": {"columns":[{"name":"Value","type":"text"}],"rows":[{"Value":"hello"}]}
    },
    {
      "name": "Result",
      "description": "The final output — checks that the stripped value is equal to the plain literal \"hello\", returning true to confirm the underlying value is unchanged.",
      "output": {"columns":[{"name":"Result","type":"logical"}],"rows":[{"Result":true}]}
    }
  ]
}
-->

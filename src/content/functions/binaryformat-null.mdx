---
title: BinaryFormat.Null
slug: binaryformat-null
category: Binary
description: A binary format that reads zero bytes and returns null.
syntax: BinaryFormat.Null(binary as binary) as any
returnType: function
returnDescription: A binary format function that reads zero bytes and returns null.
parameters:
  - name: binary
    type: binary
    required: true
    description: The binary data (no bytes are consumed).
compatibility:
  pbiDesktop: true
  pbiService: true
  excelDesktop: true
  excelOnline: true
  dataflows: true
  fabricNotebooks: true
relatedFunctions:
  - binaryformat-byte
  - binaryformat-record
  - binaryformat-choice
  - binaryformat-group
relatedConcepts: []
---

## Remarks

`BinaryFormat.Null` is a binary format specifier that reads zero bytes from the binary stream and returns `null`. It does not advance the read position.

This is useful as a placeholder in composite binary formats like `BinaryFormat.Record` or `BinaryFormat.Choice` where a field needs to exist in the result but does not correspond to any actual data in the binary stream. It can also be used in `BinaryFormat.Group` to skip or ignore certain tagged items.

## Examples

### Example 1: Use BinaryFormat.Null in a record

Include a null placeholder field in a binary record format.

```powerquery
let
    binaryData = #binary({0x01, 0x02}),
    format = BinaryFormat.Record([
        First = BinaryFormat.Byte,
        Padding = BinaryFormat.Null,
        Second = BinaryFormat.Byte
    ])
in
    format(binaryData)
```



---
title: "List.Skip"
slug: "list-skip"
category: "List"
description: "Skips the first N items of a list (or items while a condition is true) and returns the remaining items."
syntax: "List.Skip(list as list, countOrCondition as any) as list"
returnType: "list"
returnDescription: "A new list with the first N items (or leading items matching the condition) removed."
parameters:
  - name: "list"
    type: "list"
    required: true
    description: "The source list."
  - name: "countOrCondition"
    type: "any"
    required: true
    description: "A number specifying how many items to skip, or a condition function that skips items while it returns true."
compatibility:
  pbiDesktop: true
  pbiService: true
  excelDesktop: true
  excelOnline: true
  dataflows: true
  fabricNotebooks: true
relatedFunctions:
  - "list-removefirstn"
  - "list-firstn"
  - "list-range"
  - "list-select"
---

## Remarks

`List.Skip` removes items from the beginning of a list and returns all remaining items. It is functionally identical to `List.RemoveFirstN` — the two functions behave the same way. Use whichever reads more naturally in context.

When passed a number `N`, it skips the first `N` items. If `N` is greater than the list length, an empty list is returned. When passed a condition function, items are skipped from the start of the list while the condition returns `true`. Skipping stops permanently at the first item that fails the condition — subsequent items (even if they would satisfy the condition) are not skipped.

The condition mode is useful for advancing past a leading section — for example, skipping all rows before the first non-null value, or advancing past dates before a given date. For pagination, combining `List.Skip` and `List.FirstN` is the standard pattern: skip `pageSize * (page - 1)` items, then take `pageSize`.

## Examples

### Example 1: Skip the first 3 items

```powerquery
List.Skip({1, 2, 3, 4, 5}, 3)
```

<!--output
{"columns":[{"name":"Result","type":"any"}],"rows":[{"Result":"{4, 5}"}]}
-->

### Example 2: Skip items while they are less than 30

```powerquery
List.Skip({10, 20, 30, 40, 50}, each _ < 30)
```

<!--output
{"columns":[{"name":"Result","type":"any"}],"rows":[{"Result":"{30, 40, 50}"}]}
-->

### Example 3: Paginate through a list (page 2 of 3)

```powerquery
let
    AllItems = List.Numbers(1, 15),
    PageSize = 5,
    Page = 2,
    PageItems = List.FirstN(List.Skip(AllItems, PageSize * (Page - 1)), PageSize)
in
    PageItems
```

<!--output
{"columns":[{"name":"Result","type":"any"}],"rows":[{"Result":"{6, 7, 8, 9, 10}"}]}
-->
<!--steps
{
  "steps": [
    {
      "name": "AllItems",
      "description": "Generates a list of 15 consecutive integers starting at 1, representing the full dataset to paginate through.",
      "output": {"columns":[{"name":"Value","type":"number"}],"rows":[{"Value":1},{"Value":2},{"Value":3},{"Value":4},{"Value":5},{"Value":6},{"Value":7},{"Value":8},{"Value":9},{"Value":10},{"Value":11},{"Value":12},{"Value":13},{"Value":14},{"Value":15}]}
    },
    {
      "name": "PageSize",
      "description": "Defines the number of items per page as the scalar value 5.",
      "output": {"columns":[{"name":"Value","type":"number"}],"rows":[{"Value":5}]}
    },
    {
      "name": "Page",
      "description": "Defines the current page number as the scalar value 2.",
      "output": {"columns":[{"name":"Value","type":"number"}],"rows":[{"Value":2}]}
    },
    {
      "name": "PageItems",
      "description": "Skips the first 5 items (page 1) using List.Skip, then takes the next 5 items using List.FirstN, returning the second page of results.",
      "output": {"columns":[{"name":"Value","type":"number"}],"rows":[{"Value":6},{"Value":7},{"Value":8},{"Value":9},{"Value":10}]}
    },
    {
      "name": "Result",
      "description": "The final output — the five items that make up page 2 of the paginated list.",
      "output": {"columns":[{"name":"Result","type":"any"}],"rows":[{"Result":"{6, 7, 8, 9, 10}"}]}
    }
  ]
}
-->

---
title: "Number.IsNaN"
slug: "number-isnan"
category: "Number"
description: "Returns true if the number is NaN (Not a Number), such as the result of 0/0."
syntax: "Number.IsNaN(number as number) as logical"
returnType: "logical"
returnDescription: "True if the value is NaN, false otherwise."
parameters:
  - name: "number"
    type: "number"
    required: true
    description: "The number to test for NaN."
compatibility:
  pbiDesktop: true
  pbiService: true
  excelDesktop: true
  excelOnline: true
  dataflows: true
  fabricNotebooks: true
relatedFunctions:
  - "number-from"
  - "number-iseven"
  - "number-isodd"
---

## Remarks

`Number.IsNaN` returns `true` when the input is the special IEEE 754 "Not a Number" (NaN) value. NaN is the result of undefined floating-point operations such as `0 / 0`, `Number.Sqrt(-1)`, `Number.Acos(2)` (out-of-domain input), or arithmetic involving existing NaN values.

The critical behavior that makes `Number.IsNaN` necessary: **NaN is not equal to itself**. In M, `(0/0) = (0/0)` evaluates to `false`, not `true`. This means you cannot use the `=` operator or `Value.Equals` to detect NaN — you must use `Number.IsNaN`.

NaN propagates silently through arithmetic: adding, multiplying, or comparing with NaN produces NaN or incorrect results without raising an error. This makes NaN values dangerous in pipelines — they can corrupt aggregations and summaries without any visible error. Always guard against NaN at data ingestion or when dividing values.

**Important limitation**: `Number.IsNaN` does not accept `null` as input and will raise an error if the value is null. Always filter out nulls before calling this function: `if value = null then null else Number.IsNaN(value)`.

## Examples

### Example 1: Zero divided by zero produces NaN

```powerquery
Number.IsNaN(0 / 0)
```

<!--output
{"columns":[{"name":"Result","type":"logical"}],"rows":[{"Result":true}]}
-->

### Example 2: A finite number is not NaN

```powerquery
Number.IsNaN(3.14)
```

<!--output
{"columns":[{"name":"Result","type":"logical"}],"rows":[{"Result":false}]}
-->

### Example 3: Guard against division-by-zero NaN when computing a ratio

Input: `Sales`

Safely compute UnitPrice divided by Quantity, replacing NaN with null when Quantity is 0.

```powerquery
Table.AddColumn(
    Table.SelectColumns(Table.FirstN(Sales, 5), {"OrderID", "CustomerName", "UnitPrice", "Quantity"}),
    "PricePerUnit",
    each let ratio = [UnitPrice] / [Quantity]
        in if Number.IsNaN(ratio) then null else ratio,
    type number
)
```

<!--output
{"columns":[{"name":"OrderID","type":"number"},{"name":"CustomerName","type":"text"},{"name":"UnitPrice","type":"number"},{"name":"Quantity","type":"number"},{"name":"PricePerUnit","type":"number"}],"rows":[{"OrderID":1,"CustomerName":"Alice","UnitPrice":25,"Quantity":4,"PricePerUnit":6.25},{"OrderID":2,"CustomerName":"Bob","UnitPrice":50,"Quantity":2,"PricePerUnit":25},{"OrderID":3,"CustomerName":"Charlie","UnitPrice":15,"Quantity":10,"PricePerUnit":1.5},{"OrderID":4,"CustomerName":"Alice","UnitPrice":75,"Quantity":1,"PricePerUnit":75},{"OrderID":5,"CustomerName":"Diana","UnitPrice":25,"Quantity":6,"PricePerUnit":4.166666666666667}]}
-->

---
title: "Value.NativeQuery"
slug: "value-nativequery"
category: "Value"
description: "Executes a native query (e.g., SQL) against a data source, bypassing M query folding."
syntax: "Value.NativeQuery(target as any, query as text, optional parameters as any, optional options as nullable record) as any"
returnType: "any"
returnDescription: "The result of executing the native query against the target data source."
parameters:
  - name: "target"
    type: "any"
    required: true
    description: "The data source or connection against which the native query is executed (e.g., a SQL database connection or table)."
  - name: "query"
    type: "text"
    required: true
    description: "The native query string (e.g., a SQL SELECT statement)."
  - name: "parameters"
    type: "any"
    required: false
    description: "An optional record or list of parameter values for parameterized queries."
  - name: "options"
    type: "record"
    required: false
    description: "An optional record of additional options, such as [EnableFolding = true] to allow query folding on top of the native query result."
compatibility:
  pbiDesktop: true
  pbiService: true
  excelDesktop: true
  excelOnline: false
  dataflows: true
  fabricNotebooks: true
relatedFunctions:
  - "value-type"
  - "value-optimize"
---

## Remarks

`Value.NativeQuery` sends a native query string (typically SQL) directly to a data source, bypassing M's query folding pipeline. Use it when:

- You need database-specific syntax that M cannot express or fold (e.g., stored procedure calls, window functions, CTEs with specific hints)
- You want precise control over the exact query plan executed at the source
- Performance requires a hand-tuned query that M's generated SQL would not produce

The `target` is typically a database connection object (such as `Sql.Database("server", "db")`) or a table from within that connection.

**Security warning — SQL injection risk**: Never construct the `query` string by concatenating user-supplied input. Use the `parameters` argument to pass values safely. Parameterized queries are supported on most connectors and are the correct way to incorporate dynamic values.

By default, M cannot fold further operations on top of a native query result — filtering, sorting, or joining in M will be applied in-memory after retrieving the full result. To allow the engine to fold additional operations on top of the native query, pass `[EnableFolding = true]` in the `options` record (only supported by connectors that implement this capability).

Note that `Value.NativeQuery` is not supported in Excel Online (see compatibility). Also note that the function may require query approval in Power BI Desktop depending on data source settings.

## Examples

### Example 1: Execute a SQL query against a SQL Server database

```powerquery
let
    Source = Sql.Database("myserver", "AdventureWorks"),
    Result = Value.NativeQuery(
        Source,
        "SELECT TOP 10 ProductID, Name, ListPrice FROM Production.Product ORDER BY ListPrice DESC"
    )
in
    Result
```

<!--output
{"columns":[{"name":"ProductID","type":"number"},{"name":"Name","type":"text"},{"name":"ListPrice","type":"number"}],"rows":[{"ProductID":749,"Name":"Road-150 Red, 62","ListPrice":3578.27},{"ProductID":750,"Name":"Road-150 Red, 44","ListPrice":3578.27}]}
-->

<!--steps
{
  "steps": [
    {
      "name": "Source",
      "description": "Establishes a connection to the AdventureWorks database on the specified SQL Server.",
      "output": {"columns":[{"name":"Value","type":"text"}],"rows":[{"Value":"[SQL Server connection: myserver/AdventureWorks]"}]}
    },
    {
      "name": "Result",
      "description": "The final output — executes the native SQL query against the database and returns the top 10 products ordered by ListPrice descending.",
      "output": {"columns":[{"name":"ProductID","type":"number"},{"name":"Name","type":"text"},{"name":"ListPrice","type":"number"}],"rows":[{"ProductID":749,"Name":"Road-150 Red, 62","ListPrice":3578.27},{"ProductID":750,"Name":"Road-150 Red, 44","ListPrice":3578.27}]}
    }
  ]
}
-->

### Example 2: Parameterized query — safe way to include dynamic values

```powerquery
let
    Source = Sql.Database("myserver", "AdventureWorks"),
    MinPrice = 1000,
    Result = Value.NativeQuery(
        Source,
        "SELECT ProductID, Name, ListPrice FROM Production.Product WHERE ListPrice >= @MinPrice",
        [MinPrice = MinPrice]
    )
in
    Result
```

<!--output
{"columns":[{"name":"ProductID","type":"number"},{"name":"Name","type":"text"},{"name":"ListPrice","type":"number"}],"rows":[{"ProductID":749,"Name":"Road-150 Red, 62","ListPrice":3578.27},{"ProductID":750,"Name":"Road-150 Red, 44","ListPrice":3578.27}]}
-->

<!--steps
{
  "steps": [
    {
      "name": "Source",
      "description": "Establishes a connection to the AdventureWorks database on the specified SQL Server.",
      "output": {"columns":[{"name":"Value","type":"text"}],"rows":[{"Value":"[SQL Server connection: myserver/AdventureWorks]"}]}
    },
    {
      "name": "MinPrice",
      "description": "Defines the minimum price threshold as a number value to be passed safely as a query parameter.",
      "output": {"columns":[{"name":"Value","type":"number"}],"rows":[{"Value":1000}]}
    },
    {
      "name": "Result",
      "description": "The final output — executes the parameterized SQL query with @MinPrice bound to 1000, returning products with ListPrice at or above that threshold.",
      "output": {"columns":[{"name":"ProductID","type":"number"},{"name":"Name","type":"text"},{"name":"ListPrice","type":"number"}],"rows":[{"ProductID":749,"Name":"Road-150 Red, 62","ListPrice":3578.27},{"ProductID":750,"Name":"Road-150 Red, 44","ListPrice":3578.27}]}
    }
  ]
}
-->

### Example 3: Enable folding on the native query result

Pass `[EnableFolding = true]` so M can push additional operations (like filters) to the source:

```powerquery
let
    Source = Sql.Database("myserver", "AdventureWorks"),
    NativeResult = Value.NativeQuery(
        Source,
        "SELECT ProductID, Name, ListPrice FROM Production.Product",
        null,
        [EnableFolding = true]
    ),
    Filtered = Table.SelectRows(NativeResult, each [ListPrice] > 500)
in
    Filtered
```

<!--output
{"columns":[{"name":"ProductID","type":"number"},{"name":"Name","type":"text"},{"name":"ListPrice","type":"number"}],"rows":[{"ProductID":749,"Name":"Road-150 Red, 62","ListPrice":3578.27},{"ProductID":750,"Name":"Road-150 Red, 44","ListPrice":3578.27}]}
-->

<!--steps
{
  "steps": [
    {
      "name": "Source",
      "description": "Establishes a connection to the AdventureWorks database on the specified SQL Server.",
      "output": {"columns":[{"name":"Value","type":"text"}],"rows":[{"Value":"[SQL Server connection: myserver/AdventureWorks]"}]}
    },
    {
      "name": "NativeResult",
      "description": "Executes the native SQL query to retrieve all products with EnableFolding = true, allowing M to push additional operations back to the database.",
      "output": {"columns":[{"name":"ProductID","type":"number"},{"name":"Name","type":"text"},{"name":"ListPrice","type":"number"}],"rows":[{"ProductID":749,"Name":"Road-150 Red, 62","ListPrice":3578.27},{"ProductID":750,"Name":"Road-150 Red, 44","ListPrice":3578.27}]}
    },
    {
      "name": "Filtered",
      "description": "The final output — applies a row filter in M to keep only products with ListPrice above 500; because folding is enabled, this filter may be pushed to the SQL source.",
      "output": {"columns":[{"name":"ProductID","type":"number"},{"name":"Name","type":"text"},{"name":"ListPrice","type":"number"}],"rows":[{"ProductID":749,"Name":"Road-150 Red, 62","ListPrice":3578.27},{"ProductID":750,"Name":"Road-150 Red, 44","ListPrice":3578.27}]}
    }
  ]
}
-->

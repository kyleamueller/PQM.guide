---
title: "List.Covariance"
slug: "list-covariance"
category: "List"
description: "Returns the sample covariance of two lists of numbers."
syntax: "List.Covariance(numberList1 as list, numberList2 as list) as nullable number"
returnType: "nullable number"
returnDescription: "The sample covariance between the two lists, or null if the lists are empty."
parameters:
  - name: "numberList1"
    type: "list"
    required: true
    description: "The first list of numbers."
  - name: "numberList2"
    type: "list"
    required: true
    description: "The second list of numbers. Must have the same length as numberList1."
compatibility:
  pbiDesktop: true
  pbiService: true
  excelDesktop: true
  excelOnline: true
  dataflows: true
  fabricNotebooks: true
relatedFunctions:
  - "list-average"
  - "list-standarddeviation"
  - "list-sum"
  - "list-count"
---

## Remarks

`List.Covariance` computes the **sample covariance** between two numeric lists. Covariance measures how much two variables change together: a positive value means they tend to increase together, while a negative value means one tends to increase as the other decreases. A covariance near zero suggests little linear relationship.

The formula is the sample covariance: the sum of `(x - mean_x) * (y - mean_y)` divided by `n - 1` (Bessel's correction), where `n` is the number of paired observations. This is appropriate when the lists represent a sample from a larger population.

Both lists must have the same number of elements; mismatched lengths will cause an error. Returns `null` if either list is empty. Null values in the lists cause errors — clean the data with `List.RemoveNulls` first if needed. To normalize covariance into a more interpretable [-1, 1] scale, divide by the product of the two standard deviations to get the Pearson correlation coefficient.

## Examples

### Example 1: Positive covariance — two variables move together

```powerquery
List.Covariance({2, 4, 6, 8}, {1, 3, 5, 7})
```

<!--output
{"columns":[{"name":"Result","type":"number"}],"rows":[{"Result":5.333333333}]}
-->

### Example 2: Negative covariance — price increases as volume decreases

Check whether price increases correlate with volume decreases in a product line.

```powerquery
let
    Prices = {10, 20, 30, 40, 50},
    Volumes = {500, 420, 350, 260, 180},
    Cov = List.Covariance(Prices, Volumes)
in
    #table({"Covariance"}, {{Cov}})
```

<!--output
{"columns":[{"name":"Covariance","type":"number"}],"rows":[{"Covariance":-2050}]}
-->

<!--steps
{
  "steps": [
    {
      "name": "Prices",
      "description": "Defines a list of five price points increasing from 10 to 50.",
      "output": {"columns":[{"name":"Value","type":"number"}],"rows":[{"Value":10},{"Value":20},{"Value":30},{"Value":40},{"Value":50}]}
    },
    {
      "name": "Volumes",
      "description": "Defines a corresponding list of sales volumes that decrease as price increases.",
      "output": {"columns":[{"name":"Value","type":"number"}],"rows":[{"Value":500},{"Value":420},{"Value":350},{"Value":260},{"Value":180}]}
    },
    {
      "name": "Cov",
      "description": "Computes the sample covariance between Prices and Volumes; the large negative value confirms that volume falls as price rises.",
      "output": {"columns":[{"name":"Value","type":"number"}],"rows":[{"Value":-2050}]}
    },
    {
      "name": "Result",
      "description": "The final output — presents the covariance value in a single-row table labeled Covariance.",
      "output": {"columns":[{"name":"Covariance","type":"number"}],"rows":[{"Covariance":-2050}]}
    }
  ]
}
-->

### Example 3: Inversely related lists

When one list increases while the other decreases at the same rate, the covariance is negative.

```powerquery
List.Covariance({1, 2, 3, 4}, {4, 3, 2, 1})
```

<!--output
{"columns":[{"name":"Result","type":"number"}],"rows":[{"Result":-1.666666667}]}
-->

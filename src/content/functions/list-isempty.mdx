---
title: "List.IsEmpty"
slug: "list-isempty"
category: "List"
description: "Returns true if the list contains no elements."
syntax: "List.IsEmpty(list as list) as logical"
returnType: "logical"
returnDescription: "True if the list has zero elements; false otherwise."
parameters:
  - name: "list"
    type: "list"
    required: true
    description: "The list to check."
compatibility:
  pbiDesktop: true
  pbiService: true
  excelDesktop: true
  excelOnline: true
  dataflows: true
  fabricNotebooks: true
relatedFunctions:
  - "list-count"
  - "list-isdistinct"
  - "list-first"
  - "list-select"
---

## Remarks

`List.IsEmpty` returns `true` if the list has zero items. It is equivalent to `List.Count(list) = 0` but expresses intent more clearly. The primary use case is defensive branching: before calling `List.First`, `List.Single`, or any aggregation function on a filtered list, check `List.IsEmpty` to avoid errors when no results exist.

A list containing only null values is **not** empty — it has elements, they are simply null. An empty list is one with no elements at all: `{}`.

Compared to `List.Count`, `List.IsEmpty` does not need to count all elements — it only needs to check for the existence of any element, which can be more efficient on large lazy lists. For the inverse check, use `not List.IsEmpty(list)` rather than comparing count to a number.

## Examples

### Example 1: Empty list

```powerquery
List.IsEmpty({})
```

<!--output
{"columns":[{"name":"Result","type":"logical"}],"rows":[{"Result":"true"}]}
-->

### Example 2: Non-empty list

```powerquery
List.IsEmpty({1, 2, 3})
```

<!--output
{"columns":[{"name":"Result","type":"logical"}],"rows":[{"Result":"false"}]}
-->

### Example 3: Check if a filter returned results before accessing

```powerquery
let
    Values = {10, 20, 30},
    Filtered = List.Select(Values, each _ > 50),
    Result = if List.IsEmpty(Filtered) then null else List.First(Filtered)
in
    Result
```

<!--output
{"columns":[{"name":"Result","type":"any"}],"rows":[{"Result":"null"}]}
-->

<!--steps
{
  "steps": [
    {
      "name": "Values",
      "description": "Defines a list of numbers to filter.",
      "output": {"columns":[{"name":"Value","type":"number"}],"rows":[{"Value":10},{"Value":20},{"Value":30}]}
    },
    {
      "name": "Filtered",
      "description": "Filters the Values list to keep only items greater than 50, producing an empty list because no items qualify.",
      "output": {"columns":[{"name":"Value","type":"number"}],"rows":[]}
    },
    {
      "name": "Result",
      "description": "The final output — returns null because the filtered list is empty, using List.IsEmpty to safely guard against calling List.First on an empty list.",
      "output": {"columns":[{"name":"Result","type":"any"}],"rows":[{"Result":"null"}]}
    }
  ]
}
-->

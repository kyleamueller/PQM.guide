---
title: "Value.ReplaceMetadata"
slug: "value-replacemetadata"
category: "Value"
description: "Returns a copy of a value with its metadata replaced by the given metadata record."
syntax: "Value.ReplaceMetadata(value as any, metadataRecord as record) as any"
returnType: "any"
returnDescription: "The same value with its metadata replaced by metadataRecord."
parameters:
  - name: "value"
    type: "any"
    required: true
    description: "The value whose metadata is replaced."
  - name: "metadataRecord"
    type: "record"
    required: true
    description: "The new metadata record to attach to the value."
compatibility:
  pbiDesktop: true
  pbiService: true
  excelDesktop: true
  excelOnline: true
  dataflows: true
  fabricNotebooks: true
relatedFunctions:
  - "value-metadata"
  - "value-removemetadata"
  - "value-type"
---

## Remarks

`Value.ReplaceMetadata` attaches a new metadata record to a value, **replacing any existing metadata entirely**. It is the function form of the `value meta record` M expression — `Value.ReplaceMetadata(v, r)` is equivalent to `v meta r`.

Metadata is supplementary information carried alongside a value without changing the value itself. The underlying data, type kind, and equality semantics are all unaffected — `Value.ReplaceMetadata(42, [Tag = "x"])` is still equal to `42`.

Important behaviors:

- **Full replacement**: `Value.ReplaceMetadata` discards all previous metadata and replaces it with the provided record. It does not merge. To add fields to existing metadata without losing others, use the pattern: `Value.ReplaceMetadata(v, Record.Combine({Value.Metadata(v), [NewField = "value"]}))`.
- **Lost on operations**: most M operations (arithmetic, string operations, table transforms) strip metadata from their outputs. Metadata must be explicitly re-attached if needed after a transformation.
- **Engine use**: Power Query uses metadata internally to carry display format hints, column descriptions (`Documentation.FieldCaption`), and data source provenance information.

## Examples

### Example 1: Attach a metadata record to a number

```powerquery
let
    Annotated = Value.ReplaceMetadata(42, [Source = "Calculated", Verified = true]),
    Meta = Value.Metadata(Annotated)
in
    Meta[Source]
```

<!--output
{"columns":[{"name":"Result","type":"text"}],"rows":[{"Result":"Calculated"}]}
-->

<!--steps
{
  "steps": [
    {
      "name": "Annotated",
      "description": "Attaches a metadata record with Source = \"Calculated\" and Verified = true to the number 42 using Value.ReplaceMetadata.",
      "output": {"columns":[{"name":"Value","type":"number"}],"rows":[{"Value":42}]}
    },
    {
      "name": "Meta",
      "description": "Retrieves the metadata record from the annotated value, returning a record with Source and Verified fields.",
      "output": {"columns":[{"name":"Field","type":"text"},{"name":"Value","type":"any"}],"rows":[{"Field":"Source","Value":"Calculated"},{"Field":"Verified","Value":true}]}
    },
    {
      "name": "Result",
      "description": "The final output — accesses the Source field from the metadata record, returning \"Calculated\".",
      "output": {"columns":[{"name":"Result","type":"text"}],"rows":[{"Result":"Calculated"}]}
    }
  ]
}
-->

### Example 2: Replace existing metadata — old fields are discarded

```powerquery
let
    Original = "hello" meta [Tag = "old"],
    Updated = Value.ReplaceMetadata(Original, [Tag = "new"]),
    Meta = Value.Metadata(Updated)
in
    Meta[Tag]
```

<!--output
{"columns":[{"name":"Result","type":"text"}],"rows":[{"Result":"new"}]}
-->

<!--steps
{
  "steps": [
    {
      "name": "Original",
      "description": "Creates the text value \"hello\" with a metadata record containing Tag = \"old\" attached via the meta keyword.",
      "output": {"columns":[{"name":"Value","type":"text"}],"rows":[{"Value":"hello"}]}
    },
    {
      "name": "Updated",
      "description": "Replaces the entire metadata record on \"hello\" with a new record containing Tag = \"new\" — the old Tag = \"old\" is discarded.",
      "output": {"columns":[{"name":"Value","type":"text"}],"rows":[{"Value":"hello"}]}
    },
    {
      "name": "Meta",
      "description": "Retrieves the metadata record from the updated value, returning the new metadata with Tag = \"new\".",
      "output": {"columns":[{"name":"Field","type":"text"},{"name":"Value","type":"text"}],"rows":[{"Field":"Tag","Value":"new"}]}
    },
    {
      "name": "Result",
      "description": "The final output — accesses the Tag field from the new metadata record, returning \"new\" to confirm the replacement.",
      "output": {"columns":[{"name":"Result","type":"text"}],"rows":[{"Result":"new"}]}
    }
  ]
}
-->

### Example 3: The underlying value is unchanged by metadata replacement

```powerquery
let
    Annotated = Value.ReplaceMetadata(100, [Note = "special"])
in
    Annotated + 5
```

<!--output
{"columns":[{"name":"Result","type":"number"}],"rows":[{"Result":105}]}
-->

---
title: "List.Difference"
slug: "list-difference"
category: "List"
description: "Returns items from the first list that do not appear in the second list."
syntax: "List.Difference(list1 as list, list2 as list, optional equationCriteria as any) as list"
returnType: "list"
returnDescription: "A list of items present in list1 but not in list2."
parameters:
  - name: "list1"
    type: "list"
    required: true
    description: "The source list to filter."
  - name: "list2"
    type: "list"
    required: true
    description: "The list of values to exclude from list1."
  - name: "equationCriteria"
    type: "any"
    required: false
    description: "An optional equation criteria value to control comparison. Use a comparer function for custom equality logic."
compatibility:
  pbiDesktop: true
  pbiService: true
  excelDesktop: true
  excelOnline: true
  dataflows: true
  fabricNotebooks: true
relatedFunctions:
  - "list-intersect"
  - "list-union"
  - "list-removeitems"
  - "list-removematchingitems"
  - "list-distinct"
---

## Remarks

`List.Difference` performs a set-difference operation: it returns all items from `list1` that do not appear in `list2`. The result preserves the order of items from `list1`. Duplicate values in `list1` that are not in `list2` are kept; duplicates that match something in `list2` are all removed.

This is the right tool for finding what was added or removed between two snapshots — for example, which product codes appear in last month's inventory but not this month's, or which users were revoked access between two permission exports.

Comparison is case-sensitive for text by default. Pass `Comparer.OrdinalIgnoreCase` as the `equationCriteria` argument for case-insensitive matching. Note that `List.Difference` is a set-semantic operation — it deduplicates the exclusion set in `list2` but not in `list1`. If you need to remove individual matched occurrences one-for-one, use `List.RemoveItems` instead.

## Examples

### Example 1: Find items in list1 not in list2

```powerquery
List.Difference({1, 2, 3, 4, 5}, {2, 4})
```

<!--output
{"columns":[{"name":"Result","type":"any"}],"rows":[{"Result":"{1, 3, 5}"}]}
-->

### Example 2: Find discontinued products

```powerquery
let
    LastMonth = {"Widget A", "Widget B", "Widget C", "Widget D"},
    ThisMonth = {"Widget A", "Widget C"},
    Discontinued = List.Difference(LastMonth, ThisMonth)
in
    Discontinued
```

<!--output
{"columns":[{"name":"Result","type":"any"}],"rows":[{"Result":"{Widget B, Widget D}"}]}
-->

<!--steps
{
  "steps": [
    {
      "name": "LastMonth",
      "description": "Defines a list of product names that were available last month.",
      "output": {"columns":[{"name":"Value","type":"text"}],"rows":[{"Value":"Widget A"},{"Value":"Widget B"},{"Value":"Widget C"},{"Value":"Widget D"}]}
    },
    {
      "name": "ThisMonth",
      "description": "Defines a list of product names that are available this month.",
      "output": {"columns":[{"name":"Value","type":"text"}],"rows":[{"Value":"Widget A"},{"Value":"Widget C"}]}
    },
    {
      "name": "Discontinued",
      "description": "The final output — uses List.Difference to return products from last month that do not appear this month, identifying discontinued items.",
      "output": {"columns":[{"name":"Result","type":"any"}],"rows":[{"Result":"{Widget B, Widget D}"}]}
    }
  ]
}
-->

### Example 3: Case-insensitive difference

```powerquery
List.Difference({"Apple", "Banana", "Cherry"}, {"apple", "cherry"}, Comparer.OrdinalIgnoreCase)
```

<!--output
{"columns":[{"name":"Result","type":"any"}],"rows":[{"Result":"{Banana}"}]}
-->

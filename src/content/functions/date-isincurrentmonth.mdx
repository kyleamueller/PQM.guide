---
title: "Date.IsInCurrentMonth"
slug: "date-isincurrentmonth"
category: "Date"
description: "Returns true if the date falls within the current calendar month."
syntax: "Date.IsInCurrentMonth(dateTime as any) as logical"
returnType: "logical"
returnDescription: "true if the date is in the current month, false otherwise."
parameters:
  - name: "dateTime"
    type: "any"
    required: true
    description: "A date, datetime, or datetimezone value to test."
compatibility:
  pbiDesktop: true
  pbiService: true
  excelDesktop: true
  excelOnline: true
  dataflows: true
  fabricNotebooks: true
relatedFunctions:
  - "date-isincurrentday"
  - "date-isincurrentquarter"
  - "date-isincurrentyear"
  - "date-isinpreviousmonth"
  - "date-isinnextmonth"
---

## Remarks

`Date.IsInCurrentMonth` returns `true` if the input date falls within the same year and month as today's date (`DateTime.LocalNow()`). It matches the entire calendar month from the first to the last day, inclusive — including future dates within the current month. This function is re-evaluated on each query refresh.

Note that this function includes future dates in the current month. If you need only dates up to and including today, use `Date.IsInYearToDate` restricted to the current month, or add `and [OrderDate] <= Date.From(DateTime.LocalNow())` to the filter.

In Power BI Service scheduled refreshes and cloud Dataflows, the "local" time is the server's timezone (usually UTC). If your data dates are in a different timezone, the month boundary may not align with what you expect. This is the same caveat that applies to all `Date.IsIn*` functions.

## Examples

### Example 1: Filter this month's orders

Keep only rows where OrderDate falls in the current calendar month.

```powerquery
Table.SelectRows(
    Sales,
    each Date.IsInCurrentMonth([OrderDate])
)
```

<!--output
{"columns":[{"name":"OrderID","type":"number"},{"name":"OrderDate","type":"date"}],"rows":[{"OrderID":40,"OrderDate":"2026-03-01"},{"OrderID":41,"OrderDate":"2026-03-07"},{"OrderID":42,"OrderDate":"2026-03-08"}]}
-->

### Example 2: Flag current-month rows

Add a boolean column identifying rows in the current month.

```powerquery
Table.AddColumn(
    Sales,
    "IsCurrentMonth", each Date.IsInCurrentMonth([OrderDate]), type logical
)
```

<!--output
{"columns":[{"name":"OrderID","type":"number"},{"name":"OrderDate","type":"date"},{"name":"IsCurrentMonth","type":"logical"}],"rows":[{"OrderID":38,"OrderDate":"2026-02-20","IsCurrentMonth":false},{"OrderID":40,"OrderDate":"2026-03-01","IsCurrentMonth":true},{"OrderID":42,"OrderDate":"2026-03-08","IsCurrentMonth":true}]}
-->

### Example 3: Filter month-to-date orders only (excluding future dates this month)

Restrict current month results to dates up to and including today.

```powerquery
let
    Today = Date.From(DateTime.LocalNow())
in
    Table.SelectRows(
        Sales,
        each Date.IsInCurrentMonth([OrderDate]) and [OrderDate] <= Today
    )
```

<!--output
{"columns":[{"name":"OrderID","type":"number"},{"name":"OrderDate","type":"date"}],"rows":[{"OrderID":40,"OrderDate":"2026-03-01"},{"OrderID":41,"OrderDate":"2026-03-07"},{"OrderID":42,"OrderDate":"2026-03-08"}]}
-->

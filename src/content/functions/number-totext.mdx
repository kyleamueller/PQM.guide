---
title: "Number.ToText"
slug: "number-totext"
category: "Number"
description: "Converts a number to a text string, with optional format specifier and culture for locale-aware formatting."
syntax: "Number.ToText(number as nullable number, optional format as nullable text, optional culture as nullable text) as nullable text"
returnType: "text"
returnDescription: "A text representation of the number, formatted according to the optional format and culture."
parameters:
  - name: "number"
    type: "number"
    required: true
    description: "The number to convert to text."
  - name: "format"
    type: "text"
    required: false
    description: "An optional .NET format specifier such as \"D\" (decimal integer), \"F2\" (fixed 2 decimal places), \"P\" (percentage), \"E\" (scientific), \"C\" (currency), or \"G\" (general)."
  - name: "culture"
    type: "text"
    required: false
    description: "An optional culture string (e.g., \"en-US\", \"de-DE\") that controls locale-specific formatting such as decimal and thousand separators."
compatibility:
  pbiDesktop: true
  pbiService: true
  excelDesktop: true
  excelOnline: true
  dataflows: true
  fabricNotebooks: true
relatedFunctions:
  - "number-fromtext"
  - "number-round"
---

## Remarks

`Number.ToText` converts a number to a text string with optional format and culture control. When called with no format argument, it returns a compact general representation (similar to `"G"`). The format parameter follows **.NET standard numeric format specifiers** with an optional precision digit:

| Format | Name | Example |
|--------|------|---------|
| `"G"` | General | `"3.14159"` |
| `"F"` / `"F2"` | Fixed-point | `"3.14"` |
| `"N"` / `"N2"` | Number with thousands separators | `"1,234.56"` |
| `"P"` / `"P1"` | Percentage | `"87.5%"` |
| `"E"` / `"E2"` | Scientific notation | `"3.14E+002"` |
| `"C"` / `"C2"` | Currency (symbol depends on culture) | `"$3.14"` |
| `"D"` | Decimal integer (no fractions) | `"42"` |

The `culture` parameter controls locale-specific output: `"de-DE"` produces `"1.234,56"` for `"N2"` format, while `"en-US"` produces `"1,234.56"`. Always specify the culture explicitly for user-facing output that must be locale-consistent.

Note that `Number.ToText` operates only on numbers. To convert other types to text, use `Text.From`.

## Examples

### Example 1: Format to 2 fixed decimal places

```powerquery
Number.ToText(3.14159, "F2")
```

<!--output
{"columns":[{"name":"Result","type":"text"}],"rows":[{"Result":"3.14"}]}
-->

### Example 2: Format a ratio as a percentage with 1 decimal place

```powerquery
Number.ToText(0.8753, "P1")
```

<!--output
{"columns":[{"name":"Result","type":"text"}],"rows":[{"Result":"87.5%"}]}
-->

### Example 3: Format unit prices with thousands separators and 2 decimal places

Input: `Sales`

```powerquery
Table.AddColumn(
    Table.SelectColumns(Table.FirstN(Sales, 4), {"OrderID", "CustomerName", "UnitPrice"}),
    "DisplayPrice",
    each Number.ToText([UnitPrice], "N2", "en-US"),
    type text
)
```

<!--output
{"columns":[{"name":"OrderID","type":"number"},{"name":"CustomerName","type":"text"},{"name":"UnitPrice","type":"number"},{"name":"DisplayPrice","type":"text"}],"rows":[{"OrderID":1,"CustomerName":"Alice","UnitPrice":25,"DisplayPrice":"25.00"},{"OrderID":2,"CustomerName":"Bob","UnitPrice":50,"DisplayPrice":"50.00"},{"OrderID":3,"CustomerName":"Charlie","UnitPrice":15,"DisplayPrice":"15.00"},{"OrderID":4,"CustomerName":"Alice","UnitPrice":75,"DisplayPrice":"75.00"}]}
-->

---
title: "Date.ToRecord"
slug: "date-torecord"
category: "Date"
description: "Returns a record containing the Year, Month, and Day fields of a date value."
syntax: "Date.ToRecord(date as date) as record"
returnType: "record"
returnDescription: "A record with fields Year, Month, and Day as numbers."
parameters:
  - name: "date"
    type: "date"
    required: true
    description: "A date value to decompose into its components."
compatibility:
  pbiDesktop: true
  pbiService: true
  excelDesktop: true
  excelOnline: true
  dataflows: true
  fabricNotebooks: true
relatedFunctions:
  - "date-year"
  - "date-month"
  - "date-day"
  - "date-fromtext"
  - "datetime-torecord"
---

## Remarks

`Date.ToRecord` decomposes a `date` value into a record with three numeric fields: `Year`, `Month`, and `Day`. This is useful when you need all three components at once and want to avoid calling `Date.Year`, `Date.Month`, and `Date.Day` separately in three separate columns. You can expand the record into separate table columns using `Table.ExpandRecordColumn`.

Note that `Date.ToRecord` requires a plain `date` input — it does not accept `datetime` or `datetimezone` values. For those types, use `DateTime.ToRecord` or `DateTimeZone.ToRecord`. The field names in the returned record are always `Year`, `Month`, and `Day` (capitalized), which you reference when expanding the record.

A common alternative to `Date.ToRecord` is simply calling the individual accessor functions `Date.Year`, `Date.Month`, and `Date.Day` directly in separate `Table.AddColumn` calls. Use `Date.ToRecord` when all three columns are needed at once and a single expansion step is preferable for readability.

## Examples

### Example 1: Expand date components into separate Year, Month, Day columns

Decompose each OrderDate into three separate numeric columns.

Input: `Sales`

```powerquery
Table.ExpandRecordColumn(
    Table.AddColumn(
        Table.SelectColumns(
            Table.FirstN(Sales, 4),
            {"OrderID", "OrderDate"}
        ),
        "DateParts", each Date.ToRecord([OrderDate])
    ),
    "DateParts",
    {"Year", "Month", "Day"}
)
```

<!--output
{"columns":[{"name":"OrderID","type":"number"},{"name":"OrderDate","type":"date"},{"name":"Year","type":"number"},{"name":"Month","type":"number"},{"name":"Day","type":"number"}],"rows":[{"OrderID":1,"OrderDate":"2024-01-15","Year":2024,"Month":1,"Day":15},{"OrderID":2,"OrderDate":"2024-01-18","Year":2024,"Month":1,"Day":18},{"OrderID":3,"OrderDate":"2024-02-01","Year":2024,"Month":2,"Day":1},{"OrderID":4,"OrderDate":"2024-02-10","Year":2024,"Month":2,"Day":10}]}
-->

### Example 2: Decompose a specific date into a record

Return the component record for March 15, 2024.

```powerquery
Date.ToRecord(#date(2024, 3, 15))
```

<!--output
{"columns":[{"name":"Field","type":"text"},{"name":"Value","type":"number"}],"rows":[{"Field":"Year","Value":2024},{"Field":"Month","Value":3},{"Field":"Day","Value":15}]}
-->

### Example 3: Use record fields directly without expansion

Access individual fields from the record inline without expanding into columns.

Input: `Sales`

```powerquery
Table.AddColumn(
    Table.SelectColumns(Table.FirstN(Sales, 4), {"OrderID", "OrderDate"}),
    "YearMonth",
    each
        let r = Date.ToRecord([OrderDate])
        in Text.From(r[Year]) & "-" & Text.PadStart(Text.From(r[Month]), 2, "0"),
    type text
)
```

<!--output
{"columns":[{"name":"OrderID","type":"number"},{"name":"OrderDate","type":"date"},{"name":"YearMonth","type":"text"}],"rows":[{"OrderID":1,"OrderDate":"2024-01-15","YearMonth":"2024-01"},{"OrderID":2,"OrderDate":"2024-01-18","YearMonth":"2024-01"},{"OrderID":3,"OrderDate":"2024-02-01","YearMonth":"2024-02"},{"OrderID":4,"OrderDate":"2024-02-10","YearMonth":"2024-02"}]}
-->

---
title: "Date.DayOfYear"
slug: "date-dayofyear"
category: "Date"
description: "Returns the day of the year (1–366) for the given date."
syntax: "Date.DayOfYear(dateTime as any) as nullable number"
returnType: "number"
returnDescription: "An integer from 1 to 366 representing the day of the year."
parameters:
  - name: "dateTime"
    type: "any"
    required: true
    description: "A date, datetime, or datetimezone value."
compatibility:
  pbiDesktop: true
  pbiService: true
  excelDesktop: true
  excelOnline: true
  dataflows: true
  fabricNotebooks: true
relatedFunctions:
  - "date-dayofweek"
  - "date-weekofyear"
  - "date-isleapyear"
---

## Remarks

`Date.DayOfYear` returns an integer from 1 (January 1) to 365 or 366 (December 31, depending on leap year) for a `date`, `datetime`, or `datetimezone` value. This is useful for cross-year comparisons where you want to compare progress through the year without regard to the specific year — for example, to find the same-day-of-year sales across multiple years. If the input is `null`, the function returns `null`.

Note that the maximum value is 365 in a common year and 366 in a leap year. When comparing day-of-year values across years, be aware that day 366 only exists in leap years. For these same-period comparisons, also consider using `Date.Month` + `Date.Day` directly to avoid the leap year edge case.

`Date.DayOfYear` is a simple ordinal counter with no locale or timezone sensitivity. It is not related to ISO week numbering; for ISO week numbers use `Date.WeekOfYear` with `Day.Monday`.

## Examples

### Example 1: Add day-of-year number to Sales table

Add a column showing the ordinal day of the year for each order date.

Input: `Sales`

```powerquery
Table.AddColumn(
    Table.SelectColumns(
        Table.FirstN(Sales, 5),
        {"OrderID", "OrderDate"}
    ),
    "DayOfYear", each Date.DayOfYear([OrderDate]), Int64.Type
)
```

<!--output
{"columns":[{"name":"OrderID","type":"number"},{"name":"OrderDate","type":"date"},{"name":"DayOfYear","type":"number"}],"rows":[{"OrderID":1,"OrderDate":"2024-01-15","DayOfYear":15},{"OrderID":2,"OrderDate":"2024-01-18","DayOfYear":18},{"OrderID":3,"OrderDate":"2024-02-01","DayOfYear":32},{"OrderID":4,"OrderDate":"2024-02-10","DayOfYear":41},{"OrderID":5,"OrderDate":"2024-03-05","DayOfYear":65}]}
-->

### Example 2: Day of year for a specific date

Return the day-of-year number for March 15, 2024.

```powerquery
#table(
    type table [Date = date, DayOfYear = number],
    {{#date(2024, 3, 15), Date.DayOfYear(#date(2024, 3, 15))}}
)
```

<!--output
{"columns":[{"name":"Date","type":"date"},{"name":"DayOfYear","type":"number"}],"rows":[{"Date":"2024-03-15","DayOfYear":75}]}
-->

### Example 3: Filter rows to same day-of-year across all years

Find orders that fall on the same day-of-year as March 5 (day 65) regardless of year.

Input: `Sales`

```powerquery
Table.SelectRows(
    Table.AddColumn(Sales, "DayOfYear", each Date.DayOfYear([OrderDate]), Int64.Type),
    each [DayOfYear] = 65
)
```

<!--output
{"columns":[{"name":"OrderID","type":"number"},{"name":"CustomerName","type":"text"},{"name":"Product","type":"text"},{"name":"Category","type":"text"},{"name":"UnitPrice","type":"number"},{"name":"Quantity","type":"number"},{"name":"OrderDate","type":"date"},{"name":"Region","type":"text"},{"name":"DayOfYear","type":"number"}],"rows":[{"OrderID":5,"CustomerName":"Diana","Product":"Widget A","Category":"Widgets","UnitPrice":25,"Quantity":6,"OrderDate":"2024-03-05","Region":"West","DayOfYear":65}]}
-->

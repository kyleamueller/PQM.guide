---
title: "Number.BitwiseOr"
slug: "number-bitwiseor"
category: "Number"
description: "Performs a bitwise OR operation on two integer values."
syntax: "Number.BitwiseOr(number1 as number, number2 as number) as number"
returnType: "number"
returnDescription: "An integer where each bit is 1 if the corresponding bit is 1 in either input."
parameters:
  - name: "number1"
    type: "number"
    required: true
    description: "The first integer operand."
  - name: "number2"
    type: "number"
    required: true
    description: "The second integer operand."
compatibility:
  pbiDesktop: true
  pbiService: true
  excelDesktop: true
  excelOnline: true
  dataflows: true
  fabricNotebooks: true
relatedFunctions:
  - "number-bitwiseand"
  - "number-bitwisexor"
  - "number-bitwisenot"
  - "number-bitwiseshiftleft"
  - "number-bitwiseshiftright"
---

## Remarks

`Number.BitwiseOr` performs a bitwise OR between two integers. Each bit position in the result is `1` when the corresponding bit is `1` in **at least one** of the operands; it is `0` only when both operands have `0` at that position. Both inputs are treated as integers — fractional parts are truncated.

Bitwise OR is most commonly used to **set bits** (turn specific bits on) or **merge bitmasks**. Unlike AND (which tests/extracts bits), OR ensures that bits are added to a value. A value ORed with a mask will always have at least the mask's bits set.

Common patterns:

- **Combine flags**: `Number.BitwiseOr(flags, 4)` sets bit 2 in `flags` regardless of its current state
- **Merge permission sets**: OR-ing two permission integers gives the union of all permitted capabilities
- **Reconstruct packed values**: building an integer from separate bit-field contributions by OR-ing shifted components

Note that `Number.BitwiseOr(x, x) = x` (idempotent) and `Number.BitwiseOr(x, 0) = x` (OR with 0 is identity).

## Examples

### Example 1: Basic bitwise OR

12 (`1100`) OR 10 (`1010`) = `1110` = 14.

```powerquery
Number.BitwiseOr(12, 10)
```

<!--output
{"columns":[{"name":"Result","type":"number"}],"rows":[{"Result":14}]}
-->

### Example 2: Set bit 0 (value 1) in a number that doesn't have it

12 (`1100`) OR 1 (`0001`) = `1101` = 13.

```powerquery
Number.BitwiseOr(12, 1)
```

<!--output
{"columns":[{"name":"Result","type":"number"}],"rows":[{"Result":13}]}
-->

### Example 3: Combine permission flags (Read=1, Write=2, Execute=4)

```powerquery
let
    Read = 1,
    Write = 2,
    Execute = 4,
    ReadWrite = Number.BitwiseOr(Read, Write),
    AllPerms = Number.BitwiseOr(ReadWrite, Execute)
in
    #table({"ReadWrite", "AllPerms"}, {{ReadWrite, AllPerms}})
```

<!--output
{"columns":[{"name":"ReadWrite","type":"number"},{"name":"AllPerms","type":"number"}],"rows":[{"ReadWrite":3,"AllPerms":7}]}
-->
<!--steps
{
  "steps": [
    {
      "name": "Read",
      "description": "Assigns the integer value 1 to represent the Read permission flag.",
      "output": {"columns":[{"name":"Value","type":"number"}],"rows":[{"Value":1}]}
    },
    {
      "name": "Write",
      "description": "Assigns the integer value 2 to represent the Write permission flag.",
      "output": {"columns":[{"name":"Value","type":"number"}],"rows":[{"Value":2}]}
    },
    {
      "name": "Execute",
      "description": "Assigns the integer value 4 to represent the Execute permission flag.",
      "output": {"columns":[{"name":"Value","type":"number"}],"rows":[{"Value":4}]}
    },
    {
      "name": "ReadWrite",
      "description": "Combines Read (1) and Write (2) permission flags using bitwise OR, producing 3 (binary 011).",
      "output": {"columns":[{"name":"Value","type":"number"}],"rows":[{"Value":3}]}
    },
    {
      "name": "AllPerms",
      "description": "Combines ReadWrite (3) with Execute (4) using bitwise OR, producing 7 (binary 111) — all three permissions set.",
      "output": {"columns":[{"name":"Value","type":"number"}],"rows":[{"Value":7}]}
    },
    {
      "name": "Result",
      "description": "The final output — a single-row table showing ReadWrite = 3 and AllPerms = 7.",
      "output": {"columns":[{"name":"ReadWrite","type":"number"},{"name":"AllPerms","type":"number"}],"rows":[{"ReadWrite":3,"AllPerms":7}]}
    }
  ]
}
-->

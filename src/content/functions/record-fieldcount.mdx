---
title: "Record.FieldCount"
slug: "record-fieldcount"
category: "Record"
description: "Returns the number of fields in a record."
syntax: "Record.FieldCount(record as record) as number"
returnType: "number"
returnDescription: "The number of fields in the record."
parameters:
  - name: "record"
    type: "record"
    required: true
    description: "The record whose field count is returned."
compatibility:
  pbiDesktop: true
  pbiService: true
  excelDesktop: true
  excelOnline: true
  dataflows: true
  fabricNotebooks: true
relatedFunctions:
  - "record-fieldnames"
  - "record-fieldvalues"
  - "record-hasfields"
---

## Remarks

`Record.FieldCount` returns the number of fields in a record. It is equivalent to `List.Count(Record.FieldNames(record))`. The function accepts any record, including table rows accessed with `table{n}`.

Common uses:

- **Schema validation**: confirm a record has the expected number of fields before processing
- **Dynamic introspection**: count fields when the record structure is not known at query design time (e.g., semi-structured JSON data)
- **Conditional logic**: branch based on whether a record has been enriched with additional fields

Note that `Record.FieldCount` does not accept `null` — it raises an error if passed null. Use the null-coalescing operator `??` to handle potentially-null records safely: `Record.FieldCount(record ?? [])`.

## Examples

### Example 1: Count fields in a literal record

```powerquery
Record.FieldCount([Name = "Alice", Age = 30, City = "New York"])
```

<!--output
{"columns":[{"name":"Result","type":"number"}],"rows":[{"Result":3}]}
-->

### Example 2: Count fields in the first row of Sales

Input: `Sales`

```powerquery
Record.FieldCount(Sales{0})
```

<!--output
{"columns":[{"name":"Result","type":"number"}],"rows":[{"Result":8}]}
-->

### Example 3: Add a field count column to a table of records

```powerquery
let
    Data = Table.FromRecords({
        [A=1, B=2, C=3],
        [A=1, B=2, C=3, D=4],
        [A=1, B=2]
    }),
    WithCount = Table.AddColumn(
        Data, "FieldCount", each Record.FieldCount(_), type number
    )
in
    WithCount
```

<!--output
{"columns":[{"name":"A","type":"number"},{"name":"B","type":"number"},{"name":"C","type":"number"},{"name":"D","type":"number"},{"name":"FieldCount","type":"number"}],"rows":[{"A":1,"B":2,"C":3,"D":null,"FieldCount":3},{"A":1,"B":2,"C":3,"D":4,"FieldCount":4},{"A":1,"B":2,"C":null,"D":null,"FieldCount":2}]}
-->

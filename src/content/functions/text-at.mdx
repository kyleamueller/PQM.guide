---
title: "Text.At"
slug: "text-at"
category: "Text"
description: "Returns the character at the specified zero-based index position in a text value."
syntax: "Text.At(text as nullable text, index as number) as nullable text"
returnType: "text"
returnDescription: "A single-character text value at the given index."
parameters:
  - name: "text"
    type: "text"
    required: true
    description: "The source text value."
  - name: "index"
    type: "number"
    required: true
    description: "The zero-based index of the character to return."
compatibility:
  pbiDesktop: true
  pbiService: true
  excelDesktop: true
  excelOnline: true
  dataflows: true
  fabricNotebooks: true
relatedFunctions:
  - "text-start"
  - "text-end"
  - "text-middle"
  - "text-range"
  - "text-positionof"
  - "text-tolist"
---

## Remarks

`Text.At` returns a single character from `text` at the specified zero-based `index`. The first character is at index 0. It is equivalent to `Text.Middle(text, index, 1)` but expresses single-character access more clearly.

If `text` is `null`, the function returns `null`. If `index` is out of range — negative, or equal to or greater than the text length — an error is raised. Always validate the index or text length before calling this function on variable-length data to avoid runtime errors.

Use `Text.At` when you need a specific character by position. For extracting a range of characters, use `Text.Middle` or `Text.Range`. For iterating over every character, `Text.ToList` is more efficient than calling `Text.At` in a loop. To find the position of a character rather than retrieve it, use `Text.PositionOf`.

## Examples

### Example 1: Get the first character of a string

```powerquery
Text.At("Widget A", 0)
```

<!--output
{"columns":[{"name":"Result","type":"text"}],"rows":[{"Result":"W"}]}
-->

### Example 2: Extract the category prefix from a product code

Employee IDs like `"E001"` use a letter prefix — `Text.At` retrieves it by position.

```powerquery
Text.At("E001", 0)
```

<!--output
{"columns":[{"name":"Result","type":"text"}],"rows":[{"Result":"E"}]}
-->

### Example 3: Read the separator character in a formatted string

Validate that the third character of a code string is the expected separator before parsing the rest.

```powerquery
let
    code = "US-12345",
    sep = Text.At(code, 2),
    isValid = sep = "-"
in
    isValid
```

<!--output
{"columns":[{"name":"Result","type":"logical"}],"rows":[{"Result":true}]}
-->

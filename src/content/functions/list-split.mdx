---
title: "List.Split"
slug: "list-split"
category: "List"
description: "Splits a list into a list of sub-lists, each of a specified maximum size."
syntax: "List.Split(list as list, pageSize as number) as list"
returnType: "list"
returnDescription: "A list of lists, where each inner list contains at most pageSize items."
parameters:
  - name: "list"
    type: "list"
    required: true
    description: "The source list to split."
  - name: "pageSize"
    type: "number"
    required: true
    description: "The maximum number of items in each sub-list (chunk size)."
compatibility:
  pbiDesktop: true
  pbiService: true
  excelDesktop: true
  excelOnline: true
  dataflows: true
  fabricNotebooks: true
relatedFunctions:
  - "list-firstn"
  - "list-skip"
  - "list-range"
  - "list-zip"
---

## Remarks

`List.Split` divides a list into consecutive chunks (sub-lists), each containing at most `pageSize` items. If the list length is not evenly divisible by `pageSize`, the last chunk contains the remaining items and will be smaller than `pageSize`. The result is always a **list of lists** — never a flat list.

This function is the standard way to implement batching in Power Query. Common use cases include splitting a large list of IDs to send in multiple API requests, chunking a dataset for incremental processing, or paginating through records. After splitting, use `List.Transform` or `List.Accumulate` to process each batch.

The `pageSize` must be a positive integer; using `0` or a negative number causes an error. If the source list is empty, the result is an empty list. To flatten a list of lists back into a single list, use `List.Combine`.

## Examples

### Example 1: Split into chunks of 3

```powerquery
List.Split({1, 2, 3, 4, 5, 6, 7, 8, 9}, 3)
```

<!--output
{"columns":[{"name":"Result","type":"any"}],"rows":[{"Result":"{{1, 2, 3}, {4, 5, 6}, {7, 8, 9}}"}]}
-->

### Example 2: Last chunk may be smaller

```powerquery
List.Split({1, 2, 3, 4, 5}, 2)
```

<!--output
{"columns":[{"name":"Result","type":"any"}],"rows":[{"Result":"{{1, 2}, {3, 4}, {5}}"}]}
-->

### Example 3: Process a list in batches of 100

```powerquery
let
    AllIDs = List.Numbers(1, 250),
    Batches = List.Split(AllIDs, 100),
    BatchCount = List.Count(Batches)
in
    BatchCount
```

<!--output
{"columns":[{"name":"Result","type":"number"}],"rows":[{"Result":3}]}
-->
<!--steps
{
  "steps": [
    {
      "name": "AllIDs",
      "description": "Generates a list of 250 consecutive integers starting at 1, representing the full set of IDs to batch-process.",
      "output": {"columns":[{"name":"Value","type":"number"}],"rows":[{"Value":1},{"Value":2},{"Value":"..."},{"Value":250}]}
    },
    {
      "name": "Batches",
      "description": "Splits AllIDs into chunks of 100, producing a list of 3 sub-lists (two full batches of 100 and one partial batch of 50).",
      "output": {"columns":[{"name":"Value","type":"any"}],"rows":[{"Value":"{1..100}"},{"Value":"{101..200}"},{"Value":"{201..250}"}]}
    },
    {
      "name": "BatchCount",
      "description": "Counts the number of batches in the Batches list, confirming that 250 items split into chunks of 100 yields 3 batches.",
      "output": {"columns":[{"name":"Value","type":"number"}],"rows":[{"Value":3}]}
    },
    {
      "name": "Result",
      "description": "The final output — the scalar count 3, representing the number of batches produced.",
      "output": {"columns":[{"name":"Result","type":"number"}],"rows":[{"Result":3}]}
    }
  ]
}
-->

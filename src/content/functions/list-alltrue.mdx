---
title: "List.AllTrue"
slug: "list-alltrue"
category: "List"
description: "Returns true if all values in the list evaluate to true."
syntax: "List.AllTrue(list as list) as logical"
returnType: "logical"
returnDescription: "True if every value in the list is true (or truthy); false otherwise."
parameters:
  - name: "list"
    type: "list"
    required: true
    description: "The list of logical values to evaluate."
compatibility:
  pbiDesktop: true
  pbiService: true
  excelDesktop: true
  excelOnline: true
  dataflows: true
  fabricNotebooks: true
relatedFunctions:
  - "list-anytrue"
  - "list-matchesall"
  - "list-matchesany"
  - "list-select"
---

## Remarks

`List.AllTrue` returns `true` if every item in the list is logically true, and `false` as soon as any item is `false`. It is designed for lists of logical (`true`/`false`) values, and is typically paired with `List.Transform` to first convert a list of any type into a list of conditions.

An empty list returns `true` — this is known as vacuous truth: there are no items that could fail the condition. Null values in the list are treated as `false`, so a list containing any null will return `false`. Non-logical values (such as numbers or text) will cause a type error; use `List.Transform` to coerce or evaluate values before passing them in.

Compared to `List.MatchesAll`, which accepts a predicate function directly, `List.AllTrue` requires the list to already contain logical values. The typical pattern is `List.AllTrue(List.Transform(someList, each <condition>))`. Use `List.MatchesAll` when you prefer a single-step approach.

## Examples

### Example 1: All values are true

```powerquery
List.AllTrue({true, true, true})
```

<!--output
{"columns":[{"name":"Result","type":"logical"}],"rows":[{"Result":"true"}]}
-->

### Example 2: One value is false

```powerquery
List.AllTrue({true, false, true})
```

<!--output
{"columns":[{"name":"Result","type":"logical"}],"rows":[{"Result":"false"}]}
-->

### Example 3: Validate all unit prices in the Sales table are positive

Use `List.Transform` to produce the list of logical values, then check them all with `List.AllTrue`.

```powerquery
let
    Sales = #table(
        {"OrderID", "UnitPrice"},
        {{1, 25.00}, {2, 50.00}, {3, 15.00}, {4, 75.00}}
    ),
    Prices = Sales[UnitPrice],
    AllPositive = List.AllTrue(List.Transform(Prices, each _ > 0))
in
    AllPositive
```

<!--output
{"columns":[{"name":"Result","type":"logical"}],"rows":[{"Result":"true"}]}
-->

<!--steps
{
  "steps": [
    {
      "name": "Sales",
      "description": "Defines an inline table with OrderID and UnitPrice columns for four orders.",
      "output": {"columns":[{"name":"OrderID","type":"number"},{"name":"UnitPrice","type":"number"}],"rows":[{"OrderID":1,"UnitPrice":25},{"OrderID":2,"UnitPrice":50},{"OrderID":3,"UnitPrice":15},{"OrderID":4,"UnitPrice":75}]}
    },
    {
      "name": "Prices",
      "description": "Extracts the UnitPrice column as a list of numeric values.",
      "output": {"columns":[{"name":"Value","type":"number"}],"rows":[{"Value":25},{"Value":50},{"Value":15},{"Value":75}]}
    },
    {
      "name": "AllPositive",
      "description": "The final output — transforms each price into a true/false check for being greater than zero, then confirms all results are true.",
      "output": {"columns":[{"name":"Result","type":"logical"}],"rows":[{"Result":"true"}]}
    }
  ]
}
-->

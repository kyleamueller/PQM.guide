---
title: "Number.Cosh"
slug: "number-cosh"
category: "Number"
description: "Returns the hyperbolic cosine of a number."
syntax: "Number.Cosh(number as nullable number) as nullable number"
returnType: "number"
returnDescription: "The hyperbolic cosine of the number, always >= 1."
parameters:
  - name: "number"
    type: "number"
    required: true
    description: "The number whose hyperbolic cosine is computed."
compatibility:
  pbiDesktop: true
  pbiService: true
  excelDesktop: true
  excelOnline: true
  dataflows: true
  fabricNotebooks: true
relatedFunctions:
  - "number-sinh"
  - "number-tanh"
  - "number-exp"
---

## Remarks

`Number.Cosh` returns the hyperbolic cosine, defined mathematically as:

cosh(x) = (e^x + e^(−x)) / 2

Unlike the circular cosine, which oscillates between -1 and 1, the hyperbolic cosine grows without bound. Its minimum value is 1, occurring at x = 0. It is an **even function**: `cosh(−x) = cosh(x)`, meaning it is symmetric about the y-axis. For large positive or negative x, cosh grows roughly as e^|x| / 2.

Hyperbolic functions do not operate in degrees or radians — they take a real number argument directly and produce a real result. This contrasts with circular trigonometric functions like `Number.Cos`.

The fundamental identity `cosh²(x) − sinh²(x) = 1` is the hyperbolic analog of the Pythagorean identity. In data transformation, hyperbolic functions appear in physics models (catenary shape of cables), certain statistical distributions (e.g., the logistic function uses tanh), and financial risk models. Most Power Query users will not encounter them in everyday work.

## Examples

### Example 1: Hyperbolic cosine of 0 — minimum value is 1

```powerquery
Number.Cosh(0)
```

<!--output
{"columns":[{"name":"Result","type":"number"}],"rows":[{"Result":1}]}
-->

### Example 2: Hyperbolic cosine of 1

```powerquery
Number.Cosh(1)
```

<!--output
{"columns":[{"name":"Result","type":"number"}],"rows":[{"Result":1.5430806348152437}]}
-->

### Example 3: Verify the hyperbolic identity cosh²(x) − sinh²(x) = 1

```powerquery
let
    X = 3,
    Identity = Number.Cosh(X)^2 - Number.Sinh(X)^2
in
    Number.Round(Identity, 10)
```

<!--output
{"columns":[{"name":"Result","type":"number"}],"rows":[{"Result":1}]}
-->
<!--steps
{
  "steps": [
    {
      "name": "X",
      "description": "Assigns the input value 3 at which the hyperbolic identity will be evaluated.",
      "output": {"columns":[{"name":"Value","type":"number"}],"rows":[{"Value":3}]}
    },
    {
      "name": "Identity",
      "description": "Computes cosh(3)² − sinh(3)², which theoretically equals 1 by the fundamental hyperbolic identity (before floating-point rounding).",
      "output": {"columns":[{"name":"Value","type":"number"}],"rows":[{"Value":1.0000000000000002}]}
    },
    {
      "name": "Result",
      "description": "The final output — Number.Round applied to 10 decimal places, confirming the hyperbolic identity result rounds to exactly 1.",
      "output": {"columns":[{"name":"Result","type":"number"}],"rows":[{"Result":1}]}
    }
  ]
}
-->

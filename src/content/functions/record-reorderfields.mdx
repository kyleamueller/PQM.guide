---
title: "Record.ReorderFields"
slug: "record-reorderfields"
category: "Record"
description: "Returns a new record with fields reordered according to the specified list. Fields not in the list follow after in their original order."
syntax: "Record.ReorderFields(record as record, fieldOrder as list, optional missingField as nullable number) as record"
returnType: "record"
returnDescription: "A new record with fields in the specified order."
parameters:
  - name: "record"
    type: "record"
    required: true
    description: "The source record."
  - name: "fieldOrder"
    type: "list"
    required: true
    description: "A list of field name text values specifying the desired order."
  - name: "missingField"
    type: "number"
    required: false
    description: "Controls behavior when a specified field does not exist: MissingField.Error (default), MissingField.Ignore, or MissingField.UseNull."
compatibility:
  pbiDesktop: true
  pbiService: true
  excelDesktop: true
  excelOnline: true
  dataflows: true
  fabricNotebooks: true
relatedFunctions:
  - "record-renamefields"
  - "record-selectfields"
  - "record-removefields"
---

## Remarks

`Record.ReorderFields` returns a new record with fields placed in the order specified by `fieldOrder`. Fields not listed in `fieldOrder` are appended at the end, maintaining their original relative order from the source record. Field names and values are unchanged.

Record field order matters in Power Query because it affects the column order when records are expanded into table columns, and the order in which fields appear in `Record.ToTable` output. `Record.ReorderFields` is the record-level equivalent of `Table.ReorderColumns`.

The optional `missingField` parameter controls behavior when a name in `fieldOrder` does not exist in the record:
- `MissingField.Error` (default, `0`) — raises an error
- `MissingField.Ignore` (`1`) — skips missing names silently
- `MissingField.UseNull` (`2`) — inserts the specified field with a null value

A common pattern is to reorder fields before converting a record to a table with `Record.ToTable` or before using `Table.FromRecords`, to control the column order of the resulting table.

## Examples

### Example 1: Reorder all fields explicitly

```powerquery
Record.ReorderFields(
    [City = "New York", Name = "Alice", Age = 30],
    {"Name", "Age", "City"}
)
```

<!--output
{"columns":[{"name":"Name","type":"text"},{"name":"Age","type":"number"},{"name":"City","type":"text"}],"rows":[{"Name":"Alice","Age":30,"City":"New York"}]}
-->

### Example 2: Unlisted fields are appended at the end in original order

```powerquery
Record.ReorderFields(
    [D = 4, A = 1, C = 3, B = 2],
    {"A", "B"}
)
```

<!--output
{"columns":[{"name":"A","type":"number"},{"name":"B","type":"number"},{"name":"D","type":"number"},{"name":"C","type":"number"}],"rows":[{"A":1,"B":2,"D":4,"C":3}]}
-->

### Example 3: Reorder a Sales row to put OrderID and CustomerName first

Input: `Sales`

```powerquery
let
    Row = Sales{0},
    Reordered = Record.ReorderFields(Row, {"OrderID", "CustomerName", "Product"})
in
    Record.ToTable(Reordered)
```

<!--output
{"columns":[{"name":"Name","type":"text"},{"name":"Value","type":"any"}],"rows":[{"Name":"OrderID","Value":1},{"Name":"CustomerName","Value":"Alice"},{"Name":"Product","Value":"Widget A"},{"Name":"Category","Value":"Widgets"},{"Name":"UnitPrice","Value":25},{"Name":"Quantity","Value":4},{"Name":"OrderDate","Value":"2024-01-15"},{"Name":"Region","Value":"East"}]}
-->

<!--steps
{
  "steps": [
    {
      "name": "Row",
      "description": "Retrieves the first row of the Sales table as a record with all eight fields in their original order.",
      "output": {"columns":[{"name":"Field","type":"text"},{"name":"Value","type":"any"}],"rows":[{"Field":"OrderID","Value":1},{"Field":"CustomerName","Value":"Alice"},{"Field":"Product","Value":"Widget A"},{"Field":"Category","Value":"Widgets"},{"Field":"UnitPrice","Value":25},{"Field":"Quantity","Value":4},{"Field":"OrderDate","Value":"2024-01-15"},{"Field":"Region","Value":"East"}]}
    },
    {
      "name": "Reordered",
      "description": "Reorders the record so that OrderID, CustomerName, and Product appear first, with all remaining fields following in their original order.",
      "output": {"columns":[{"name":"Field","type":"text"},{"name":"Value","type":"any"}],"rows":[{"Field":"OrderID","Value":1},{"Field":"CustomerName","Value":"Alice"},{"Field":"Product","Value":"Widget A"},{"Field":"Category","Value":"Widgets"},{"Field":"UnitPrice","Value":25},{"Field":"Quantity","Value":4},{"Field":"OrderDate","Value":"2024-01-15"},{"Field":"Region","Value":"East"}]}
    },
    {
      "name": "Result",
      "description": "The final output — converts the reordered record to a two-column Name/Value table showing the new field order.",
      "output": {"columns":[{"name":"Name","type":"text"},{"name":"Value","type":"any"}],"rows":[{"Name":"OrderID","Value":1},{"Name":"CustomerName","Value":"Alice"},{"Name":"Product","Value":"Widget A"},{"Name":"Category","Value":"Widgets"},{"Name":"UnitPrice","Value":25},{"Name":"Quantity","Value":4},{"Name":"OrderDate","Value":"2024-01-15"},{"Name":"Region","Value":"East"}]}
    }
  ]
}
-->

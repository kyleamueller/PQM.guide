---
title: "List.ContainsAll"
slug: "list-containsall"
category: "List"
description: "Returns true if a list contains all of the values in another list."
syntax: "List.ContainsAll(list as list, values as list, optional equationCriteria as any) as logical"
returnType: "logical"
returnDescription: "true if every value from the second list is found in the first list."
parameters:
  - name: "list"
    type: "list"
    required: true
    description: "The list to search."
  - name: "values"
    type: "list"
    required: true
    description: "The list of values that must all be present."
  - name: "equationCriteria"
    type: "any"
    required: false
    description: "A comparer function for custom equality testing."
compatibility:
  pbiDesktop: true
  pbiService: true
  excelDesktop: true
  excelOnline: true
  dataflows: true
  fabricNotebooks: true
relatedFunctions:
  - "list-contains"
  - "list-containsany"
---

## Remarks

`List.ContainsAll` returns `true` only if every value in the second list appears in the first list. A single missing value causes it to return `false`. For a looser check that requires just one match, use `List.ContainsAny`.

The optional `equationCriteria` parameter accepts a comparer (such as `Comparer.OrdinalIgnoreCase`) for case-insensitive or culture-sensitive comparisons.

## Examples

### Example 1: Verify all required columns are present
Check whether a table has all of the columns needed for a downstream transformation.

```powerquery
let
    RequiredColumns = {"OrderID", "CustomerID", "Amount", "Region"},
    ActualColumns   = Table.ColumnNames(Sales),
    AllPresent      = List.ContainsAll(ActualColumns, RequiredColumns)
in
    #table({"AllColumnsPresent"}, {{AllPresent}})
```

<!--output
{"columns":[{"name":"AllColumnsPresent","type":"logical"}],"rows":[{"AllColumnsPresent":true}]}
-->

<!--steps
{
  "steps": [
    {
      "name": "RequiredColumns",
      "description": "Defines the list of column names that must be present in the table for downstream processing.",
      "output": {"columns":[{"name":"Value","type":"text"}],"rows":[{"Value":"OrderID"},{"Value":"CustomerID"},{"Value":"Amount"},{"Value":"Region"}]}
    },
    {
      "name": "ActualColumns",
      "description": "Retrieves the actual column names from the Sales table as a list.",
      "output": {"columns":[{"name":"Value","type":"text"}],"rows":[{"Value":"OrderID"},{"Value":"CustomerName"},{"Value":"Product"},{"Value":"Category"},{"Value":"UnitPrice"},{"Value":"Quantity"},{"Value":"OrderDate"},{"Value":"Region"}]}
    },
    {
      "name": "AllPresent",
      "description": "Checks whether every required column name appears in the actual columns list.",
      "output": {"columns":[{"name":"Value","type":"logical"}],"rows":[{"Value":"true"}]}
    },
    {
      "name": "Result",
      "description": "The final output — wraps the logical result in a single-row table labeled AllColumnsPresent.",
      "output": {"columns":[{"name":"AllColumnsPresent","type":"logical"}],"rows":[{"AllColumnsPresent":true}]}
    }
  ]
}
-->

### Example 2: Check for missing values
When not all values are present, the function returns false.

```powerquery
let
    Source    = {"a", "b", "c"},
    CheckList = {"a", "b", "d"},
    Result    = List.ContainsAll(Source, CheckList)
in
    Result
```

<!--output
{"columns":[{"name":"Result","type":"logical"}],"rows":[{"Result":false}]}
-->

<!--steps
{
  "steps": [
    {
      "name": "Source",
      "description": "Defines the list to search within, containing three text values.",
      "output": {"columns":[{"name":"Value","type":"text"}],"rows":[{"Value":"a"},{"Value":"b"},{"Value":"c"}]}
    },
    {
      "name": "CheckList",
      "description": "Defines the list of values that must all be present in Source; note that \"d\" is not in Source.",
      "output": {"columns":[{"name":"Value","type":"text"}],"rows":[{"Value":"a"},{"Value":"b"},{"Value":"d"}]}
    },
    {
      "name": "Result",
      "description": "The final output — returns false because \"d\" from CheckList is not found in Source.",
      "output": {"columns":[{"name":"Result","type":"logical"}],"rows":[{"Result":false}]}
    }
  ]
}
-->

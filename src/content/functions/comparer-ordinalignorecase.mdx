---
title: "Comparer.OrdinalIgnoreCase"
slug: "comparer-ordinalignorecase"
category: "Comparer"
description: "A case-insensitive ordinal comparer function."
syntax: "Comparer.OrdinalIgnoreCase as function"
returnType: "function"
returnDescription: "A comparer function that performs case-insensitive ordinal comparisons."
parameters: []
compatibility:
  pbiDesktop: true
  pbiService: true
  excelDesktop: true
  excelOnline: true
  dataflows: true
  fabricNotebooks: true
relatedFunctions:
  - "comparer-ordinal"
  - "comparer-fromculture"
  - "list-distinct"
---

## Remarks

`Comparer.OrdinalIgnoreCase` is a built-in comparer **function** that performs case-insensitive ordinal comparisons. It treats uppercase and lowercase versions of the same letter as equal (e.g., "A" equals "a").

This comparer is commonly used with `List.Distinct` to deduplicate text that differs only by casing, and with `List.Sort` or `Table.Sort` to produce case-insensitive sorted results.

## Examples

### Example 1: Case-insensitive distinct on employee names

Demonstrate that `List.Distinct` with `Comparer.OrdinalIgnoreCase` treats differently-cased duplicates as equal. Here we add a duplicate "ALICE SMITH" to show the deduplication.

Input: `Employees`

```powerquery
let
    Names = Table.Column(Employees, "FullName"),
    NamesWithDup = List.Combine({Names, {"ALICE SMITH"}}),
    Unique = List.Distinct(NamesWithDup, Comparer.OrdinalIgnoreCase)
in
    #table({"FullName"}, List.Transform(Unique, each {_}))
```

<!--output
{"columns":[{"name":"FullName","type":"text"}],"rows":[{"FullName":"alice smith"},{"FullName":"BOB JONES"},{"FullName":"Charlie Brown"},{"FullName":"diana PRINCE"},{"FullName":"Eve Martinez"},{"FullName":"frank lee"}]}
-->

<!--steps
{
  "steps": [
    {
      "name": "Names",
      "description": "Table.Column extracts the FullName column from Employees as a plain list — 6 values, mixed casing as stored.",
      "output": {"columns":[{"name":"Value","type":"text"}],"rows":[{"Value":"alice smith"},{"Value":"BOB JONES"},{"Value":"Charlie Brown"},{"Value":"diana PRINCE"},{"Value":"Eve Martinez"},{"Value":"frank lee"}]}
    },
    {
      "name": "NamesWithDup",
      "description": "List.Combine appends a second list containing \"ALICE SMITH\" — now 7 values. This is the duplicate we want deduplication to catch.",
      "output": {"columns":[{"name":"Value","type":"text"}],"rows":[{"Value":"alice smith"},{"Value":"BOB JONES"},{"Value":"Charlie Brown"},{"Value":"diana PRINCE"},{"Value":"Eve Martinez"},{"Value":"frank lee"},{"Value":"ALICE SMITH"}]}
    },
    {
      "name": "Unique",
      "description": "List.Distinct with Comparer.OrdinalIgnoreCase treats \"ALICE SMITH\" as equal to \"alice smith\" and removes it — back to 6 values. The first occurrence wins.",
      "output": {"columns":[{"name":"Value","type":"text"}],"rows":[{"Value":"alice smith"},{"Value":"BOB JONES"},{"Value":"Charlie Brown"},{"Value":"diana PRINCE"},{"Value":"Eve Martinez"},{"Value":"frank lee"}]}
    },
    {
      "name": "Result",
      "description": "#table wraps the deduplicated list as a single-column table with a FullName header — the final output.",
      "output": {"columns":[{"name":"FullName","type":"text"}],"rows":[{"FullName":"alice smith"},{"FullName":"BOB JONES"},{"FullName":"Charlie Brown"},{"FullName":"diana PRINCE"},{"FullName":"Eve Martinez"},{"FullName":"frank lee"}]}
    }
  ]
}
-->

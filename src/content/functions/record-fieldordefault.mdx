---
title: "Record.FieldOrDefault"
slug: "record-fieldordefault"
category: "Record"
description: "Returns the value of a named field from a record, or a default value if the field does not exist."
syntax: "Record.FieldOrDefault(record as record, field as text, optional defaultValue as nullable any) as nullable any"
returnType: "any"
returnDescription: "The value of the specified field, or defaultValue (null if omitted) when the field is absent."
parameters:
  - name: "record"
    type: "record"
    required: true
    description: "The record to retrieve the field from."
  - name: "field"
    type: "text"
    required: true
    description: "The name of the field to retrieve."
  - name: "defaultValue"
    type: "any"
    required: false
    description: "The value to return when the field does not exist. Defaults to null."
compatibility:
  pbiDesktop: true
  pbiService: true
  excelDesktop: true
  excelOnline: true
  dataflows: true
  fabricNotebooks: true
relatedFunctions:
  - "record-field"
  - "record-hasfields"
  - "record-fieldnames"
---

## Remarks

`Record.FieldOrDefault` is the **safe alternative** to direct field access (`record[FieldName]`) and `Record.Field`. Direct field access raises an error if the field does not exist; `Record.FieldOrDefault` returns `null` by default or a custom fallback value you specify.

This is essential when working with semi-structured or inconsistent data — for example, JSON expanded into records where some rows have fields that others do not, or API responses where optional fields are absent rather than null.

The comparison to related approaches:

| Approach | Missing field behavior |
|---|---|
| `record[FieldName]` | Raises error |
| `Record.Field(record, name)` | Raises error |
| `Record.FieldOrDefault(record, name)` | Returns null |
| `Record.FieldOrDefault(record, name, default)` | Returns default value |

The `defaultValue` parameter accepts any type and defaults to `null` when omitted. You can use any expression as the default, including function calls.

## Examples

### Example 1: Retrieve a field that exists — returns its value

```powerquery
Record.FieldOrDefault([Name = "Alice", Age = 30], "Name")
```

<!--output
{"columns":[{"name":"Result","type":"text"}],"rows":[{"Result":"Alice"}]}
-->

### Example 2: Field is absent — returns null by default

```powerquery
Record.FieldOrDefault([Name = "Alice", Age = 30], "Email")
```

<!--output
{"columns":[{"name":"Result","type":"any"}],"rows":[{"Result":null}]}
-->

### Example 3: Specify a custom fallback value for missing fields

```powerquery
Record.FieldOrDefault([Name = "Alice"], "Email", "no-email@example.com")
```

<!--output
{"columns":[{"name":"Result","type":"text"}],"rows":[{"Result":"no-email@example.com"}]}
-->

### Example 4: Safely extract an optional "Region" field from each Sales row

Input: `Sales`

```powerquery
Table.AddColumn(
    Table.SelectColumns(Table.FirstN(Sales, 4), {"OrderID", "CustomerName"}),
    "Region",
    each Record.FieldOrDefault(_, "Region", "Unknown"),
    type text
)
```

<!--output
{"columns":[{"name":"OrderID","type":"number"},{"name":"CustomerName","type":"text"},{"name":"Region","type":"text"}],"rows":[{"OrderID":1,"CustomerName":"Alice","Region":"East"},{"OrderID":2,"CustomerName":"Bob","Region":"West"},{"OrderID":3,"CustomerName":"Charlie","Region":"East"},{"OrderID":4,"CustomerName":"Alice","Region":"North"}]}
-->

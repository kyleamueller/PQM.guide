---
title: "Text.Proper"
slug: "text-proper"
category: "Text"
description: "Capitalizes the first letter of each word in a text value."
syntax: "Text.Proper(text as nullable text, optional culture as nullable text) as nullable text"
returnType: "text"
returnDescription: "The input text with the first letter of each word capitalized and the rest lowercased."
parameters:
  - name: "text"
    type: "text"
    required: true
    description: "The text value to convert to proper case."
  - name: "culture"
    type: "text"
    required: false
    description: "A culture code (e.g., \"en-US\") that determines locale-specific casing rules."
compatibility:
  pbiDesktop: true
  pbiService: true
  excelDesktop: true
  excelOnline: true
  dataflows: true
  fabricNotebooks: true
relatedFunctions:
  - "text-upper"
  - "text-lower"
---

## Remarks

`Text.Proper` converts text to title case — the first letter of each word is capitalized and the remaining letters are lowercased. This is ideal for cleaning up names and titles that have inconsistent casing.

Note that `Text.Proper` treats any non-letter character as a word boundary, so hyphens, apostrophes, and other punctuation trigger capitalization of the next letter.

## Examples

### Example 1: Proper case employee names

Standardize the mixed-case FullName column to proper case.

Input: `Employees`

```powerquery
Table.TransformColumns(
    Table.SelectColumns(Employees, {"FullName"}),
    {"FullName", Text.Proper, type text}
)
```

<!--output
{"columns":[{"name":"FullName","type":"text"}],"rows":[{"FullName":"Alice Smith"},{"FullName":"Bob Jones"},{"FullName":"Charlie Brown"},{"FullName":"Diana Prince"},{"FullName":"Eve Martinez"},{"FullName":"Frank Lee"}]}
-->

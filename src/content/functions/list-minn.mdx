---
title: "List.MinN"
slug: "list-minn"
category: "List"
description: "Returns the N smallest values from a list, or values meeting a condition, in ascending order."
syntax: "List.MinN(list as list, countOrCondition as any, optional comparisonCriteria as any) as list"
returnType: "list"
returnDescription: "A list of the N smallest values, sorted in ascending order."
parameters:
  - name: "list"
    type: "list"
    required: true
    description: "The list to search."
  - name: "countOrCondition"
    type: "any"
    required: true
    description: "A number specifying how many minimum values to return, or a condition function."
  - name: "comparisonCriteria"
    type: "any"
    required: false
    description: "An optional comparison criteria or comparer function to control ordering."
compatibility:
  pbiDesktop: true
  pbiService: true
  excelDesktop: true
  excelOnline: true
  dataflows: true
  fabricNotebooks: true
relatedFunctions:
  - "list-maxn"
  - "list-min"
  - "list-sort"
  - "list-firstn"
  - "list-lastn"
---

## Remarks

`List.MinN` returns the bottom `N` values from a list as a **list** sorted in ascending order. It is the multi-value counterpart of `List.Min`, which returns a single scalar.

When passed a number `N`, it returns the `N` smallest values from the list. If `N` exceeds the list length, the entire list is returned sorted ascending. Null values are ignored. Duplicates are preserved — if the Nth-smallest value appears multiple times, all occurrences are included.

When passed a condition function, the list is first sorted ascending and the condition is applied to each item in that order; items are collected while the condition returns `true`. This is useful for "give me all values below a threshold" scenarios.

For the single lowest value, use `List.Min`. To get the highest values instead, use `List.MaxN`. To sort and slice a full list, combining `List.Sort` and `List.FirstN` is an equivalent and readable alternative.

## Examples

### Example 1: Return the 3 smallest numbers

```powerquery
List.MinN({15, 3, 42, 8, 27, 1}, 3)
```

<!--output
{"columns":[{"name":"Result","type":"any"}],"rows":[{"Result":"{1, 3, 8}"}]}
-->

### Example 2: Return values while they are below a threshold

```powerquery
List.MinN({10, 20, 30, 40, 50}, each _ <= 30)
```

<!--output
{"columns":[{"name":"Result","type":"any"}],"rows":[{"Result":"{10, 20, 30}"}]}
-->

### Example 3: Find the 3 lowest-cost items

```powerquery
let
    Costs = {1200, 850, 3400, 450, 2100, 975},
    BottomThree = List.MinN(Costs, 3)
in
    BottomThree
```

<!--output
{"columns":[{"name":"Result","type":"number"}],"rows":[{"Result":450},{"Result":850},{"Result":975}]}
-->

<!--steps
{
  "steps": [
    {
      "name": "Costs",
      "description": "Defines a list of item costs to find the lowest values from.",
      "output": {"columns":[{"name":"Value","type":"number"}],"rows":[{"Value":1200},{"Value":850},{"Value":3400},{"Value":450},{"Value":2100},{"Value":975}]}
    },
    {
      "name": "BottomThree",
      "description": "The final output — returns the 3 smallest cost values from the list, sorted in ascending order.",
      "output": {"columns":[{"name":"Result","type":"number"}],"rows":[{"Result":450},{"Result":850},{"Result":975}]}
    }
  ]
}
-->

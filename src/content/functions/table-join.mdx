---
title: "Table.Join"
slug: "table-join"
category: "Table"
description: "Joins two tables on key columns, returning a flat combined table."
syntax: "Table.Join(table1 as table, key1 as any, table2 as table, key2 as any, optional joinKind as nullable number, optional joinAlgorithm as nullable number, optional keyEqualityComparers as nullable list) as table"
returnType: "table"
returnDescription: "A table containing columns from both inputs joined on the specified keys."
parameters:
  - name: "table1"
    type: "table"
    required: true
    description: "The left table."
  - name: "key1"
    type: "any"
    required: true
    description: "The join key column name (text) or list of column names for table1."
  - name: "table2"
    type: "table"
    required: true
    description: "The right table."
  - name: "key2"
    type: "any"
    required: true
    description: "The join key column name (text) or list of column names for table2."
  - name: "joinKind"
    type: "number"
    required: false
    description: "Join type: JoinKind.Inner (0, default), LeftOuter (1), RightOuter (2), FullOuter (3), LeftAnti (4), RightAnti (5)."
  - name: "joinAlgorithm"
    type: "number"
    required: false
    description: "Algorithm hint for performance tuning."
  - name: "keyEqualityComparers"
    type: "list"
    required: false
    description: "Custom comparers for key matching (e.g., case-insensitive)."
compatibility:
  pbiDesktop: true
  pbiService: true
  excelDesktop: true
  excelOnline: true
  dataflows: true
  fabricNotebooks: true
relatedFunctions:
  - "table-nestedjoin"
  - "table-selectrows"
---

## Remarks

`Table.Join` is a flat join — it returns a single table with all columns from both inputs side by side, similar to a SQL `JOIN`. If both tables share a column name (other than the key), the duplicate is renamed with a suffix (e.g. `CustomerID.1`).

For most Power Query workflows, `Table.NestedJoin` followed by `Table.ExpandTableColumn` is preferred because it gives finer control over which columns to expand and avoids automatic column renaming. Use `Table.Join` when you want a flat result immediately and don't mind resolving column name collisions manually.

When both key columns have the same name, M retains the left table's key and renames the right table's key with `.1`. Rename or drop the duplicate before exposing the result downstream.

## Examples

### Example 1: Inner join Sales to Customers

Join Sales to Customers on CustomerID to add customer name and country to each order row.

Input: `Sales`

```powerquery
let
    Joined = Table.Join(Sales, "CustomerID", Customers, "CustomerID", JoinKind.Inner),
    Result = Table.SelectColumns(Joined, {"OrderID", "CustomerID", "Amount", "Name", "Country"})
in
    Result
```

<!--output
{"columns":[{"name":"OrderID","type":"number"},{"name":"CustomerID","type":"text"},{"name":"Amount","type":"number"},{"name":"Name","type":"text"},{"name":"Country","type":"text"}],"rows":[{"OrderID":1,"CustomerID":"C001","Amount":250.00,"Name":"Alice Johnson","Country":"USA"},{"OrderID":2,"CustomerID":"C002","Amount":120.00,"Name":"Bob Smith","Country":"Canada"},{"OrderID":3,"CustomerID":"C001","Amount":75.00,"Name":"Alice Johnson","Country":"USA"},{"OrderID":4,"CustomerID":"C003","Amount":500.00,"Name":"Carol Davis","Country":"USA"},{"OrderID":5,"CustomerID":"C002","Amount":90.00,"Name":"Bob Smith","Country":"Canada"},{"OrderID":6,"CustomerID":"C004","Amount":200.00,"Name":"David Lee","Country":"UK"},{"OrderID":7,"CustomerID":"C001","Amount":350.00,"Name":"Alice Johnson","Country":"USA"},{"OrderID":8,"CustomerID":"C003","Amount":160.00,"Name":"Carol Davis","Country":"USA"}]}
-->

### Example 2: Left anti-join to find unmatched rows

Return Sales rows with no matching CustomerID in the Customers table.

Input: `Sales`

```powerquery
Table.Join(Sales, "CustomerID", Customers, "CustomerID", JoinKind.LeftAnti)
```

<!--output
{"columns":[{"name":"OrderID","type":"number"},{"name":"CustomerID","type":"text"},{"name":"Amount","type":"number"},{"name":"Region","type":"text"},{"name":"Status","type":"text"}],"rows":[]}
-->
